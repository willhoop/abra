# FIX BATCH M5 / M7 / M8 — the second three mechanisms of the board-material 77

ENGINE, 2026-09-04. Three mechanisms, landed and probed **one at a time**, then measured **once**.

- **BEFORE release** `f3504e5f88d6` — the AFTER of the first batch, artifact
  `data/verification/fix-batch-M1M3M4.json` (2026-09-04T09:22:08Z).
- **AFTER release `9b449a41c865`** (`engine/medicham2-browser.js` digest `c0fb3bcc324c`), 26 files
  frozen. The frozen bytes were checked against the live file before the run: both `c0fb3bcc324c`.
- **Artifact:** `data/verification/fix-batch-M5M7M8.json`, generated 2026-09-04T10:01:36Z.
  **`data/game-differential.json` was NOT written** — its mtime is still 2026-09-03 22:16:45.

`engine/medicham2-browser.js` is the only engine file that moved between the two releases; the three
new probes are not in the frozen set. That is still two photographs and it is said here rather than
implied.

---

## 0. THE SCOREBOARD, CALLED IN WRITING BEFORE THE RUN

Pins on both sides: arm `middle`, `--end-state`, census `9446a684709d` (via
`--census data/verification/census-pin-9446a684709d.json`), pool `0d103fb9fa87` (via
`--team-store data/team-pool-frozen`), `--steering empirical` (`empirical-click/v1`), `--games 1200`
— a PAIR budget yielding **961 games PLAYED**, which is the quantity quoted throughout.

| quantity | baseline (`f3504e5f88d6`) |
|---|---|
| **board-material** (`state.games` 961 − `state.games_board_never_diverged` 900) | **61 of 961** |
| protocol first-divergence (`diverged`) | **161** |
| VOID (instrument desync) | **7** |

**All three mechanisms come out of the 77, i.e. out of real ladder games, so the POOL was called to
move on all three. The LAB — `data/mechanics-census.json` — was called to STAY at 829/829, because
none of the three adds a mechanic row; every one corrects a row that already fires.** The census was
regenerated and diffed after EACH fix, not only at the end.

- **M5 (4 games: 14, 46, 69, 70).** Parts on both scoreboards today — `-fail` against `-damage` in the
  narration, the redirector's HP on the board. Called board −3 to −4, protocol −3 to −4. Stated risk:
  the fix changes the board from that turn on, so the rest of the game is a different game and a later
  divergence can appear where none was.
- **M7 (4 games: 3, 30, 33, 43).** Called board −2 to −4 **with churn**, protocol −2 to −4. The blast
  radius was called wider than four in advance: the address restamp also fires on the `randomTarget`
  re-roll, so every `acc`/`crit`/`dmg`/`sec` draw inside an Outrage-class action changes address too.
  That is a correctness fix and it MOVES DICE, so a game that agreed by luck could newly part.
- **M8 (4 games: 42, 48, 61, 73).** Called protocol −3 to −4, board −2 to −4. #541 says one of the
  four board leaves (`p1.party.metagross.ability`) runs OPPOSITE to the derivation and is deliberately
  NOT attributed, so at least one was expected not to clear.

**Called: board-material 61 → 48-56, most likely ~52. Protocol 161 → 148-156, most likely ~152.
VOID 7 → 5-7. Census unchanged.**

Also called in advance: the 50 after are **not** guaranteed to be a subset of the 61 before.

---

## 1. RESULT

| quantity | BEFORE `f3504e5f88d6` | AFTER `9b449a41c865` | called | |
|---|---|---|---|---|
| games played | 961 | 961 | — | |
| **BOARD-MATERIAL** | **61** | **50** | 48-56, ~52 | **hit** |
| protocol first-divergence | 161 | 150 | 148-156, ~152 | **hit** |
| VOID | 7 | 7 | 5-7 | **hit** (top of the range) |
| threw | 1 | 1 | — | |
| census (the LAB) | 829 live / 829 probed | 829 live / 829 probed | unchanged | **hit** |

**Board-material 61 → 50, a fall of 11. Protocol 161 → 150, a fall of 11. 4 of 4 calls hit.**

The run was performed TWICE — once without `--write` (the brief's command omits it, and `--out`
only redirects a write, so the first run produced no artifact) and once with `--write --out`. Both
read **961 / 911 / 150 / 7 / 1 threw**, identical on every headline, which is a free reproducibility
check on a run that pins three things.

### The leaf-level evidence, which is what attributes it

Every changed board family FELL. **Not one went up**, which is the answer to the churn risk called in
§0 for M7.

| board family | BEFORE | AFTER | |
|---|---|---|---|
| `active[].stall` | 11 | **5** | M7 — the single largest move in the table |
| `pp[].protect` | 7 | 5 | M7 |
| `active[].vol.encore` | 1 | **0** | M7 |
| `party.ability` | 2 | **0** | M8 |
| `active[].ability` | 6 | 3 | M8 |
| `active[].boosts.atk` | 5 | 3 | M8 — the acquired Intimidate |
| `party.boosts.atk` | 5 | 3 | M8 |
| `pp[].ragepowder` | 1 | **0** | M5 |
| `active[].hp` | 37 | 32 | M5 + downstream |
| `party.hp` | 35 | 31 | M5 + downstream |
| `active[].item` | 10 | 8 | downstream |
| `active[].species` / `active[].types` | 7 / 7 | 5 / 5 | downstream |
| `active[].status`, `party.status`, `party.species`, `party.types`, `tailwind`, `vol.charging` | — | **unchanged** | the untouched groups, as called |

**No Sucker Punch, Follow Me, Rage Powder, Encore, Skill Swap, Mummy or Wandering Spirit cause
survives anywhere in the board-material worklist** (44 causes, 45 games — grep of that block returns
zero). `acc encore [sd only]`, which stood in the baseline's `unshared_address_shapes` table, is gone.

Headline metrics, all in the right direction, none regressed:

```
diverged                                    161 -> 150
state.games_board_never_diverged            900 -> 911
state.game_agreement                     0.9365 -> 0.9480
state.turn_boundary_agreement            0.9803 -> 0.9856
state.protocol_diverged_board_held_longer     8 -> 6
state.turn1.protocol_parted_during_turn1      9 -> 7
state.protocol_diverged_board_never_did     105 -> 105   (unchanged — the UNCAUSED set is not this batch)
```

---

## 2. WHAT EACH MECHANISM WAS, AND WHAT ITS PROBE DID RED-FIRST

Every one was **confirmed at the line, or measured, before a byte moved.**

### M5 — Sucker Punch does not fail into a redirector. `tests/probe_sucker_redirect_refusal.js`

**Authority, re-opened, in the order it runs.** Champions' `scripts.ts` overrides `formeChange`,
`clearVolatile`, `getActionSpeed`, `statModify`, `calculatePP`, `canTerastallize`, `canMegaEvo`,
`modifyDamage`, `spreadMoveHit` and `hitStepMoveHitLoop` — and **not** `useMoveInner`,
`getMoveTargets` or `suckerpunch`, which §0 of the probe greps on every run.

```
sim/battle-actions.ts:466   const { targets, pressureTargets } = pokemon.getMoveTargets(move, target);
sim/battle-actions.ts:468   target = targets[targets.length - 1];      // "in case of redirection"
sim/pokemon.ts:829-835        if (activePerHalf > 1 && !move.tracksTarget)
                                target = this.battle.priorityEvent('RedirectTarget', ...)
sim/battle-actions.ts:590   singleEvent('Try', move, null, pokemon, targets[0], move)
data/moves.ts:18399         onTry(source, target) { const action = this.queue.willMove(target); ... }
```

So the draw happens FIRST and `onTry` is asked of `targets[0]`. A Follow Me / Rage Powder user moved
at +2 and is off the queue by the time Sucker Punch's +1 resolves, so `willMove` finds nothing and the
authority prints `-fail`. This engine evaluated the refusal at `medicham2-browser.js:29844` against
`a.target` — the ORIGINAL aim — and redirected 137 lines later.

**Confirmed live TODAY, red-first, before any edit:** arm 1 `DIFFERS`, `p1.party.maushold.hp` ours 62
against the authority's 149, with the three controls green.

**Fix:** the whole `failsIfTargetNotAttacking` block MOVED to below the draw and below the
priority-blocking gate — which is the authority's own order (`runEvent('TryMove')` is
battle-actions.ts:487, the move's own `Try` is :590) — and reads `targets[0]`. **ROADMAP #403's queue
clause was not touched**: `queueWillMove` still asks whether the body has an outstanding action; all
that moved is WHICH body it is asked about.

**Probe, red-first with `MEDI_SUCKER_AIMS_PRE_REDIRECT=1`:**

| arm | clean | knob armed |
|---|---|---|
| followme-draws-the-sucker-punch | IDENTICAL | DIFFERS `p1.party.maushold.hp` 62 / 149 |
| redirect-lands-an-ordinary-attack (CONTROL over the DRAW) | IDENTICAL | IDENTICAL |
| sucker-punch-lands-with-no-redirector (CONTROL over the FIX) | IDENTICAL | IDENTICAL |
| **sucker-punch-still-fails-into-a-mover (CONTROL over #403)** | IDENTICAL | **IDENTICAL** |

§2 reads the AUTHORITY's own HP on three of the arms — the redirector untouched on arm 1, damaged on
arm 2, the named foe damaged on arm 3 — because "both engines agree" is also what two engines that
both refuse every Sucker Punch look like. §3 reads `MEDSEEN.suckerRefusedRedirectedTarget`, a counter
kept APART from #403's `suckerRefusedAlreadyMovedTarget`: 2 clean, 0 under the knob.

### M7 — the Encore'd Protect and the `stall` die. `tests/probe_encore_stall_address.js`

**#543 named two candidate causes and told ENGINE to suspect the die address before the logic. IT IS
THE ADDRESS, and it was measured rather than argued.** One staged turn — a Prankster Whimsicott
Encoring a Snorlax that Protected the turn before and clicked Crunch this turn — with both address
logs printed side by side, before any byte moved:

```
authority   20260813|2|any|protect|p20|0     the stall die, named for the FORCED move
this engine 20260813|2|any|crunch|p10|0      the same die, a different address
```

Two addresses that cannot match are two INDEPENDENT dice. The CONTROL — the identical board with no
Encore on it — shared **5 of 5** addresses before the fix as well as after.

**Authority:** `runMove` writes the active move ONCE and writes it AFTER every rewrite of the action —
`OverrideAction` at battle-actions.ts:227, `getRandomTarget` at :233, `setActiveMove(move, pokemon,
target)` at :244 — and the die is `stall.onStallMove`'s `randomChance(1, counter)`, raised from
Protect's `onPrepareHit` inside `useMoveInner`, i.e. after `setActiveMove`. Champions carries no
`stall` condition row and does not override `runMove`; its Protect row changes the PP only. All three
are grepped by the probe on every run.

**This engine** stamped `MID_MOVE`/`MID_TGT` at the top of the action (line 24111) and restamped them
only at the `|move|` announcement — which is BELOW `_shieldGate`.

**Fix:** the three-line stamp becomes ONE function, `_midWriteActionAddr`, called at the top of the
action as before AND again at the dispatch re-aim — `setActiveMove`'s own position, after Encore's
override, after the `randomTarget` re-roll and after the slot re-aim. `MID_ADDR_CAT` was NOT touched:
the instrument's `{ stall: 'any' }` mapping is correct and the field VALUES were wrong.

**Probe, red-first with `MEDI_MID_ADDR_PRE_OVERRIDE=1`:**

| arm | clean | knob armed |
|---|---|---|
| encore-forces-the-second-protect | all addresses shared | RED — `…\|any\|crunch\|p10\|0` unshared |
| **plain-second-protect (CONTROL)** | shared | **shared** |
| encore-forces-an-attack | shared | RED — the address moves with no shield under it |
| encore-second-protect-that-parts | IDENTICAL | RED on the address **and the BOARD**: `p2.party.snorlax.hp` 188 / 235, `status` `par` / `""` |

The fourth arm exists because **arm 1's board does not part on its own seed and the probe says so out
loud**: two independent coins agree about two thirds of the time, so the ADDRESS is the claim and the
board is the consequence. Arm 4 is arm 1 with three padding turns in front, which changes the turn
field of the address and therefore the pair of numbers drawn. §3 asserts
`MEDSEEN.midAddrMovedAtOverride > 0` — the restamp is load-bearing rather than a redundant write; it
reads 3 clean and 0 under the knob.

### M8 — the contact ability transfer. `tests/probe_contact_ability_transfer.js`

**The framing in #541 is the one that survives and the diagnosis report's is not.** §0 of the probe
walks the format and prints every legal carrier of every `rewritesAbilityOnContact` ability:
**`wanderingspirit` → Runerigus, `mummy` → Cofagrigus, `lingeringaroma` → no legal carrier.** The MOVE
Skill Swap is not in this population at all.

**Authority, both handlers:** `Battle#skillSwap` (sim/battle.ts:1311) announces on the **SOURCE, which
is the ATTACKER**, with both ability names and `[of] <holder>`, and ends with **two**
`singleEvent('Start', ...)` — the holder's new ability first, then the attacker's. `Pokemon#setAbility`
takes its own `case 'mummy':` and announces on the **HOLDER** with the ATTACKER in field 4 and
`[ability] <old>` in field 5, then one `Start`. `/data/mods/champions/abilities.ts` mentions neither.

**#541's framing was confirmed at the line and by the probe: the two WRITES always picked the right
bodies.** What was wrong was the ANNOUNCEMENT's body and shape, and that no acquired ability's
`onStart` ran at all. Red-first, before any edit:

```
SD  |-activate|p2a: Scrafty|Skill Swap|Wandering Spirit|Intimidate|[of] p1a: Runerigus
ME  |-activate|p1a: Runerigus|ability: intimidate

SD  |-activate|p1a: Cofagrigus|ability: Mummy|p2a: Scrafty|[ability] Intimidate
ME  |-activate|p1a: Cofagrigus|ability: mummy
```

and the board half, on the same run: `p2.active[0].boosts.atk` ours **0** against the authority's
**−1**, on all four of that side's leaves — the acquired Intimidate that never fired.

**`TR.abswap` — the correct one-line emitter — has existed in this file since the MOVE branch was
wired and WIRE 80 never called it.** That is the capability-present-but-uncalled shape this project is
named for.

**Fix, three parts, all at WIRE 80:**
1. the swap calls `TR.abswap` and the infection calls a new `TR.abinfect`, built beside it and
   modelled on `setAbility`'s `mummy` case;
2. both acquired abilities' `Start` is run through `applyEntryEffects` + `applyEntryDrops` — the pair
   every other caller of `singleEvent('Start', ability, ...)` in this file already uses (the lead pass,
   the refill, the mega). A private copy here would be a second implementation of an ability's entry
   handler. `MEDFAILS.acquiredAbilityStartNoSide` is the loud half and must read 0;
3. the swap goes through `abRewrite` instead of a bare assignment, which is the same door the MOVE
   Skill Swap has always used — **so a swapped ability is now undone by leaving the field**, which the
   bare assignment never was. That is a second, separate defect and it has its own probe arm rather
   than riding along unmeasured.

**Probe, red-first with `MEDI_CONTACT_ABILITY_LEGACY=1`:**

| arm | board clean | board knob | protocol clean | protocol knob |
|---|---|---|---|---|
| wanderingspirit-swaps-with-an-intimidate-attacker | IDENTICAL | **DIFFERS** (4 atk leaves) | agreed | **RED** |
| mummy-infects-the-attacker | IDENTICAL | IDENTICAL | agreed | **RED** |
| **no-contact-no-transfer (CONTROL)** | IDENTICAL | **IDENTICAL** | agreed | **agreed** |
| the-swapped-ability-goes-when-the-body-leaves | IDENTICAL | **DIFFERS** `p2.party.scrafty.ability` ours `wanderingspirit` / authority `intimidate` | agreed | **RED** |

The Mummy arm's board deliberately does NOT move under the knob — that write was already on the right
body and `mummy` has no `onStart`, so its whole defect is the announcement. Labelling it "expected RED"
on the board would have been a false expectation dressed as a control, and the probe splits
`GOV_BOARD` from `GOV_PROTO` to say so.

The protocol assertion is `playGame`'s own `div` — **the differential's aligner against the
authority's stream**, not a copy of the expected line held in the probe.

---

## 3. WHAT WENT WRONG ON THE WAY, SAID PLAINLY

- **The M8 probe was VACUOUS-GREEN on its first run and the fix would have been credited for it.** The
  holder clicked Protect, so the Crunch never reached `onDamagingHit` on EITHER engine, and all four
  arms read IDENTICAL over a mechanic that had not run. Caught by §3's "both modes actually fired"
  assertion reading `undefined`, i.e. by the counters, not by the boards. The holder clicks Shadow
  Claw at the far foe now.
- **The M7 diagnostic's first script never reached the roll**, because the Encore user was slower than
  the victim and `stall`'s `duration: 2` had expired by the turn the override fired. That was visible
  as "no `p20` address on either side", which is the same shape as a green arm that asks nothing.
- **The first run of the measurement wrote no artifact.** `--out` only redirects a `--write`; the
  brief's command carries `--out` and not `--write`, so it was read as a no-write run. The exit code
  was 0 either way — the artifact's absence on disk is what said so, which is the "never read an exit
  code as evidence" rule paying for itself.
- **M7's fix moves dice beyond its own four games**, and that was called before the run rather than
  explained after. It did not produce a regression: every changed board family fell.

---

## 4. THE CENSUS, CHECKED AFTER EACH FIX AND NOT ONLY AT THE END

| after | rows | live | stopped being live | newly live |
|---|---|---|---|---|
| M5 | 829 | 829 | [] | [] |
| M7 | 829 | 829 | [] | [] |
| M8 | 829 | 829 | [] | [] |

`node engine/status.js` reads `829/829 probed mechanics live, 0 missing`. **The one number ENGINE owns
never went down at any point in this batch.**

---

## 5. REGRESSION, AND WHAT MOVED ON DISK

- `tests/staged_board.js` — **25 of 25 clean and board-identical**, run after M5, after M7 and after M8.
- All six probes green together on the final engine: `probe_sucker_redirect_refusal`,
  `probe_encore_stall_address`, `probe_contact_ability_transfer`, and the first batch's
  `probe_multiaccuracy_address`, `probe_nonpermanent_forme_revert`, `probe_choicelock_cleared`.
- Also green: `probe_multihit_corners`, `probe_multihit_update`, `probe_kingsrock_volley`,
  `probe_party_key_collision`, `probe_mega_trace_entry`.
- Files this session wrote: `engine/medicham2-browser.js` (the only engine change),
  `data/mechanics-census.json` (regenerated three times, final 829/829, same membership),
  `data/verification/fix-batch-M5M7M8.json` (new), `data/releases/9b449a41c865/` (new snapshot),
  `data/engine-release.json`, and three new probes under `tests/`.
- **Nothing was committed.** `data/_diag77-sample.json` and `data/_diag77-cards.json` were read and
  left in place. Nothing under `docs/` was touched except this report — another agent owns that tree
  this session.

---

## OWED

1. **`docs/ENGINE.md` and `node engine/status.js --write` are NOT done.** A docs agent owns `docs/`
   this session and `--write` restamps its generated block. The hand-list entries these three probes
   retire, and the restamp, are owed to whoever holds it.
2. **The gate is still RED at board-material 50 of 961.** The bar is ZERO.
   `data/game-differential.json` still holds the 77 and is the file `engine/quarantine.js` reads — it
   was deliberately not overwritten, so the published gate figure is unchanged until MEASURE
   republishes it from a run on `9b449a41c865`.
3. **Cutting `9b449a41c865` stranded four frozen artifacts against the tree**, and `status.js` now
   reports each as `MEASURED AGAINST A DIFFERENT ENGINE`: `data/engine-diff.json`,
   `data/roster.items.json`, `data/roster.abilities.json`, `data/roster.moves.json` and
   `data/all-mechanics-fire.json`. They were already stale against `f3504e5f88d6` before this batch
   (they name `8ad06030e129`), so this did not create the condition — but the re-runs are owed, and
   the roster is one of the quarantine gate's own clauses.
4. **Register rows.** M5 is ROADMAP **#403**, closed 2026-08-23 over the queue clause and **re-opened
   tonight on the redirect road** — the queue-clause fix is intact and arm 4 of the probe is the
   control that proves it. M7 is **#543** and this closes the address half of it; **#463 was not
   reopened and nothing in `_stallExpire` or the four eager clears was touched.** M8 is **#541**, and
   two of its three declared clauses are now closed (the announcement body and shape, and the missing
   `Start`). ROADMAP is owned by another agent this session, so none of these were filed.
5. **#541's third clause is NOT closed and must not be read as closed.** The board leaf
   `p1.party.metagross.ability` runs opposite to the derivation, #541 deliberately left it
   unattributed, and this pass did not attribute it either. The three live candidates it names — the
   `POST_FAINT` hold in `board_state.js`, the missing `source.fainted || target.fainted` refusal
   (reachable because `Pokemon#faint()` sets `faintQueued` and not `fainted`), and the absent `Start`
   events — only the last is now closed. `active[].ability` is 3, not 0.
6. **The MOVE Skill Swap raises no acquired `Start` either.** It is a second caller of the same
   authority line (`skillSwap`'s two `singleEvent('Start', ...)`), it carries ZERO of this
   population's cards, and it was reported rather than changed in the same pass. It has no probe.
7. **`failskillswap` and the `cantsuppress` guard are still unmodelled**, as WIRE 80's header already
   declared. No legal carrier of `cantsuppress` exists in this format.
8. **VOID is unmoved at 7 and its head is still `acc partingshot [sd only]` 8**, unexamined. Two new
   shapes appeared in that table (`sec populationbomb [sd only]` 7, `crit`/`dmg populationbomb [me
   only]` 3 each) where `acc encore`, `gigadrain` and `sludgebomb` left it; the VOID GAME count did not
   move, so this is a reshuffle inside the same seven games and is not a regression — but it is not
   examined either. Section 0 of the diagnosis also stands: `quarantine.js:2298`'s board denominator
   does not exclude void games while the protocol clause does — a MEASURE decision, still untaken.
9. **M2 was not touched**, per the brief (fenced by #542 until it is split). Its Moonblast /
   Gigaton Hammer multiplier games and its roll-index games are all still in the 50.
10. **The remaining 50 are the untouched groups**, and the board-material worklist names them: M6
    confusion index, M11 Dire Claw sleep (5 games, the largest single cause left), M12 Infestation
    duration, M13 Soundproof vs Perish Song, M14 the berries, M15 Poison Touch, plus the singletons.
