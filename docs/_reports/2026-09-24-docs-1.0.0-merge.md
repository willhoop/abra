# 2026-09-24 — docs 1.0.0 merge (fold-ins A + B) on draft/regmc-1.0.0

Historical record, not current state.

## Merges
- `origin/docs-1.0.0-a` merged clean (white paper, deck, technical docs).
- `origin/docs-1.0.0-b` conflicted in two files:
  - `docs/RUNNING-NOTES.md` — both 1.0.0 rows kept (A, then B).
  - `data/docs-currency-baseline.json` — taken from B (SEARCH.md -> SOLVER.md, GLOSSARY and MODELS
    retirements), then regenerated with `node tests/test-docs-current.js --update` (monotone:
    removals only). That also retired A's dropped white paper / technical docs grandfather lists.
    No entry was added by hand.

## A vs B agreement
- Model roles, statuses and cited figures (MAG/DODUO 25,477 / 2.730 / 3,826; XATU 10,942 / 1.348 /
  1.792; SLOWKING 1,559; MILTANK 3,414; arena 15; playout 1,184; lean 22,283) match between the white
  paper and MODELS/SUMMARY. Figures only in A (34.8%, 2,518, 1.25x-1.33x) are in CHANGELOG-REGMC.
- SEARCH -> SOLVER: A already says "SEARCH becomes the SOLVER division"; no stale `docs/SEARCH.md` refs.
- **Fixed:** A described MILTANK as asking XATU for worlds and spending playouts by successive halving,
  as if built. B's correction holds: v1 draws worlds uniformly and has no halving. Marked as planned in
  white paper §2.2, §2.4 (intro + bullet), §3.1 registry, §3.5 (explicit sentence); technical docs
  model table; deck slide 7 (halving) and slide 9 (XATU not yet used by the player).

## Tests
- `tests/test-docs-current.js` all pass; `tests/test-docs-quarantine.js` pass;
  `tests/test-roadmap-register.js` 3/3.

## PDFs
`node build/build_pdfs.js` (the repo's build calls `build/md_to_pdf.js`; pandoc is not installed on
this machine) rebuilt 13: ABRA-WhitePaper (from ABRA-whitepaper.md), ABRA-deck-plain-english,
ABRA-technical-docs, ABRA-Defense, ABRA-Methodology, ABRA-Orientation, ORIENTATION, DIVISIONS,
GLOSSARY, MODELS, SUMMARY, and two previously missing: REGMC, REGULATION-ROTATION. Ledgers, ROADMAP
and RUNNING-NOTES excluded by the script. `pdf-inspector --pages 1` returns real text for the white
paper and the deck.

## Not done
- Not merged to main, not tagged, 1.0.0 not published — held for Will.
