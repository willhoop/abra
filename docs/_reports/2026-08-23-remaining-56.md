# The remaining divergences, grouped by mechanism — read-only triage

**Read from `git show HEAD:data/game-differential.json`** (commit `192d917`), never from disk. Release
`c66976713feb`, showdown commit `20ad99ffc9a5`, census pin `80e648f34d56`, team store pinned to
`data\team-pool-frozen` (8,778 teams, 1,968 picked), **961 games / 95 diverged / 0 threw**, generated
2026-08-23T02:40:37Z. Nothing was run. No game was played, no release was cut, `game_differential.js`
was not invoked, nothing under `engine/` or `tests/` was edited.

---

## 0. THE BRIEF'S CLASS COUNTS ARE FROM A DIFFERENT ARTIFACT. THE SCOPE IS 47, NOT 56.

The brief quotes `22 unrelated / 18 ordering / 9 -boost field 3 / 7 extra` and `33 event missing`.
Those numbers are real and they are **commit `22e3578`** — an earlier run of **777 games** on release
`59bb68aa89a9`, generated 2026-08-22T22:50Z. That is also the artifact ROADMAP #363 was filed against.
The settled copy at HEAD (`192d917`) that the brief's headline correctly describes — 961 games,
95 divergences, release `c66976713feb` — has a different composition:

| class | HEAD (`192d917`, 961 games) | brief / `22e3578` (777 games) |
|---|---|---|
| event missing from medicham2 | 42 | 33 |
| ordering | **23** | 18 |
| unrelated event mismatch | **13** | 22 |
| -boost field 3 | **6** | 9 |
| extra event emitted by medicham2 | **5** | 7 |
| -damage field 3 | 3 | 2 |
| -damage: a different body | 1 | 2 |
| -status: a different body | 1 | — |
| -unboost field 3 | 1 | 1 |
| showdown stopped emitting / -start field 4 / drag | — | 2 |

**So my four classes are 47 divergences at HEAD, not 56.** Every occurrence count in this artifact
equals its game count (`n` sums to the class `games` in all nine classes), so there is no double
counting either way.

**None of the 47 is declared.** `DECLARED_DIVERGENCE` in `engine/quarantine.js` has exactly one live
member (`fallenundefined`), and all **5** of its hits are inside `event missing from medicham2` —
the other agent's class. The "90 after 5 declared" subtraction lands entirely outside my scope.

**Ranking caveat that applies to every count below.** `steering.policy` is
`census-coverage-seeking/v1`: *"at every decision the driver prefers the legal action reaching the
least-exercised row of `data/mechanics-census.json`"*. The TEAMS are the real pinned pool; the CLICKS
are not the ladder's distribution. A count of 2 on a 5-use move in this sample is not the same
evidence as a count of 2 on a 134,710-use move. Both numbers are given below for that reason.

---

## 1. THE 47 REDUCE TO 16 MECHANISMS (14 if the refusal family is taken as one)

Every row of the four classes is assigned. Counts sum to 47.

| # | mechanism | n | verdict axis | register row |
|---|---|---|---|---|
| A | **Showdown REFUSES a move and medicham2 executes it** (3 sub-causes) | **8** | board-material | **NO ROW** |
| B | **the faint/death announcement is not batched** | **6** | announcement-position, masks everything after it | #331 (open, unprobed) |
| E | **a uniform random STAT choice (Moody)** | **6** | INSTRUMENT-SUSPECT | #351, #363 (open, no verdict) |
| C | **`\|switch\|` order is not keyed on the departing body's speed** | **5** | board-material (which body is where) | #353 (open, no verdict) |
| D | **an exact speed tie between two +4 shields** | **3** | INSTRUMENT-SUSPECT | #290 (measured), not filed as a defect |
| J | **spread-move per-target step / announce order** | **3** | mixed | #339 covers the drain HP half only |
| K | **a redundant or wrong-shaped `-activate` announcement** | **3** | announcement-only | #360 covers Telepathy; the other 2 have NO ROW |
| F | **switch-in effect order (`onSwitchInPriority`)** | **2** | board-material | #330 (open, unprobed) |
| G | **the target's berry vs the attacker's own self-damage** | **2** | announcement-position | **NO ROW** |
| H | **mega-evolution resolution order** | **2** | announcement-position | **NO ROW** |
| I | **two side conditions expiring on the same turn (Tailwind)** | **2** | announcement-position | #355 (open, no verdict) |
| L | **flinch fires here and not there** | **1** | board-material | #354 (open, no verdict) |
| M | **a Substitute breaks there and survives here** | **1** | board-material | #358 (open) |
| N | **the `\|-weather\|W\|[upkeep]` line / an extra `-fail` on Copycat** | **1** | announcement | #352 (open) |
| O | **`??:` off-field body reaches the protocol stream** | **1** | INSTRUMENT | #224 (reopened 2026-08-22) |
| P | **a move that produced no effect lines at all** | **1** | unattributed | none |

**11 of the 16 already have a register row. 5 do not: A, G, H, and the two non-Telepathy members of
K.** J's announce-order half and D's residual are arguably new against #339 and #290 respectively.

---

## 2. THE GROUPS, WITH EVIDENCE

Confidence is stated per group: **CONFIRMED** = the rule is cited from the format or the engine source;
**DERIVED** = read out of the artifact; **HYPOTHESIS** = the shape fits and the artifact cannot settle it.

### A — Showdown refuses the move, medicham2 executes it. 8 of 47. NO REGISTER ROW.

Eight causes share one signature: Showdown's line is `|-fail|SLOT` and ours is the move's effect. In
the two sampled cases the authority's `|move|` line carries `||[still]`. That pair —
`this.battle.add('-fail', pokemon); this.battle.attrLastMove('[still]');` — is written at
`sim/battle-actions.ts:594-596`, the branch where `singleEvent('Try') && singleEvent('PrepareHit') &&
runEvent('PrepareHit')` returned an explicit `false`, and again at `:645-647` for a `TryHit` that came
back all-false. So it narrows the failure to those two gates without naming one of them.
This is the largest mechanism-shaped family in scope and every member is board-material: a move that
should not have resolved did.

**A1 — a stalling move that is not a shield never faces the gate. 2 of 8. CONFIRMED, source-cited.**

```
unrelated event mismatch :: |-fail|p1b <> |-singleturn|p1b|endure
unrelated event mismatch :: |-fail|p2a <> |-singleturn|p2a|endure
```

`data/moves.ts:4816-4818` — `endure.onPrepareHit(pokemon) { return !!this.queue.willAct() && this.runEvent('StallMove', pokemon); }`,
byte-identical to `protect`'s at `:13974-13976`. Champions overrides `protect` (pp 5 only,
`data/mods/champions/moves.ts:755`) and does **not** override `endure` at all.

Ours gates on the CHOOSER'S ACTION KIND, not on the move being a stalling move —
`engine/medicham2-browser.js:14901`:

```js
if(it.a.kind==='protect'&&!volatileForbidsMove(it.mon,actionMoveId(it.a))){
  it._shieldPending=true; it._preWillAct=_anyActionAfter(i);
}
```

and `PROTECTMOVES` (`:3891`) is `protect, detect, spikyshield, kingsshield, banefulbunker,
burningbulwark, silktrap, maxguard`. **Endure is not in it.** ROADMAP #232 closed on a family of six
sharing one counter — Protect, Detect, Spiky Shield, Baneful Bunker, King's Shield and Endure — and
five of the six reach the gate. #178 removed Endure from `shieldsUser` for the right reason (it blocks
nothing) and the gate is keyed on the same road. Both the `willAct()` scan and the `stall` counter are
missed; the sampled case at index 26 reconstructs as willAct (every other body had already switched,
so the Endure user held the last action).

*Probe*: two bodies, one switches and the other clicks Endure with nothing move-like left in the queue.
Assert `-fail` and no `-singleturn`. Second probe: Endure on consecutive turns, assert the stall roll
is consumed. Endure is 5 corpus clicks; the fix is the keying, not the move.

**A2 — a trapping move re-applied to an already-trapped body. 2 of 8. CONFIRMED mechanism, HYPOTHESIS on the instance.**

```
unrelated event mismatch :: |-fail|p2a <> |-activate|p1a|trapped
unrelated event mismatch :: |-fail|p1b <> |-activate|p2a|trapped
```

Derived from the format: `meanlook` and `block` are both
`onHit(target, source, move) { return target.addVolatile("trapped", source, move, "trapper"); }` and
`addVolatile` returns false on a repeat, so the second use fails. The `trapped` condition's
`onStart(target) { this.add("-activate", target, "trapped"); }` is exactly the line we emit — our
announcement is right and the refusal is missing. Our engine implements the repeat refusal
**per volatile** (`applyAttract`: *"addVolatile refuses a repeat: no onRestart"*, `:11366`), so a
global refusal is not in place. A coverage-seeking driver re-clicks, which is why this appears.

*Probe*: Mean Look twice into the same body; assert the second is `-fail` with no second `-activate`.
*Control*: the same two clicks separated by a switch-out, which clears `trapped` and must succeed.

**A3 — four refusals with no attribution anywhere in the artifact. 4 of 8. UNATTRIBUTED.**

```
|-fail|p2b <> |-supereffective|p1b|1
|-fail|p2a <> |-resisted|p1a|1
|-fail|p1a <> |-damage|p2a|0 fnt
|-fail|p2b <> |-damage|p1a|H/H
```

All four carry `mentions: []` and `max_uses: undefined`, and none is among the 60 sampled
`first_divergences`, so there is no species, no move and no preceding context in the file. Two of them
damage a body Showdown never damaged, one of them to a faint. These cannot be worked until the
instrument records context for every divergence (see §4).

### B — the faint/death announcement is not batched. 6 of 47. ROW #331, open and unprobed.

```
ordering :: |-sidestart|p2:|stealthrock       <> |faint|p2a      (Stone Axe; SD writes the hazard first)
ordering :: |-enditem|p2b|ironball|[from]fling <> |faint|p1a
ordering :: |-damage|p2b|H/H|[from]sandstorm  <> |faint|p1b      (SD chips every body, then faints)
ordering :: |-damage|p2a|H/H|[from]sandstorm  <> |faint|p1a
ordering :: |-enditem|p2b|sitrusberry|[eat]   <> |faint|p2a      (spread EQ: SD lets the survivor eat first)
extra    :: |upkeep                            <> |-damage|p1b|0 fnt   (perish0: we announce a DAMAGE line)
```

`this.add('faint', pokemon)` lives inside `Battle#faintMessages` (`sim/battle.ts:2532`), which drains a
`faintQueue` at the engine's checkpoints — after the action and after the residual block — not at the
instant HP reaches zero. Five rows are ours announcing at the instant of lethal damage. The sixth is
the same fact wearing a different line type: a Perish Song KO takes `pokemon.faint()`, which is not
damage, and we emit `|-damage|p1b|0 fnt` where Showdown emits nothing until the faint drains. ROADMAP
#90 closed the perish KO itself; the announcement shape is not that row.

**A usage ranking cannot see this group at all**, because a faint has no move id and no item id — the
annotation can only name the OTHER line of the pair, so these six rank as Stealth Rock (198), Iron
Ball (249), Sandstorm (36, twice), Sitrus Berry (21,927) and `undefined`, and not one of those five
entities is the mechanism. That is `max_uses` naming the line we did not diverge on, exactly as the
brief warns. The mechanism fires in every game that has a KO, and because a first divergence
**terminates the comparison**, every one of these six hides an unknown number of later lines.

*Probe*: a spread move that KOs one target and leaves the other on a Sitrus threshold; assert the
survivor's `-enditem` precedes `|faint|`. Second: sandstorm residual killing one of three bodies;
assert all three `-damage` lines precede the faint.

### C — `|switch|` order is not keyed on the departing body's speed. 5 of 47. ROW #353 (filed at 3).

```
ordering :: |switch|p1a|garchomp   <> |switch|p2b|blastoise
ordering :: |switch|p1a|swampert   <> |switch|p2a|delphox
ordering :: |switch|p1b|whimsicott <> |switch|p1a|alakazam      (same side, start of turn 2)
ordering :: |switch|p1a|staraptor  <> |switch|p2a|incineroar
ordering :: |switch|p1a|palafin    <> |move|p2b|helpinghand
```

#353 filed three of these on release `13ba05093aa3`; on `c66976713feb` there are **five**, and the two
new ones (garchomp/blastoise, swampert/delphox) are both post-faint replacements. #353's derivation of
the authority's key holds (`sim/battle-queue.ts:270` → `sim/battle.ts:2657`
`action.speed = action.pokemon.getActionSpeed()`, `pokemon` being the OUTGOING body). One thing the
artifact adds: in the swampert/delphox case Showdown moved the SLOWER side first (Swampert base 60,
Delphox base 104), so a naive "replacements go in speed order of the incoming body" is refuted from
inside the file.

**Four of the five carry `max_uses: null`** — the mentions are species and species have no `uses` — so
a usage-ordered head buries this group at the bottom while it fires on every double switch and every
post-faint replacement.

#353's instrument ask still stands and is still unmet: `MEDFAILS.entryOrderTie` and
`MEDFAILS.replaceOrderTie` (`medicham2-browser.js:1866-1874`) are not published in the artifact, so a
declared tie cannot be told from an ordering defect.

### D — three exact speed ties between two +4 shields. 3 of 47. PROBABLY THE INSTRUMENT.

```
ordering :: |move|p1b|protect <> |move|p2a|protect
ordering :: |move|p1b|protect <> |move|p1a|protect
ordering :: |move|p1a|protect <> |move|p2a|detect
```

**The artifact already carries the control and it is decisive.** `order_probe` holds all three pairs
with both bodies' `getActionSpeed()` read off the AUTHORITY at the turn boundary:

| pair | showdown first | medicham first | speed | tied |
|---|---|---|---|---|
| Venusaur / Politoed | Venusaur 122 | Politoed 122 | gap **0** | yes |
| Garchomp-Mega / Metagross | Garchomp 134 | Metagross 134 | gap **0** | yes |
| Blaziken-Mega / Blaziken-Mega | 228 | 228 | gap **0** | yes |

`same_priority: true` on all three, priority 4 on both sides of all three. A speed tie has **no correct
answer** — in a real battle `speedSort` ends in `prng.shuffle` over the tied group — so these can only
be scored by aligning two tie devices (Showdown's shuffle pinned to identity against medicham2's `tie`
stream, ROADMAP #290/#311). The alignment is working at 107,878 of 107,878 tied groups resolved with
`medicham_tie_sequence_saturated: 0`; three residual disagreements out of 107,878 is an alignment edge,
not a turn-order defect. ROADMAP #290 is what proved `ordering` is not merely the tie artefact — it
found 9 of 25 pairs NOT tied. **On this release all three shield pairs are tied**, which is the
opposite reading and should be recorded against that row.

*Control that settles it*: stage two bodies at identical `getActionSpeed()` both clicking Protect,
across N seeds, and count how often the two engines agree. If agreement is ~50% it is the tie device;
if it is 0% the alignment is inverted; if it is 100% the three are something else. Note the second row's
authority line is `|move|p1b: Garchomp|Protect||[still]` — that Protect FAILED on the authority, so
that row may be A1's mechanism wearing an ordering class.

### E — a uniform random stat choice. 6 of 47 (+1 `-unboost`, out of scope). PROBABLY THE INSTRUMENT.

```
-boost field 3 :: |-boost|p2a|spa|2 <> |-boost|p2a|def|2      (n=2)
-boost field 3 :: |-boost|p2a|spd|2 <> |-boost|p2a|spa|2
-boost field 3 :: |-boost|p1a|def|2 <> |-boost|p1a|spe|2
-boost field 3 :: |-boost|p2a|atk|2 <> |-boost|p2a|spd|2
-boost field 3 :: |-boost|p1a|spa|2 <> |-boost|p1a|spd|2
```

**This section EXTENDS ROADMAP #363 and does not re-derive it.** #363's structural finding stands: the
`stat-attribution` normaliser strips `[from]` from every `-boost` and `-unboost` (9,631 lines collapsed
on this run), so the cause key cannot name a mechanism. Four things are new on this artifact.

1. **The class is 6 here, not 9.** #363 was filed against `22e3578` / `59bb68aa89a9` / 777 games. On
   `c66976713feb` / 961 games the class is 6 in 5 causes (one with `n=2`).
2. **The `-unboost` half is BACK.** #363 recorded *"THERE IS NO `-unboost` CLASS IN THIS ARTIFACT AT
   ALL"*; on HEAD there is exactly one, `|-unboost|p1a|spa|1 <> |-unboost|p1a|spe|1`, which is Moody's
   drop shape and matches #351's original observation. It sits in its own class, outside my four.
3. **Attribution recoverable from the artifact: 1 of 6, and no more.** A whole-file scan of the HEAD
   artifact finds the string `moody` **once** and `acupressure` **zero** times. The single occurrence is
   in the one sampled `first_divergences` entry for this class (line index 27), where our raw line is
   `|-boost|p2a: Scovillain|def|2|[from] ability: moody` and the authority's is
   `|-boost|p2a: Scovillain|spa|2` with **no `[from]` at all** — so even the raw authority line could
   not have attributed it. The other five causes carry `mentions: []`, `max_uses: undefined`, no
   species and no context. **Recovery requires the instrument change #363 already names**; nothing in
   the file can be squeezed further.
4. **The candidate split can be bounded from the PINNED POOL, which #363 did not do.** Derived from
   `Dex.forFormat('gen9championsvgc2026regmb')`: Moody's legal carriers are exactly **Glalie and
   Scovillain**; Acupressure is `isNonstandard: null`, `target: adjacentAllyOrSelf`, `this.sample(stats)`
   for +2. Counted in `data/team-pool-frozen` — the store this run was pinned to —
   `games.bo3.jsonl` + `games.ots.jsonl` hold **`acupressure` in 1 line each (the same Tsareena set),
   `Scovillain` in 957 + 236, `Glalie` in 11 + 1, `moody` in 813 + 204.** With 1,968 of 8,778 teams
   picked, Acupressure can explain **at most 1** of the 6 and only if that one team was drawn AND the
   coverage driver clicked it; Moody is the overwhelming prior for all 6. A third candidate — a
   deterministic +2 move whose stat we map wrongly — is refuted for these rows only insofar as every
   one is a same-slot, same-magnitude, different-stat pair, which is the `sample` shape.

**Why this is the instrument, restated with in-artifact evidence.** Both `moody.onResidual` and
`acupressure.onHit` choose via `this.sample`, which executes in none of the three authority methods
`game_differential.js` wraps for its die address (`hitStepAccuracy`→acc, `secondaries`→sec,
`getDamage`→dmg). Our own engine says the same from its side:
`RNG_STREAMS = ['acc','crit','sec','dmg','stall','tie']` and *"target selection, the chooser's own
coins, sleep/freeze/paralysis timers, Ally Switch's stall and the forme cycles all keep reading the
generic stream"* (`medicham2-browser.js:14278-14289`). And the artifact shows what unaddressed draws
do in practice — `mid_void.one_sided_game_shapes` records **117 `any -` draws the authority made that
we did not, and 82 we made that it did not.** A uniform choice with no shared address agrees about 1
time in 5 by luck; five different stats in five causes is what that looks like.

*Control*: stage one Moody residual and print the address each side uses for the draw. Either extend
the category wrapper past the three `BattleActions` methods, or publish that a residual draw is
unshared — after which these 6 are removed from the divergence count rather than fixed.

### F — switch-in effect order. 2 of 47. ROW #330, open and unprobed.

```
ordering :: |-unboost|p1a|atk|1 <> |-weather|raindance|[from]drizzle    (Intimidate vs Drizzle)
ordering :: |-unboost|p1a|atk|1 <> |-unboost|p2a|atk|1                  (two Intimidates)
```

#330 is the row and its Hospitality/Drought card is the proof. Two additions from the source, both
worth carrying into the probe: `fieldEvent('SwitchIn')` speed-sorts a handler list in which
`comparePriority` reads **order, then priority, then speed, then subOrder, then effectOrder**
(`sim/battle.ts:404-411`) — and for SwitchIn specifically Showdown subtracts a fractional term,
`handler.speed -= this.speedOrder.indexOf(pokemon.getFieldPositionValue()) / (this.activePerHalf * 2)`
(`:1008-1011`), *"Pokemon speeds including ties are resolved before all onSwitchIn handlers and aren't
re-sorted in-between"*. So the two-Intimidate row is decided by field position after the speed key,
not by slot order and not by a coin.

### G — the target's berry against the attacker's own self-damage. 2 of 47. NO REGISTER ROW.

```
ordering :: |-enditem|p2a|sitrusberry|[eat] <> |-damage|p1b|H/H|[from]recoil
ordering :: |-enditem|p2a|sitrusberry|[eat] <> |-damage|p1a|H/H|[from]lifeorb
```

Showdown consumes the target's Sitrus before the attacker's own recoil / Life Orb chip; we do the
reverse. **This is the highest real corpus usage of any group in scope** — `sitrusberry` 21,927,
`lifeorb` 21,257, both carried in the artifact's own mentions. It is not #338 (Life Orb not firing at
all for a turn) and not #167 (the Sitrus threshold); it is where the two sit relative to each other.

*Probe*: one attacker with Life Orb into one target that lands exactly on the Sitrus threshold; assert
`-enditem|[eat]`, `-heal`, then `-damage|[from] item: Life Orb`. Repeat with a recoil move.

### H — mega-evolution resolution order. 2 of 47. NO REGISTER ROW.

```
ordering :: |detailschange|p1a|starmiemega,l50 <> |detailschange|p2a|charizardmegay,l50
ordering :: |detailschange|p1a|mawilemega,l50  <> |detailschange|p2a|mawilemega,l50
```

Corroborated inside the artifact: `mega.cost_of_the_megas.first_divergence_is_a_detailschange_line: 2`
and `first_divergence_is_a_mega_line: 0`, with both raw pairs recorded under
`on_a_detailschange_line`. The mega counts themselves AGREE (521 = 521,
`engines_agree_on_the_count: true`, `every_issued_choice_evolved_in_both_engines: true`), so this is
purely which side's `detailschange` is written first. `megaEvo` is order **104** in
`sim/battle-queue.ts:177-186`, between `runSwitch` (101) and `priorityChargeMove` (107), and inside the
bracket the action's speed is `action.pokemon.getActionSpeed()` — i.e. mega evolutions resolve in speed
order of the evolving bodies. ROADMAP #322 moved the pre-turn CHARGE announcement to order 107 for the
same reason; the mega bracket has no row.

*Probe*: both sides mega on turn 1 with a known speed gap; assert the faster body's `detailschange`
first. Then invert with Trick Room up, which does not touch `order`.

### I — two side conditions expiring on the same turn. 2 of 47. ROW #355.

```
ordering :: |-sideend|p2:|tailwind <> |-sideend|p1:|tailwind    (n=2)
```

The authority's key, read for the probe: side-condition handlers are pushed with
`effectHolder: side` (`sim/battle.ts:1216-1231`), a Side has no `getStat`, so `handler.speed` is never
assigned and both sides' Tailwind handlers **tie perfectly** on order, priority, speed and subOrder
(subOrder 4 = side condition, `:1240-1250`). The tie therefore goes to `prng.shuffle`, which this arm
pins to identity, which leaves the list order — and `fieldEvent` walks `for (const side of this.sides)`,
i.e. p1 then p2. The authority emitted **p2 first**. So either the shuffle is not identity here, or the
two Tailwinds do not expire on the same turn and this is a DURATION divergence wearing an ordering
class. #355 says *"#242 settled where in the walk rather than which side"*; this narrows it further.

*Probe, two arms because they answer different questions*: (a) both sides set Tailwind on the same turn
and assert the emission order; (b) sides set Tailwind one turn apart and assert only one `-sideend`
fires on each of two consecutive turns. Arm (b) is the one that would expose a duration defect.

### J — spread-move per-target step and announce order. 3 of 47.

```
ordering  :: |-heal|p1b|H/H|[from]drain <> |-damage|p2b|H/H
ordering  :: |-immune|p2b                <> |-immune|p1a|[from]levitate
unrelated :: |-damage|p1b|H/H            <> |-supereffective|p1a|1
```

The drain row's sampled context (Matcha Gotcha into two foes) shows Showdown emitting the drain heal
`[of] p2a: Excadrill` immediately after the FIRST target's damage, with no damage line for the second
target at all, while we damage both. Either the authority's target set was one body or its per-target
interleave differs from ours; the six-line window cannot tell, and no `sdAfter`/`meAfter` is recorded
for a non-mega divergence. **#339 is about the drain HP arithmetic (per-target rounding), not the step
order**, so the order half is unfiled. The other two rows are the announcement walk over targets —
Showdown speed-sorts spread targets, and the levitate row has us announcing our own side's ally
immunity before a foe's type immunity.

*Probe*: an `allAdjacent` move (hits ally and both foes) where one body is immune by type and another
by ability; assert the announcement order and the target set on both engines.

### K — announcement shape. 3 of 47. Telepathy is #360; the other two are unfiled.

```
unrelated :: |-activate|p2a|telepathy <> |-immune|p2a|[from]telepathy                         (#360)
unrelated :: |-start|p2a|disable|phantomforce|[from]cursedbody <> |-activate|p1a|cursedbody    (NO ROW)
extra     :: |upkeep <> |-activate|p1b|sitrusberry                                            (NO ROW)
```

All three are the same fact: **Showdown attributes a source with `[from]` on the effect line, and we
emit a separate `-activate` announcement instead of, or in addition to, the effect.** For Cursed Body
the authority writes the Disable with `[from] ability: Cursed Body` on the ABILITY HOLDER's victim and
never announces the ability separately; ours announces the ability. For Sitrus the sampled context shows
both engines already agreeing on `-enditem|[eat]` and `-heal|[from] item: Sitrus Berry`, after which we
emit a third, redundant `|-activate|p1b|item: sitrusberry`. Zero board impact in all three.

`normalisation.rules` already collapses `|-ability|` (the `ability-announcement` rule, 265,226 lines).
A companion rule for a state-free `-activate` would close all three at the comparator — **so this group
is a choice between an engine fix and one normaliser rule, and somebody has to pick.** Corpus usage:
`sitrusberry` 21,927, `cursedbody` 2,903, `telepathy` 297.

### L, M, N, O, P — one each.

- **L** `|move|p2a|psychicfangs <> |cant|p2a|flinch` — ROW **#354**, and #354's own note is right that
  this one IS inside the addressed set (`secondaries` → `sec`), so it is testable. `psychicfangs` 2,663.
- **M** `|-end|p2a|substitute <> |-activate|p2a|substitute|[damage]` — ROW **#358**. Board-material: the
  sub broke there and survived here. `substitute` 1,222.
- **N** `|-weather|raindance|[upkeep] <> |-fail|p1a` — ROW **#352** (the `wSup` gate). Note the sampled
  context makes this a Copycat with no valid last move, where we emit a `-fail` the authority does not,
  so #352's suspicion that the gate *"probably does not explain its own five games"* extends here.
- **O** `|upkeep <> |move|??:farigiraf|roar` — ROW **#224**, reopened. **INSTRUMENT**, with a receipt in
  this same artifact: `declared_gaps.trace_body_off_field: 6`, `trace_body_off_field_first: "farigiraf"`.
  The `??:` is `ident()`'s fallback when the body is in neither active array at emission time.
- **P** `|-supereffective|p1a|1 <> |move|p1a|gravity` — after both engines agree on
  `|move|p2b: Dragapult|Phantom Force|p1a: Metagross|[from] lockedmove`, the authority resolves the hit
  and we move straight to the next body's action. Unattributed; possibly the locked-move charge turn.

---

## 3. THE RANKED QUEUE

Ranked on what it costs: divergences explained on the **pinned pool**, then corpus usage of the entity
where the mechanism has one, then whether it is board-material. Instrument-suspect groups are ranked
separately because fixing the engine cannot close them.

**ENGINE queue**

| rank | mechanism | n | corpus usage | why here |
|---|---|---|---|---|
| 1 | **A — the refusal family** (A1 stalling gate, A2 trapping repeat, A3 four unattributed) | 8 | A1 5 clicks, A2 unnamed, A3 unknown | biggest family in scope; every member board-material; A1 is a one-line keying fix with a source citation and a probe |
| 2 | **B — faint announcement batching** (#331) | 6 | **no entity — fires on every KO** | invisible to a usage ranking (`max_uses` undefined/misleading on all six); each instance truncates the comparison and hides the rest of the game |
| 3 | **C — `\|switch\|` order by departing speed** (#353) | 5 | **`uses: null` on 4 of 5** — every double switch and every replacement | board-material; #353's asked-for counters are still unpublished |
| 4 | **G — berry vs the attacker's self-damage** | 2 | **sitrus 21,927 + life orb 21,257** | highest real usage in scope; no row; a tight probe |
| 5 | **F — switch-in effect order** (#330) | 2 | drizzle 5,096, intimidate family | board-material; already filed with a derived membership of five carriers |

Then: H mega order (2, no row), J spread walk (3, half-filed), I tailwind (2, #355), K announcements
(3, one row, zero board impact), L flinch (1, #354), M substitute (1, #358), N weather/copycat (1, #352).

**INSTRUMENT queue — 10 of 47 (21%) will not move by editing the engine**

| mechanism | n | what settles it |
|---|---|---|
| E — Moody / any `this.sample` draw | 6 | print the address both sides use for one staged residual; extend the category wrapper past the three `BattleActions` methods, or declare the draw unshared and stop counting it |
| D — three exact +4 speed ties | 3 | the artifact's own `order_probe` already says `speed_gap: 0` on all three; N-seed staged tie to price the tie-device alignment |
| O — `??:` off-field body | 1 | #224's gate already measures it on two arms (`declared_gaps.trace_body_off_field: 6`) |

---

## 4. THE INSTRUMENT CHANGE WITH THE MOST TRIAGE LEVERAGE

**`first_divergences` samples 60 of 95 games and holds only 22 of my 47.** Twenty-five of the 47 carry
nothing but a normalised cause string: no species, no preceding lines, `mentions: []` and
`max_uses: undefined`. That is what makes A3 (4 refusals), 5 of the 6 `-boost` rows and several ordering
rows unattributable **by construction**, and it is the same complaint #363 filed about the
`stat-attribution` normaliser arriving through a second door. #349 already owns the honesty of the
rollup.

Two asks, both cheap and both already implied by open rows:

1. record `showdown_before`, `sdAfter` and `meAfter` for **every** divergence, not a 60-game sample —
   or at minimum one entry per distinct CAUSE;
2. publish the stripped `[from]`/`[of]` attribution and the body's SPECIES beside each cause, so the
   normaliser can stay lossy for the comparison and lossless for triage (#363's INSTRUMENT OWED).

And one already asked for by #353 and still unmet: publish `MEDFAILS.entryOrderTie` and
`MEDFAILS.replaceOrderTie` in the artifact, so a declared switch-order tie can be told from a defect.

---

## 5. WHAT I DID NOT DO

Nothing was executed against the engine or the tests. `data/game-differential.json` was read from
`git show HEAD:` throughout and never from disk (the live file was byte-identical in size and mtime at
23:10 EDT, but the rule stands). No roadmap row was filed. Three earlier committed copies of the
artifact (`22e3578`, `b26c2d4`, `239dd16`) were read to locate the brief's numbers; they were written
to the session scratchpad and nothing in the repo was touched.
