# Living docs brought to 5.252.0 — the release where the gate became honest

MEASURE, 2026-09-04. Documentation only. Nothing was run that plays a game, nothing under `engine/`
or `tests/` was touched, `docs/ROADMAP.md` was not touched, and nothing was committed.

## What was written

| file | version header | change |
|---|---|---|
| `docs/ABRA-whitepaper.md` | 5.251.0 → **5.252.0** | 7 new blocks above the 5.251.0 record |
| `docs/ABRA-deck-plain-english.md` | 5.251.0 → **5.252.0** | 6 new blocks |
| `docs/ABRA-technical-docs.md` | 5.251.0 → **5.252.0** | 5 new blocks, STE shape |
| `docs/DAMAGE-STAGES.md` | 5.251.0 → **5.252.0** | 3 new blocks |
| `docs/MODELS.md` | 5.251.0 → **5.252.0** | 3 new blocks |
| `docs/SUMMARY.md` | 5.251.0 → **5.252.0** | headline, artifact table, 3 blocks |
| `docs/ENGINE.md` | (no version header) | new dated section above the 5.251.0 one |
| `docs/MEASURE.md` | (no version header) | new dated section above the 5.251.0 one |

Every dated block below the insertion point was left exactly as authored. Nothing was rewritten in
place; the new blocks supersede from above and say what they supersede.

## The two headlines, as written into all eight

1. **The PUBLISHED gate figure moved 77 → 46 of 961 (4.8%).** `data/game-differential.json` was
   rewritten on release `0dec37ff5ad9` and now holds board-material 46 of 961 and protocol
   first-divergence 141 of 961 raw, 140 declared-adjusted, 961 games PLAYED of a 1200-PAIR budget,
   7 void and 1 thrown. **Every document that carried *the published clause still holds 77* now
   states that the caveat is DISCHARGED**, in those words, rather than dropping the sentence.
2. **The KIND of failure changed.** `CLOSED — 1 of 8 GATING clauses fail`, against 6 of 8, with five
   of those six in state (a) — nothing known — and all five stranded pins cleared. The one FAIL is
   state (b): a named instrument genuinely RED at 46 real games on the current engine. Written as
   *unmeasured → measured-and-red*, with state (c) (40 / 7 / 3 register rows) reported separately
   inside the clause that PASSES.

Also carried into every document that has a place for it: the 4-for-4 pre-run prediction; the
incomplete premise caught in advance (`data/smogon-priors.json`, month 2026-07 → 2026-08, 284 → 283
species, named as the falsifier and then measured inert with `classes` and `first_divergences`
byte-identical); the release cut from a working tree reachable from no commit; and the weights owing
a **REFIT, not a restamp**, because the damage table moved 318 → 322 species.

## A caveat discharged that the brief did not name

**`docs/DAMAGE-STAGES.md` has said "the currency of the stage-by-stage differential is still OWED"
for six consecutive versions (5.246.0 through 5.251.0). It is discharged too.** `tests/test-engine-diff.js`
ran in this release: `data/engine-diff.json` is stamped 2026-09-04T21:18:15Z on release
`0dec37ff5ad9` and reads 6,000 requested / 6,000 compared / 6,000 agreed / **0 disagreed**, at the
midpoint and all 16 corner arms — 17 damage indices, seed 20260804, with `skipped_multihit` 134 and
`skipped_ability_multihit` 17 unchanged. The stage table is a current measurement again, and the page
now says so. A third block was added there restating why a clean stage table still does not clear the
gate: a defect of ADDRESS or STREAM is invisible to a per-hit comparison by construction.

## Do the `<!-- GENERATED -->` blocks agree with the prose above them?

**Yes, and the one apparent disagreement is a counting convention, not a conflict.** The blocks are
stamped `2026-09-04 17:21` local, which is after `data/game-differential.json` (17:15:27) and after
`data/engine-diff.json` (17:18) — so `status.js --write` genuinely ran last and the blocks are
current. Checked value by value: census 829/829 with 0 missing matches `data/mechanics-census.json`;
`0/6000 ... (2026-09-04 17:18)` and the fifteen per-index rows match `data/engine-diff.json`; the
store block's `live.js 2026-09-04` is today.

The one thing a reader will trip over:

- the gate's own banner prints **`CLOSED — 1 of 8 GATING clauses fail`** (`engine/quarantine.js:5354`
  filters `c.gates !== false`);
- the ledgers' generated blocks print **`2 of 9 gate clauses fail`** and name BOARD-MATERIAL and
  NARRATION (`engine/quarantine.js:3566`, `gate.failing.length` of `gate.clauses.length`).

Same verdict counted two ways — 9 clauses of which 8 gate, 2 failing of which 1 gates; the non-gating
failure is NARRATION, which is its own gate by Will's 2026-08-22 call. **A paragraph explaining this
was added to `docs/MEASURE.md`**, because two live renderings of one count that look like a
contradiction is exactly the shape that gets a status tool disbelieved.

## Gate

`node tests/test-docs-current.js` → **23 passed, 0 failed.** It was 22 passed / 1 failed before this
pass, on the six stale version headers. Neither ratchet moved: the citation baseline holds at 66 and
the untraceable-figure census at 35 across 6 documents. `data/docs-currency-baseline.json` was not
edited and the test reports no ratchet movement.

Method note, because the citation rule is easy to satisfy dishonestly: every block was scored against
`engine/docs_scan.js`'s own `artifactNumbers` / `artifactHas` **before** it was written to a
document, and one draft was reworded rather than re-cited. The MEASURE OWED bullet on the refit
(318 → 322) was split into its own paragraph so it does not borrow the citation of the leaf-calibration
bullet above it — `data/winrate-backtest.json` does not hold the damage-table counts, and a paragraph
that names an artifact makes a promise about every figure in it.

## Line endings

All eight files are CRLF on disk before and after (`core.autocrlf=true`, so the repository stores
LF). **The brief's note that `docs/ENGINE.md` is LF-only does not describe the current working tree:**
all 39,443 lines carried a CR before this pass and all 39,460 carry one after. The writer asserted
zero lone-LF endings on every file before writing, and no file was converted.

## What was NOT touched, and why

- `CHANGELOG.md` — it already tops at 5.252.0 and this pass adds no new claim to log.
- `data/docs-currency-baseline.json` — forbidden by the brief and unnecessary; no ratchet moved.
- Anything under `engine/`, `tests/`, `docs/ROADMAP.md` — an ENGINE agent owns those and is playing
  games.
- `data/policy-weights.json` — the refit is OWED and is the owner's call.

## OWED

- **The gate is still CLOSED on one clause: board-material 46 of 961 on release `0dec37ff5ad9`.**
  That is ENGINE's number and the only thing between here and a lifted quarantine.
- **Leaf calibration — this division's one number — stays WITHHELD.** `data/winrate-backtest.json` is
  downstream of MEDICHAM and was not run. The standing brief to point `backtest_winrate.js` at the
  current leaf and publish a reliability curve cannot be discharged while a board parts in 46 of 961
  games; a calibration curve measured through that engine would be a number wearing a receipt.
- **The MAG refit stays OWED and is a REFIT, not a restamp** — damage table 318 → 322 species. No fit
  was started and no vector was written.
- **`docs/OPS.md:41` is now false and was deliberately left alone.** It reads *"`node engine/status.js
  --write` is still OWED, so the `<!-- GENERATED -->` block above still carries its 2026-09-01 store
  figures"* — an undated standing claim, not part of a superseded dated block, sitting directly under
  a block that was in fact stamped today at 17:21. It is OPS's ledger and outside this brief. Reported,
  not edited. `docs/SEARCH.md` carries no equivalent stale claim; `docs/WEB.md` has no generated block
  at all, so `status.js --write` stamps four of the five ledgers.
- **The narration baseline restamp is available and unrun**, so direction of travel on protocol
  first-divergence stays withheld — the stamped baseline sits on a different pin from this run.
- **PDFs were not rebuilt.** The living-docs rule names a `.pdf` beside the white paper, the deck, the
  technical documentation and the summary; this pass wrote only the `.md` files.
- **Nothing was committed.** Eight documents are modified in the working tree alongside the thirteen
  data files and four stamped ledgers the settled-republish pass left there.
