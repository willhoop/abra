# Reg M-C narration: Lightning Rod and Magic Bounce closed; the faint-line row was the engine

**Date.** 2026-09-24. **Division.** ENGINE (agent, isolated worktree). **Line.** abra/regmc 0.87.0, 0.88.0, 0.88.1
(the coordinator renumbers at merge). **Status.** A findings record. It is historical and never cited as current state.

## Verdict

- **Lightning Rod (group K): closed, 0.87.0.** The two checkouts disagree, not the engine and one checkout. The Reg M-C
  checkout (`pokemon-showdown-mc` `f10d679`) carries upstream `efe4948` "Remove unnecessary redirection code", which
  deleted the `isCharging` guard from `Pokemon#getMoveTargets` (`sim/pokemon.ts:829-831`). The Reg M-B checkout
  (`20ad99f`) keeps it (`:829-836`). Neither Champions mod overrides the method. So under Reg M-C the redirect runs on
  a real charge turn: a Lightning Rod writes `-activate` above `-prepare`, and **Pressure is priced off the drawn body
  (board-material, the PP leaf)**. MEDICHAM had copied the Reg M-B guard into both regulations.
- **Magic Bounce (group M): closed, 0.88.0, both regulations.** A bounced status move is the bouncer's move
  (`magicbounce.onTryHit` -> `useMove(newMove, target, {target: source})`), so its `-fail` names the bouncer
  (`runMoveEffects`, `sim/battle-actions.ts:1306`, `add('-fail', source)`). MEDICHAM called `mvFail(clicker)`.
- **The faint-line instrument question: it is the GAME (the engine), not the instrument**, and it is already fixed.
  The pass-9 row `omit-protect …bo3-2678516446` t8 (Farigiraf, `|faint|` never emitted) is a body revived by Revival
  Blessing on turn 7 and knocked out again on turn 8: the once-per-body faint latch that 0.85.0 reset. Control on one
  release, one game, one knob (below).
- **No board-material change.** Reg M-C `--games 1200` lattice, same pins, base against final: board-material 0/954 and
  0/954; narration 6 -> 4, the two removed games are exactly K and M, and no new cause appeared. Reg M-B: §4.

## 1. Lightning Rod

**Confirmed first.** `tests/probe_redirect_above_prepare.js`, run under Reg M-C on the base release `ec377f6f8159`
(0.86.1 bytes, identical tree to main's pointer): ROD-NORAIN red,

```
showdown : |-activate|p1araichu|abilitylightningrod   |-prepare|p1barchaludon|electroshot   |-activate|p1araichu|abilitylightningrod
medicham : |-prepare|p1barchaludon|electroshot   |-activate|p1araichu|abilitylightningrod
```

The authority draws on the charge turn AND the release turn (the release strikes `lastMoveTargetLoc`, the original
aim, via `twoturnmove.onStart`, `data/conditions.ts:287-313`, byte-identical in both checkouts, so it is drawn again).
The probe itself hardcoded the Reg M-B checkout and asserted the guard in source: it could not have passed under M-C.

**Fix.** `engine/tag_dex.js` reads `Pokemon.prototype.getMoveTargets` from the compiled checkout and sets
`DRAWN_WHILE_CHARGING` when no `flags["charge"]` test stands between `tracksTarget` and `RedirectTarget`; it throws if
the method names no `RedirectTarget`. It writes `chargeTurn.drawnWhileCharging: true` only when so. Printed before
wiring (LESSONS §4): the param landed on exactly the ten `chargeTurn` rows of `data/tags-regmc.json` (bounce, dig, dive,
electroshot, fly, meteorbeam, phantomforce, skyattack, solarbeam, solarblade) and nowhere else; `data/tags.json`
regenerated byte-identical in shape (the Reg M-B run prints `false`). `chargeStateOf` returns `drawBlocked`, read by
the attack-branch draw and the Pressure PP site. Knob `MEDI_CHARGE_TURN_NEVER_DRAWS`.

**A caveat on the artifact.** The worktree has no store, so a regeneration writes zero usage counts. The committed
`data/tags-regmc.json` is HEAD's file plus the ten param additions; the tag SETS of all rows were compared between
the regenerated and HEAD files and differ on 0 rows. A regeneration in main reproduces it.

**Proof** (probe regulation-aware now; new PRESSURE-NORAIN arm, derived Pressure body: Aerodactyl with Agility):

| run | exit |
|---|---|
| Reg M-C, release `d43292131dd6` (fix) | 0 |
| Reg M-C, knob | 1 (ROD-NORAIN protocol; PRESSURE-NORAIN board `p1.pp[1].electroshot` medicham 2 / showdown 1) |
| Reg M-C, `--release ec377f6f8159` (0.86.1 bytes) | 1 (same three) |
| Reg M-B, fix | 0 (checkout reads GUARDED; tag absent) |
| Reg M-B, knob | 0 (the knob has nothing to restore there) |
| Reg M-B, `MEDI_REDIRECT_BELOW_CHARGE=1` | 1 (the older red still holds) |

## 2. Magic Bounce

**Confirmed first.** New `tests/probe_bounced_fail_names_bouncer.js`, both regulations, derived cast (Hawlucha's Encore
into Hatterene; Hawlucha's last move is Encore, which carries `failencore`, so the bounced Encore fails). On the 0.87.0
bytes: `showdown |-fail|p1a: Hatterene` against `medicham |-fail|p2a: Hawlucha`. The CONTROL (Hatterene on Healer, the
knob-cleared arm) shows the authority's `-fail` naming the clicker there, so the line can name either body and the
ability is what moves it.

**Fix.** `mvFailBounced(clicker, bouncer)` at the three sites a bounced status move can fail: the `affect` branch's
whole-move volatile refusal and both Yawn refusals. It writes `-fail` on the bouncer and sets the BOUNCER's move result
false; the clicker's is left alone. Read, not assumed: `useMove` stores the bounced move's result on the bouncer
(`sim/battle-actions.ts:371-374`), and the clicker's `trySpreadMoveHit` ends `moveThisTurnResult = null` (:616) because
the bounce returned `null`. `mvFail` had written the clicker's result `false`, which Stomping Tantrum reads. The
`abilitywrite` branch's `_failAw` already named the bouncer; it does not set the bouncer's result and is left as is
(noted, not changed). Knob `MEDI_BOUNCED_FAIL_NAMES_CLICKER`.

**Proof.** Reg M-C: green on `148b03f6132e`; red under the knob and on `d43292131dd6` (0.87.0 bytes). Reg M-B: green;
red under the knob and with `--medi` on the 0.87.0 bytes. Fourteen related probes (bounce accuracy, reflectable
class, bounced redirect, pivot, shield-before-bounce twice, Yawn Safeguard and Substitute, both Encore tests, Encore
bracket, Pressure redirect, charge-boost Contrary, charge-turn Protean) exit 0 in both regulations.
`probe_pivot_magic_bounce.js` and `probe_yawn_safeguard_refusal.js` hardcode the Reg M-B checkout and throw under
`--regulation regmc`; with the M-C checkout named both are green. That is the pass-9 trap in two more probes, not
touched here.

## 3. The faint-line instrument question

Selector `omit-protect gen9championsvgc2026regmcbo3-2678516446`, `--games 1200`, release `ec377f6f8159`, census pin
`census-pin-regmc-0d03e83f0e65`, team store `team-pool-frozen-regmc`, `--only-game`:

- **knob off:** no divergence; turn 8 reads `|-damage|p1a: Farigiraf|0 fnt` then `|faint|p1a: Farigiraf` in both.
- **`MEDI_REVIVE_KEEPS_FAINT_LATCH=1`:** the pass-9 card returns exactly: SD `|faint|p1a: Farigiraf`, ME `|upkeep`.
  Turn 7 of the same replay is Pawmot's Revival Blessing reviving Farigiraf at 97/195.

Same game, same release, one knob. The instrument is innocent; the row was the once-per-body latch 0.85.0 reset. The
1200 lattice on the base release carries no faint-line row at all.

## 4. Board-material check

Pins for every run: `--steering empirical --arm middle --end-state`, the census pin named above, `--games 1200`, the
frozen pools read from the main checkout's `data/team-pool-frozen{,-regmc}` (the worktree holds only their receipts).
Written with `--out` to the session scratchpad, never to `data/`.

| regulation | release | games | board-material | narration games |
|---|---|---|---|---|
| Reg M-C | `ec377f6f8159` (base, 0.86.1) | 954 usable | 0 | 6: L, H, B, B, K, M |
| Reg M-C | `148b03f6132e` (0.88.0) | 954 usable | 0 | 4: L, H, B, B |
| Reg M-B | `7822a83cc49b` (base, 0.86.1) | 961 | 0 | 0 |
| Reg M-B | `ac65051daacc` (0.88.0) | 961 | 0 | 0 |

Reg M-B census pin `census-pin-833a997d7e42`. Reg M-B shows neither card in its pool, as expected for a lab-found
shared rule: the Magic Bounce change is carried there by the probe alone.

## 5. Census

`tests/test-mechanics.js` in the worktree: Reg M-B 1006 live / 0 missing / 1006 probed; Reg M-C 1010 / 0 / 1010. Both
equal the pass-9 figures; nothing went down. No census row was added (each mechanic is carried by its probe). The two
regenerated census files were NOT committed: the gate re-read in main pins its own census, and a worktree has no
store. `tests/test-mechanics.js` now lists both new knobs' receipts in `DELIBERATE_BREAK` (0.88.1), so an armed run
refuses to write a census.

## OWED

- `node engine/status.js --write` from main after merge (not run from the worktree: it writes a worktree's missing
  untracked files as fact).
- The Reg M-C lattices 1600 and 1900 on the merged engine, and the gate re-read, per the pass-10 OWED block.
- Remaining Reg M-C narration on the 1200 lattice: Revival Blessing / Leppa order (L), Emergency Exit timing (H), and
  the declared Supreme Overlord row (B, twice).
- `probe_pivot_magic_bounce.js`, `probe_yawn_safeguard_refusal.js`: route the checkout through `engine/showdown_path.js`.
