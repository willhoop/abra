# Living documents brought to 5.245.0 — the whole-game clause reversal

Documentation-only pass. No engine, no artifact, no measurement was run. The only executed commands
were `node engine/quarantine.js --selftest` (210 passed, 0 failed) and `node tests/test-docs-current.js`.

## What the headline reverses

The gating whole-game clause used to count PROTOCOL first-divergence (`j.diverged`) and print
**167 of 961**. It now counts BOARD-MATERIAL games — `state.games` less
`state.games_board_never_diverged` = 961 − 884 = **77 of 961 (8.0%)** — and protocol first-divergence
is carried by `narrationClause` at **167**, which REPORTS (`gates: false`) and exits 1 on
`--narration` while red. Gate: **CLOSED — 1 of 8 GATING clauses fail**.

Every figure written into the documents came from the `## [5.245.0]` CHANGELOG block, except three
that were checked against the artifact itself before use:

| figure | where it came from |
|---|---|
| 884 | `data/game-differential.json` → `state.games_board_never_diverged` (961 − 884 = 77) |
| 102 | the same artifact → `state.protocol_diverged_board_never_did` |
| 11 | derived, as `quarantine.js` derives it: `77 − (168 − 102)`, four artifact fields, unclamped |

No figure was authored. Nothing was taken from a `docs/_reports/` file as current state.

## Files changed

| file | change |
|---|---|
| `docs/ABRA-whitepaper.md` | header 5.244.0 → 5.245.0; new dated block at the top of the stack (technical, with the derivation, the pins, and an explicit "no CI is owed — these are exact counts over one pinned paired sample") |
| `docs/ABRA-deck-plain-english.md` | header bump; new block in plain English — commentary vs board, 77 vs 167, the 11 uncaused, the Unburden bug stated as "every Pokemon that loses an item is wrongly twice as fast" |
| `docs/ABRA-technical-docs.md` | header bump; new block in ASD-STE100 shape (RULE / DEFECT / CHANGE / NAMING / DERIVED FIGURE / RESULT / HOW TO REPRODUCE / NOT RUN), short declarative sentences |
| `docs/DAMAGE-STAGES.md` | header bump; new block — no damage stage and no damage number moved; `data/engine-diff.json` regenerated and now carries a verifiable pin, 0 of 6,000 at the midpoint and both corners; Unburden named as a SPEED defect that enters no damage stage but changes turn order |
| `docs/MODELS.md` | header bump; new block — no model changed, no vector written, quarantine unchanged, but the clause that decides when it lifts now counts a different quantity |
| `docs/SUMMARY.md` | header bump; new block with a seven-row whole-project table (gate, the clause that gates, the clause that reports, the uncaused 11, compared leaves, damage, roster) |
| `docs/ENGINE.md` | new section above the existing 2026-09-04 leaf-widening section: the prediction it left open is RESOLVED (flat, 77 → 77 and 168 → 168), the gate reversal, the exit-2 guard, the five pinned regenerations, Unburden as an ENGINE defect owed a register row |
| `docs/MEASURE.md` | new section: the clause split as this division's own ruler failure, why it is a tightening on the 11, the wrapper on every return path, the arms that tested a copy (186 → 210), the vintage gap, and an OWED block |
| `docs/OPS.md` | new section: the Smogon August 2026 comparison set that feeds nothing, and the store contamination re-measured (85 `isNonstandard: 'Past'`, 0 species Smogon has seen that we never have, the `six ∪ sets` correction to the naive join of 71) |

## Reversals written as reversals

Each new block states, in its own words, that any dated block below it which quotes 167 as *the*
whole-game number is superseded from above and NOT rewritten. No prior conclusion was edited in
place. The two current-state tables that carry `8 of 8 PASS, OPEN` (`docs/SUMMARY.md:208` and
`docs/ABRA-technical-docs.md:367`, both now shifted down by the insert) were checked and are INSIDE
dated 5.209.0-era version blocks, so they are history and were left standing.

## Line endings

The repository is MIXED, and the earlier check in this session that reported every file as CRLF was
wrong — `grep -c $'\r$'` was miscounting. Measured byte by byte instead:

| file | before | after |
|---|---|---|
| `docs/ABRA-deck-plain-english.md` | CRLF 1446, bare LF **0** | CRLF 1448, bare LF **0** |
| `docs/ABRA-technical-docs.md` | CRLF 1766, bare LF **0** | CRLF 1768, bare LF **0** |
| `docs/ABRA-whitepaper.md` | CRLF 2126, bare LF **0** | CRLF 2128, bare LF **0** |
| `docs/DAMAGE-STAGES.md` | CRLF 974, bare LF **0** | CRLF 976, bare LF **0** |
| `docs/MODELS.md` | CRLF 2234, bare LF **0** | CRLF 2236, bare LF **0** |
| `docs/SUMMARY.md` | CRLF 1092, bare LF **0** | CRLF 1106, bare LF **0** |
| `docs/MEASURE.md` | CRLF 5837, bare LF **0** | CRLF 5949, bare LF **0** |
| `docs/OPS.md` | CRLF 142, bare LF **0** | CRLF 168, bare LF **0** |
| `docs/ENGINE.md` | **LF-only**, 38946 | **LF-only**, 39016 |

The splice script derives the terminator from the file it is editing rather than assuming one, so
`docs/ENGINE.md` (LF-only, like `CHANGELOG.md`) did not gain a CR and the eight CRLF files did not
gain a bare LF.

## Gate

`node tests/test-docs-current.js` — **23 passed, 0 failed**. It was 22 passed / 1 failed before this
pass, the failure being exactly the six version-headed documents stuck at 5.244.0.

One clause went red mid-pass and was fixed by citation rather than by baseline: rule **3b(b)** (a
figure attributed to an artifact is IN that artifact) works per PARAGRAPH, so the `SUMMARY.md`
closing paragraph — which named `data/policy-weights.json` — was being asked to find 961, 168, 310
and 1,269,250 inside the weights file. The paragraph now cites `data/game-differential.json` and
`data/smogon-coverage-2026-08.json` where those figures actually live. `data/docs-currency-baseline.json`
was NOT edited.

## Two discrepancies found and not resolved here

1. **The compared-leaf DENOMINATOR disagrees between two sources written the same day.**
   `CHANGELOG.md` §5.245.0 and `tests/probe_uncompared_leaves.js`'s published table say **80**
   (34 compared + 42 unread + 4 declared uncomparable, and 37 + 39 + 4 after this pass). The heading
   of the existing `docs/ENGINE.md` 2026-09-04 leaf-widening section says **56**. Every document
   written in this pass uses **80**, and the new `docs/ENGINE.md` section says so explicitly from
   above rather than editing the older heading. The moved quantities are identical in both readings
   (34 → 37, 42 → 39, 22 → 19). Somebody who knows what 56 counts should say so.
2. **`docs/MEASURE.md`'s `<!-- GENERATED -->` block still reads `5 of 8 gate clauses fail`** and is
   stamped 2026-09-01 17:21. It is a generated block and was not hand-edited. The new section above
   it names the stale figure and points the reader at the gate.

## OWED

- **`node engine/status.js --write`.** Not run in this pass (documentation-only brief). Every
  `<!-- GENERATED -->` block in `docs/{ENGINE,MEASURE,OPS,SEARCH,WEB}.md` is at least one pass
  behind; `docs/MEASURE.md`'s still says `5 of 8 gate clauses fail` where the gate says
  `CLOSED — 1 of 8 GATING clauses fail`, and `docs/OPS.md`'s store counts are from 2026-09-01.
- **A register row for the Unburden speed doubling.** `engine/medicham2-browser.js:14770` —
  `effSpeed` recomputes the doubling from `_hadItem && !m.item`, so every body that loses an item
  gets it. Named in four documents now and still not filed.
- **The compared-leaf denominator, 56 vs 80** (above). One of the two is wrong in writing.
- **`data/winrate-backtest.json` is untouched and still QUARANTINED** — this pass was documentation
  only, so MEASURE's standing priority (re-point `backtest_winrate.js` at the current leaf and
  publish the reliability curve) did not move, and no calibration figure is quoted anywhere in it.
- **The PDFs were not rebuilt.** `docs/ABRA-whitepaper.pdf`, `docs/ABRA-deck-plain-english.pdf`,
  `docs/ABRA-technical-docs.pdf` and `docs/SUMMARY.pdf` still carry 5.244.0 content.
- **Nothing was committed.** Will publishes.
