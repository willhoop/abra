# Publishing abra/regmc 1.0.0 — 2026-09-25

Will said "publish". This is the account of the merge. It is historical and is not maintained.

## What was merged

- `origin/draft/regmc-1.0.0` at `9193edf1` (5 commits over merge base `14c36f21`) into a branch cut from
  `origin/main` at `e4cc21dc`.
- Main had gained 7 commits since the draft was cut: ROTOM v0 (0.126.0), PORYGON2 v0 and its MILTANK leaf
  (0.127.0), and the solver arena on a frozen release with the `doduo` and `mag` greedy bots (0.128.0). No bot
  ingest commits landed in that window.

## Conflicts and how each was resolved

| File | Resolution |
|---|---|
| `CHANGELOG-REGMC.md` | 1.0.0 on top; 0.128.0, 0.127.0, 0.126.0 below it unchanged. The line "DRAFT — held for Will to read. Not published." was removed and a "Published 2026-09-25" note added. The release date stays 2026-09-24, the day the gate was read OPEN. |
| `docs/RUNNING-NOTES.md` | Every row kept. The four 1.0.0 rows sit above the 0.128.0–0.126.0 rows. The 1.0.0 rows keep their DRAFT titles: they are log rows and are not rewritten. |
| `docs/REGMC.md`, `docs/REGULATION-ROTATION.md` | Header takes the draft's `1.0.0 — 2026-09-24`. |
| `data/docs-currency-baseline.json` | Did not conflict. `tests/test-docs-current.js` reported no ratchet movement and left it untouched, so no regeneration was needed. |

Two lines that stated the release as still held were corrected in the same commit, because they became false
the moment it merged: `docs/SUMMARY.md` §5 and `CLAUDE.md` (the Reg M-C gate bullet).

## Owed — not done here

The documents folded in for 1.0.0 do not describe ROTOM v0, PORYGON2 v0 or the frozen-release arena
(0.126.0–0.128.0). Their notes rows each name the owed document (technical docs solver section,
`docs/MODELS.md`). Per the brief the docs were not rewritten in this pass; those land as the next MINOR's rows.

## Checks

- `tests/test-docs-current.js`: 39 passed, 0 failed.
- Pre-commit hook gates ran on the merge commit (no `--no-verify`).
