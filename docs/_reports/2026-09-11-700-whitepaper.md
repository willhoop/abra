# 7.0.0 fold-in — `docs/ABRA-whitepaper.md` only

Dated findings record. Not maintained, not current state, superseded by the register rows and the
notes page it feeds.

## What was done

Folded the 6.x notes rows above 6.0.0 into the white paper and bumped the header to
`**Version 7.0.0 · Last updated 2026-09-11**`. PDF rebuilt to `docs/ABRA-WhitePaper.pdf`
(2,987,930 bytes). `docs/ABRA-whitepaper.pdf` and `docs/ABRA-WhitePaper.pdf` are **the same file** on
this case-insensitive filesystem; the existing name's case was kept.

No other document was touched. No git command was run. No games were played. `tests/test-docs-current.js`
was NOT run (it rewrites the shared baseline).

## The gate verdict is a placeholder, six times

The literal `<<GATE>>` is written wherever a current gate verdict belongs. Every sentence around it is
phrased to survive either reading.

| line | site |
|---|---|
| 5 | the 7.0.0 headline |
| 7 | "THE CURRENT VERDICT, WHICH IS THE ONLY ONE THAT GOVERNS THIS DOCUMENT." |
| 1648 | §0.3, whether phase 1 is reached |
| 1704 | §1, leaf calibration — the gate decides only whether it may be re-run |
| 1750 | §3, whether the instrument readings add up to "MEDICHAM is correct" |
| 2173 | §6 item 6, R1/R2/R3 |

## What the new 7.0.0 block publishes, and what binds it

All on release `534442d71183` unless stated. Every figure sits in a paragraph naming the artifact that
holds it, or the CHANGELOG entry that carries it (the 6.6.2 ratchet).

- Whole-game: `state.games` 961 less `state.games_board_never_diverged` 961 = 0; narration
  `state.protocol_diverged_games` 0 and `state.protocol_diverged_board_never_did` 0, **with no declared
  row subtracted** — the 6.0.0 closeted perish row was closed at 6.26.0 (1 of 961 → 0 of 961). Void 0,
  cut-off 0 at cap 50, 10,705 of 10,705 boundaries identical, verdicts 960 / 0 / 0 / 0 / 1 THREW.
- Sample: mode A/middle, `pins.digest` `de38d17e15a2`, pool `data/team-pool-frozen`, 8,778 teams /
  1,968 picked / digest `0d103fb9fa87`, census pin `9b14cebfae95`, 43 Illusion teams dropped,
  natures 17,440 real against 96 fallback, 2,626 stones kept and 0 stripped.
- Damage: 6,000 requested / compared / agreed, 0 disagreed at the midpoint and at all sixteen indices;
  **both multi-hit skip counters now 0**, `volley.move_rows` 130 and `volley.bond_rows` 12.
- Roster: 142 / 139 / 486 FIRED-AND-BOARDS-MATCH, 0 differ and 0 DID-NOT-FIRE on every stage; scope
  148 / 200 (116 of 316 out of scope) / 497; could-not-stage 6 / 43 / 8; plant anchors 18, 44, 36 with
  `reds_ran` true.
- Census 883 live of 883 probed; harness 4,702 games, 0 threw, 3,836 planner-built, diverged 4 moves /
  1 ability / 0 items.
- Corners, on release `b42b81899631`: 1 of 961 and 2 of 961, protocol 12 and 15. **Stated as not
  re-measured on the current release**, because they were not.

New §3.2 records the 6.x work in the document's voice: the twenty owed probes, the staging planner,
one scope module, the eight partings found and fixed, the engine fixes behind their own switches, the
clone-cost and interleave benches, and the ruler corrections (provenance temp-dir credit, the
quarantine classifier, the traceability ratchet, RULE 5).

## The basis change, as written

7.0.0 states plainly that the 6.0.0 headline and this one are **not two readings of one question** —
the instrument set widened (6.9.1, 6.10.0, 6.13.0, 6.14.0, 6.1.0) and immediately found eight partings
the 6.0.0 gate could not stage, which is why "MEDICHAM is correct" was withdrawn at 6.16.0. And that a
lifted quarantine makes nothing downstream true: every downstream artifact is RE-RUNNABLE, not true,
**none was re-run** (Will, 2026-08-11, quoted verbatim; re-run list ROADMAP #57), so MAG, MILTANK,
PORYGON2, DODUO, SLOWKING, GARY, leaf calibration, R1–R4 and every head-to-head stay ABSENT with their
generators named. **No withdrawn figure was restored.**

## Flags found by `engine/docs_scan.js` and fixed in this pass

Scanned before and after; the white paper's baseline was 0 retraction violations and 0 citation
mismatches, and it ends at 0 for both.

1. **10 flags on the first pass.** My "THE SAMPLE" paragraph named `data/move-priors.json` and
   `data/rollout-switch-census.json` by path but not `data/game-differential.json`, so its figures were
   judged against the two artifacts it did name and 7 went unbound. **The citation rule judges a figure
   against EVERY artifact the paragraph names by path** — adding the differential's path did not clear
   `345`; removing the other two paths did. The sentence now names the source by field
   (`steering.driver_inputs`) instead.
2. Corner protocol counts 12 and 15 bound by naming `CHANGELOG.md` 6.22.0, which carries the pair.
3. My dated pointer line under the 6.0.0 corner paragraph restated operands that would not bind; it now
   points at the 7.0.0 block, where both operands of each subtraction are named.

Final: retraction violations 0, citation mismatches 0, unbound 0, quarantined figures 0, untraceable
census for this document absent (corpus total unchanged at 9, in two other documents). Grandfathered
figures 261 → 260 — one sentence I rewrote lost its grandfathered place and is bound or gone, which is
the ratchet's permitted direction.

## Dated blocks: added after, never rewritten

The 6.0.0 headline block and the 2026-09-11 corrections stand as written. Dated follower lines were
added where they had been overtaken: under the 6.18.0 update (#318 and #511 both closed), under the
6.0.0 damage paragraph (the volley loop is compared now), under the 6.0.0 corner paragraph, and under
§3.1's endpoint-instrument record. Present-tense claims that were not dated were rewritten: §0.3's
phase table and paragraph, §3's opening, and three sites that asserted "MEDICHAM is not correct" as a
standing fact rather than as a gate verdict.

Two sentences that presupposed the withdrawn 2026-07-23 leaf verdict (flagged in
`docs/_reports/2026-09-11-withdrawals-4.md` as owed and untouched) are fixed: §1's "whether the
inversion replicates" and §7's "speed bias that inverted the greedy engine".

## Could not bind, or deliberately not written

- **`740 of 845`** (the pre-integration staged-fired count) was left out of §3.2. It is retracted in the
  6.14.0 notes row, and restating a retracted figure is what the retraction registry exists to catch.
  Only `783 of 845` is published, bound to CHANGELOG 6.14.0.
- **The corner ladder figures** (16→12, 15→11, 12→8) were left out for the same reason — 8 and 11 are
  retracted by the 6.7.0 row. The ladder is described without digits; the current readings are published.
- **A current "staged mechanics fired of 845"** is not published. `engine/coverage.js` computes it, no
  artifact carries it, and `docs/ENGINE.md` has no such line today, so there was nothing to bind it to.
  The harness artifact's own counts are published instead.
- **The clone-cost figures** (0.32 / 0.44 of a turn) are written as CHANGELOG 6.9.2's words — "about a
  third of one turn", "slightly under half" — because `data/verification/clone-cost-2026-09-11/` holds
  only `bench_clone.js` and no artifact. The `6%` figure is bound to that entry.
- **The 6.12.2 profiling shares** are not published; they come from a rollout profile and the claim that
  survives without them ("skipping narration in rollouts would save nothing") is qualitative.

## Owed / open, for whoever picks this up

- **`<<GATE>>` × 6 must be filled from a verified run** before this is published. A parser fix was in
  flight while this was written.
- **The corner arms are two releases stale** (`b42b81899631`, generated 16:01 and 16:05, against the
  20:36 differential). The document says so; re-measuring them on `534442d71183` would let that caveat go.
- **RULE 5 not verified for `b42b81899631`.** The release directory exists on disk; whether its bytes are
  TRACKED was not checked, because this pass ran no git. `data/releases/534442d71183` likewise. Worth a
  `git ls-files` before publishing, since a published figure may not rest on an untracked release.
- **`data/mechanics-census.json` was generated at 21:07**, after the 20:3x artifacts, with no release
  stamp. Its 883/883/0 agrees with the 6.26.0 notes row, but if another agent regenerated it mid-pass the
  figure should be re-read before publication.
- The four RULE-5 "hard arm" artifacts (`million-run`, `million-run-150k`, `mutation-coverage`,
  `search-decision-profile`) are **not cited by path anywhere in the white paper**; its one mention of
  `data/million-run.json` states no figure from it. Nothing was owed here after all.
