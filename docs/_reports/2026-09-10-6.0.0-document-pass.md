# 6.0.0 — the living-document fold-in for the MEDICHAM major

**2026-09-10, MEASURE. Historical by construction: every figure below was read out of the artifact
named beside it at 09:5x–10:1xZ on 2026-09-10, and `node engine/status.js` is what is true now.**
Documents only. Nothing under `engine/`, `tests/` or `web/` was edited, nothing was run that plays a
game, and **no artifact was re-run**. Nothing was committed or pushed.

---

## 0. THE ONE LINE

The gate is OPEN, the six living documents carry a 6.0.0 block and a 6.0.0 header, the backlog is
**0 of 100**, all **23** cited-artifact mismatches in the living set are cleared, and **8 untraceable
figures are handed back by name rather than sourced by invention**. `node tests/test-docs-current.js`
reads **35 passed, 0 failed**. **No downstream figure was restored, because none was re-run** — the
partial lift is written as one in all six documents.

---

## 1. THE NUMBERS, RE-DERIVED ON THE DAY

Release **`cbd510bc2b13`**, authority commit `20ad99ffc9a5a4a4e8fb56ab04ad8e4255b3f2b4`. The brief's
figures were re-read rather than trusted; two of them had moved.

| figure | value | `path:field` |
|---|---:|---|
| gate | **OPEN, 0 of 9 clauses fail** | `node engine/quarantine.js`, `node engine/major_readiness.js` |
| games played, primary arm `middle` | **961** | `data/game-differential.json:state.games` |
| board never parted | **961** | `:state.games_board_never_diverged` |
| **BOARD-MATERIAL** | **0 of 961** | the subtraction of the two above — the gate's own clause |
| void excluded / before exclusion | **0 / 961** | `:state.games_void_excluded`, `:state.games_before_void_exclusion` |
| cut off by the cap / cap / longest game | **0 / 50 / turn 36** | `:state.games_cut_off_by_the_turn_cap`, `:turns_cap`, `:mid_void.by_reason_detail` |
| turn boundaries compared / identical | **10,705 / 10,705** | `:state.turn_boundaries_compared`, `:state.turn_boundaries_identical` |
| protocol diverged / of which board never did | **1 / 1** | `:state.protocol_diverged_games`, `:state.protocol_diverged_board_never_did` |
| **NARRATION** | **0 undeclared of 961** | 1 raw less the 1 closeted row (perish above `\|upkeep\|`, ROADMAP #440) |
| end state | SAME **960**, DIFFERENT 0, ENDED-APART 0, THREW 1 | `:end_state[0].summary.verdicts` |
| by-cause | causes 1, NARRATION_ONLY 1, BOARD_MATERIAL 0, reconciles true | `:end_state[0].summary.by_cause_totals` |
| board fields declared uncompared | **8** — *was 9 in the brief; item disposition became COMPARED at 5.283.0* | `:state.not_compared` |
| board leaves | compared **54**, ceiling **54**, total **80**, hole **20** (18 duration-1 + 2 self-removed), standing at a boundary **0** | `tests/probe_uncompared_leaves.js derive()` |
| damage differential | **6,000 / 6,000 / 6,000 / 0** at 17 roll indices, seed 20260804, `band_missing` 0 | `data/engine-diff.json` |
| damage skips | `skipped_multihit` **134**, `skipped_ability_multihit` **17**, `pool.dropped` 9 | same |
| roster items | 148 in scope, **142 tested**, differ 0, DID-NOT-FIRE 0, 6 fixture gaps | `data/roster.items.json:scope` |
| roster abilities | 316 total, **115** no legal carrier, 201 in scope, **139 tested**, differ 0, 43 gaps, 14 CONTROL-NOT-QUIET, 5 deferred | `data/roster.abilities.json:scope` |
| roster moves | 500 total, 2 no legal carrier, 498 in scope, **487 tested**, differ 0, 8 gaps, 3 deferred | `data/roster.moves.json:scope` |
| carrier derivation | **347** legal species, 72 mega, 83 battle-only | `data/roster.items.json:scope.carrier_derivation` |
| census | **835 probed / 835 live / 835 armed / 0 missing / 0 hollow / 0 threw**, `run_ok` true | `data/mechanics-census.json` |
| staged mechanics | **1,313** games, 0 threw, 0 sheets unassembled | `data/all-mechanics-fire.json` |
| corner arms | **16 of 961** and **15 of 961** on release `cbd510bc2b13` | `data/verification/gd-{top,bottom}-tie-first-2026-09-10.json` |
| tie groups on those arms | 38,319 and 32,690, none attributable to tie order | `:speed_ties.tied_groups_resolved`; `docs/_reports/2026-09-10-corner-arms.md` |
| pool | `data/team-pool-frozen`, **8,778** teams, **1,968** picked, digest `0d103fb9fa87` | `:steering.team_pool_*` |
| pins | mode A `de38d17e15a2`; census pin `ab219c68f165`; `--games 1200 --turns 50 --arm middle` | `:mode`, `:pins`, `:steering.input_digest` |
| declared exclusions | Illusion **43** teams; spreads synthetic; natures real (**17,440** declared, **96** fallback); mega stones **2,626** kept, 0 stripped | `:closet`, `:declared_gaps` |
| re-runnable / LIFT / STAY | **64 / 40 / 24** | `node engine/quarantine.js`, `node engine/major_readiness.js` |

**Two figures in the brief were stale and are corrected here.** `state.not_compared` is **8**, not 9 —
item disposition was wired into the comparator at 5.283.0. And "43 of 80 leaves in neither the compared
nor the declared-uncomparable list" no longer holds: `derive()` now reads `hole` 20 with
`standing_at_the_boundary` **0**, every one of the 20 accounted for as an 18-strong duration-1 family
plus 2 that remove themselves inside the action.

---

## 2. WHAT WAS WRITTEN

### 2a. Six living documents, each with a 6.0.0 block and a 6.0.0 header

| document | header | block |
|---|---|---|
| `docs/ABRA-whitepaper.md` | 5.266.0 → **6.0.0** | headline with both operands named; narration; the bound on "correct"; the sample; damage; roster; census; **the corner arms**; the partial lift; what it does not establish |
| `docs/ABRA-deck-plain-english.md` | 5.266.0 → **6.0.0** | five plain-English paragraphs, including *what the number is not* and *what comes back and what does not* |
| `docs/ABRA-technical-docs.md` | 5.266.0 → **6.0.0** | ASD-STE100: one fact per sentence, every field named as a read-then-assert |
| `docs/SUMMARY.md` | 5.266.0 → **6.0.0** | question/artifact/answer table, *why this is a major*, *and it is a partial lift* |
| `docs/MODELS.md` | 5.266.0 → **6.0.0** | the distance is zero; re-runnable is not true; leaf calibration is not in this version; the refit stays owed |
| `docs/DAMAGE-STAGES.md` | 5.266.0 → **6.0.0** | the damage chain did not move, and the skip is now two families |

**`docs/DAMAGE-STAGES.md` was not on the brief's list and had to be included.** `documentedAt()` in
`engine/docs_scan.js` takes the floor over *every* unpinned versioned living document, not over the
five named ones, and this file held the floor at 5.266.0 on its own. With it left behind the backlog
read **87 of 100** after all five headers had been raised.

### 2b. Living (unstamped) sections rewritten, not restamped

- White paper: §0.3 plan block (*phase 1 REACHED, phase 2 has not begun*), §1 leaf-calibration
  paragraph, §3 header (validated against the **authority**, not only the Smogon calculator), §6 item 6
  (R1/R2/R3), item 7 (exploitability *stays* withheld by decision), item 8 (speed artifact named).
- Deck: the leaf/engine blank, the MEDICHAM bullet on slide 4, the click-censoring counts, the
  beatability blank, the speed parenthetical.
- Technical docs: the frozen-source count now points at `SOURCES`, `M.TRACE_EVENTS` now points at
  `data/protocol-events.json` (`emittedCount` 44) instead of carrying a hard-coded 36.
- `SUMMARY.md`: the plan block, the leaf/engine contrast, and three CURRENT-STATE table rows —
  MEDICHAM, MAGNEMITE and click censoring.
- `MODELS.md`: a state block under the MEDICHAM heading, and a *stays withheld* sentence at the head
  of the MAG and MILTANK sections.

Every version-stamped block was left where it stood. Dated waypoints are not edited in place.

### 2c. `docs/RUNNING-NOTES.md`

- The **6.0.0 row** at the top: what changed, what was measured, **a `NOT MEASURED, AND SAID SO`
  bullet**, `**Basis.** CHANGED` with the three things a reader can no longer be told, `Supersedes`,
  and an `Owed now` list.
- **All 48 `[Unreleased]` headings renamed to the version they landed as**, derived from git rather
  than typed: for each row, the commit that first introduced its title, then the earliest CHANGELOG
  version greater than that commit's parent's top. 22 → 5.267.0, 14 → 5.268.0, and singletons through
  5.274.0. The mapping is monotone in date, which is the check that it is not noise.
- **Two of the 48 were renamed to `[6.0.0]` instead**, because they declare `**Basis.** CHANGED` and
  clause 5d refuses a basis change released as anything but `X.0.0`: *"the turn cap was the horizon"*
  (whose own text says *"whoever cuts the version owes it `X.0.0` with the document set folded in"*)
  and *"the spread move's named target is a DIE"*. Both are absorbed by the 6.0.0 row's `Basis` line.

### 2d. `CHANGELOG.md`

`## [6.0.0] — 2026-09-10` — the gate, the fold-in, the 23 cleared citations, the partial lift, and a
`### Notes` block carrying what "correct" does not mean, the owed refit, the 8 handed-back figures, and
the three-places statement (local yes, GitHub owed to the publisher, live site not updated).

---

## 3. THE 23 STALE CITATIONS — CLEARED WITHOUT A SINGLE RE-RUN

`node engine/docs_scan.js --quarantine` now reads **cited-artifact mismatches: 0**, down from 23. Each
was handled one of four ways. **None was struck**, and that was deliberate: `retractionRegistry` in
`engine/docs_scan.js` promotes anything inside `~~…~~` on a line carrying *retract* to a **STRONG**
registry entry that then accuses every restatement corpus-wide, and none of these figures had been
shown to be wrong.

| doc | figure | what was actually true | action |
|---|---|---|---|
| technical docs ×2 blocks | 81, 82, 21 (side-selection) | the artifact reads **103 sites / 78 undeclared** today | re-cited today's value beside the dated reading |
| white paper | 322 | 322 is the damage table's species count, not a field of `data/policy-weights.json` | citation corrected to say so |
| white paper | 24.2, 26.5 | **notation, not decimals** — order 24 subOrder 2, order 26 subOrder 5 | rewritten as order-and-subOrder |
| white paper | 1,995 | `data/wire-ladder.json:games_per_arm` has read **1,997** since it was generated in 2026-08 | **corrected to 1,997 with the correction stated on the line** |
| white paper | 26 | an ISO timestamp `…T21:04:26Z` being lexed as a figure | wrapped in inline code, which the lexer skips |
| white paper + SUMMARY ×2 | 19,589 / 8,713 | already declared as *never in the artifact* by the 5.259.0 correction block | the quoting sentence marked `previously` so it is read as the report it is |
| white paper | 181 | a 3.40.0 census reading; the artifact reads **835 live of 835** today | marked as a dated reading with today's value beside it |
| `MODELS.md` ×4 | 1.256 / 1.544 / 0.9943 / 1.4080 | `data/pory-eval.json:reduced_form` reads **0.9809 / 1.4093** today | today's pair added; the two historical pairs marked as such |
| `SUMMARY.md` | 56 | the stale half of a sentence *about* staleness; the artifact reads `emittedCount` **44** / `notEmittedCount` **50** | the stale pair named as stale |
| `SUMMARY.md` | 85 | a reading of **our store**, not a field of `data/smogon-coverage-2026-08.json` | said so on the line |
| `SUMMARY.md` | 144 | 144 is 12 × 12 derived; `matrix` is a 12-key object of 12-key objects | rewritten as "a 12 x 12 `matrix`" |
| `SUMMARY.md` | 3.6978 | it is in `baselines`, not in `species_only_clone` | the correct field cited |
| `SUMMARY.md` | 1,163,315 | **Smogon's** battle count, contrasted with the count in `data/live.js` | attribution corrected on the line |

The ratchet in `data/docs-currency-baseline.json` tightened by itself as a result — **23 entries
retired, 45 → 22**, and every one of the 22 that remain is in `docs/RUNNING-NOTES.md`, which is a log.
The file was written by `tests/test-docs-current.js`, **not hand-edited**.

---

## 4. THE 8 UNTRACEABLE — HANDED BACK, NAMED, NOT INVENTED AND NOT DELETED

They are the three groups the 2026-09-10 sweep already handed back (`docs/_reports/2026-09-10-untraceable-sweep.md` §3),
and they still need a judgement on a model that is Will's. **They are left standing in the documents.**

**(a) DODUO's held-out log-likelihood table — `−3.3425 / −3.3318 / −3.2447`, `docs/MODELS.md:1073-1075`.**
`data/policy-weights-joint.json` has never carried a held-out block at any of its 18 revisions, so the
table is `fit_joint.js` console output.
*Options:* (1) withhold the table, on the 2026-08-15 precedent in the white paper; (2) have the next
joint fit persist a `heldOut` block and re-cite. **MAG-family, and MAG is paused.**

**(b) The paired refit table — `−0.000076 / −0.000172 / −0.117`, `docs/ABRA-whitepaper.md:2559-2560`
and `docs/MEASURE.md` §13.** No artifact and no generator exists; the scoring script was not kept. The
46,162 held-out decisions ARE real (`data/policy-weights-pre-censoring.json:corpus.test`); the paired
differences are not. The honest unit is the whole table, not the three cells the census names.
*Options:* (1) withhold the table; (2) re-run the paired comparison and persist it. This is MEASURE's
own headline for *"the refit bought nothing"*, so withholding it is a publication decision.

**(c) The partial-label EM validation table — `1.0208 / 1.0021`, `docs/ABRA-whitepaper.md:2314-2315`.**
`data/partial-label-em.json` was regenerated on 2026-08-26 and holds none of the six figures — different
numbers for the same question — and its generator `engine/em_validation.js` is on the **STAY** list, so
its current values may not be quoted either.
*Options:* (1) withhold the table until the artifact is quotable; (2) restate it from the 2026-08-26
blob after the MAG refit. Either way the whole table moves.

---

## 5. THE PARTIAL LIFT, WRITTEN AS ONE

`node engine/quarantine.js` lists **64** artifacts downstream of MEDICHAM as RE-RUNNABLE.
`node engine/major_readiness.js` splits them **40 LIFT / 24 STAY**.

**The 24 STAY** — generator writes or reads `data/policy-weights.json`, or is MILTANK:
`brood`, `censoring-value`, `collinearity-audit`, `exploit-step-probe`, `exploit-step-probe-reparam`,
`exploitability`, `exploitability-mag`, `exploitability-machamp`, `ladder`, `mag.js`,
`opponent-calibration`, `opponent-recall`, `partial-label-em`, `policy-weights`,
`policy-weights-joint`, `policy-weights-joint-presheet`, `recall-at-k`, `rollout-r3`,
`scoreboard.js`, `scoreboard.json`, `search-decision-profile`, `seed-source-audit`,
`sheet-channel-value`, `weight-multiplicity`.

**The 40 LIFT were NOT re-run** (Will, 2026-09-09: *"Dont re run the artifacts weve bene over this"*),
so leaf calibration, the leaf/engine and leaf/position contrasts, the click-censoring census, every
rollout figure, the replay differentials, MEW's bundle and every speed artifact are **absent from the
documents with their generator named**. **MEASURE's one number is not in this release.** It is one
command when the owner wants it.

---

## 6. GREEN

```
node tests/test-docs-current.js      35 passed, 0 failed
node engine/docs_scan.js --owed      0 of 100 owed; documents last folded at 6.0.0
node engine/docs_scan.js --quarantine  0 cited-artifact mismatches, 0 retracted-restated, 22 untraceable corpus-wide (8 living)
node engine/major_readiness.js       GATE OPEN 0/9 fail; 0 withheld-source, 0 stale-citation, 8 untraceable
node tests/test-roadmap-register.js  3 passed, 0 failed
node engine/status.js --write        stamped the five division ledgers
node build/build_pdfs.js             15 built, 0 failed (7 excluded: 5 division ledgers + 2 declared residuals)
```

**Not committed and not pushed** — the publisher does that. Three places: **local yes; GitHub not yet;
live site NOT updated** (WEB is paused, `tests/test-site-sync.js` waived by Will 2026-09-09).

---

## OWED, NOT RUN

```bash
# 1. Re-verify everything this pass claims, in the order it was claimed.
node engine/quarantine.js
node engine/major_readiness.js
node engine/docs_scan.js --owed
node engine/docs_scan.js --quarantine
node tests/test-docs-current.js
node tests/test-roadmap-register.js
```

```bash
# 2. Publish. NOT run here.
git add -A
git commit
git push
```

```bash
# 3. The release the six gate artifacts stamp must be tracked before the numbers are cited from git.
git ls-files data/releases | grep -c cbd510bc2b13     # 0 -> git add -f data/releases/cbd510bc2b13
node engine/engine_release.js drift cbd510bc2b13
```

```bash
# 4. The 40 LIFT artifacts, ONLY when Will asks. Six processes maximum; RAM is the cap.
#    Each restores a figure the six living documents currently leave blank.
cmd /c tools\lownode.cmd engine\backtest_winrate.js          # leaf calibration - MEASURE's one number
cmd /c tools\lownode.cmd engine\leaf_engine_contrast.js
cmd /c tools\lownode.cmd engine\leaf_position_contrast.js
cmd /c tools\lownode.cmd engine\click_census.js
cmd /c tools\lownode.cmd engine\bench_speed.js
node engine/quarantine.js    # prints the re-run command beside each of the 64 - copy from there
```

```bash
# 5. The MAG refit. A REFIT, not a restamp. ASK WILL FIRST. It unblocks the 24 STAY artifacts.
node --max-old-space-size=4096 engine/fit_policy.js
node --max-old-space-size=4096 engine/fit_joint.js
node engine/miltank.js
```

```bash
# 6. The three untraceable tables, once Will has chosen withhold or re-measure (see section 4).
node engine/docs_scan.js --quarantine --json    # the 8, with line numbers
```
