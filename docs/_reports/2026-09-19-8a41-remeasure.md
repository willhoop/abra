# 6.54.0 re-measured on `8a4140de3eaa`, and the three stranded roster plants re-aimed — 2026-09-19, ENGINE

A findings record, not a living document. Superseded by the register rows it feeds; not cited as current
state. `node engine/status.js` and `node engine/quarantine.js` hold the current state.

---

## PREDICTION — written before any lattice was launched

The three remaining board-material games on `a1c7dcd5696b` (`state.first_board_divergences` of the three
artifacts at HEAD `86ccc49e`, which still hold the `a1c7dcd5696b` run: 0, 1 and 2 entries, under the cap
of 40, so the whole population):

| lattice | game (first seed) | config | mechanism | fix in 6.54.0 | predicted |
|---|---|---|---|---|---|
| 1350 | `…2636045527` | baseline | Sucker Punch into a target Encore turned into Swords Dance (turn 15) | **none** | **STAY** |
| 1950 | `…2635092694` | omit-weather | Cursed Body Disable against Triple Axel (`vol.disable` 0 vs 3, turn 6) | on-hit chance abilities roll per landed hit; Triple Axel's accuracy drawn per hit | **LEAVE** |
| 1950 | `…2657391947` | pair-protect-bust | Future Sight payout KOs a Focus Sash Kleavor the authority leaves at 1 (turn 6) | **none** | **STAY** |

**Predicted board-material: 0 / 1 / 1** at `--games` 1200 / 1350 / 1950.

Narration: the 1350 Cursed Body-per-hit game (`…2654469023`) should leave with the same fix. The 1950
Poison Point-on-a-multi-hit ordering game (`…2635518198`) may leave (the per-arrival roll moves where the
reaction fires); not confident. Predicted undeclared narration **0 / 10 / 23 or 24**.

**Joins are the risk.** 6.54.0 moved die addresses (Triple Axel's accuracy per hit, per-arrival reaction
rolls, Effect Spore's die gated on the attacker's powder immunity) and changed Protean, Clear Smog,
Contrary under Mold Breaker, Cud Chew and Copycat. Any game touching those can part somewhere new.
Predicted joins: none, held weakly.

---

## 0. VERDICT

- **Lattices on `8a4140de3eaa`: board-material 0 of 961 / 1 of 1069 / 1 of 1497**, from 0 / 1 / 2.
  **Exactly the prediction.** Cursed Body against Triple Axel (`…2635092694`, g1950) LEFT. Sucker Punch
  into an Encored Swords Dance (`…2636045527`, g1350, turn 15, Scolipede 85 vs 135) and Future Sight
  into a Focus Sash Kleavor (`…2657391947`, g1950, turn 6, 0 vs 1 HP) STAYED at the same turn with the
  same diff. **No game joined**, board or protocol, on any lattice.
- **Narration (undeclared): 0 / 10 / 23**, from 0 / 11 / 24. Raw 0 / 11 / 25, less the declared Supreme
  Overlord `fallenundefined` row (1 and 2 games). Left: the Cursed Body per-hit game `…2654469023` (g1350)
  and the Poison Point multi-hit ordering game `…2635518198` (g1950). None joined.
- **Roster: items 148 of 148 MATCH** (Greninjite now MATCH, the 6.54.0 Protean fix), **abilities 194 of
  200, moves 494 of 497**, 0 DIFFER / DID-NOT-FIRE / COULD-NOT-STAGE in every stage. Anchors 22 of 22,
  63 of 63 and 37 of 37 live; reds 22 / 63 / 37 CAUGHT, 0 not.
- **Gate `CLOSED — 1 of 8`**, from 4 of 8. Only board-material fails.
- **Part A: the three stranded plants are re-aimed and each is CAUGHT on a single-rule `--reds` run.** No
  other plant was dead.

## 1. PART A — THE PLANTS

A string count of every `patch: [[…]]` in `tests/roster.js` (124 plants) against
`data/releases/8a4140de3eaa/engine/medicham2-browser.js` found exactly three dead, the three the brief
named. Two more plants did not parse in the scratch counter (one has a comment with an apostrophe inside
its array, the other names the `CRIT_X15` constant); both were counted by hand at 1 match each. So nothing
6.54.0 touched (Protean, Clear Smog, Contrary, Cud Chew, Copycat, the multi-hit loop) stranded a plant by
text, and the full `--reds` stages in §3 showed every plant still moves a board.

| rule | old anchor (0 matches) | new anchor | single-rule `--reds` on `8a4140de3eaa` |
|---|---|---|---|
| `ability/contact-statuses-the-attacker-by-chance` | `const _r=rng();let _cum=0;` | `for(const _inf of _pun.inflicts){` (the walk over the tag's list, emptied; the die is still thrown) | **CAUGHT** via `effectspore`, CONTROL-NOT-QUIET on `hp`, `status`, exit 0 |
| `ability/cures-a-status-at-the-residual-by-chance` | the up-front Healer / Shed Skin die line | the `curesStatusResidual` tag read, `null&&` | **CAUGHT** via `healer`, DID-NOT-FIRE on `status`, exit 0 |
| `move/boosts-target` | `_t.boosts[_s2]=clamp(_t.boosts[_s2]+_d,-6,6);` | `function boostTableOnto(`: a no-op takes the name and the real body is renamed | **CAUGHT** via `babydolleyes`, FIRED-AND-BOARDS-DIFFER on `boosts.atk`, exit 0 |

Before the edit, on the same release, the boost rule's single-rule run printed `DEAD ANCHOR
move/boosts-target matched 0 time(s)` and exited 1. The two ability rules were dead by the same string
count the roster's own anchor audit uses; they were not re-run before the edit. Both ability verdict
shapes match what the pre-6.52.0 plants produced (`git show 07e01c85:data/roster.abilities.json`).

The boost plant is tied to the function NAME, not its parameter list. The `applyStatus` anchor died twice
when that function gained a parameter; this form survives that.

## 2. PINS

| pin | value |
|---|---|
| release | `8a4140de3eaa`, cut 2026-09-19T07:46:28Z on HEAD `86ccc49e`. `engine_release.js list`: 0 of 27 files moved. `tests/roster.js` is not in `SOURCES` (named only in comments of `engine/engine_release.js`) |
| census | `data/mechanics-census.json` at HEAD, 925 live, generated 07:46:20Z, `steering.input_digest` **`9f251986d34e`** in all three artifacts (= `sha12Content` of the file). The last run used `1b735ff555ad` (915 live), so this is not a strict before/after |
| team store | `--team-store data/team-pool-frozen`, pools `0d103fb9fa87` / `7e7a37ded7fc` / `a5ce76242f8d`, the same as the last run |
| flags | `--release 8a4140de3eaa --steering empirical --arm middle --end-state`, cap 50 (`TURNS_DEFAULT`), `--games` 1200 / 1350 / 1950, `--write --out` to the three canonical paths, `--dump-games 400 --dump-out data/_8a41_dump_g<N>.json` (12 and 26 cards, moved to the session scratchpad afterwards; 1200 wrote none) |
| mode | `A/middle/pins:de38d17e15a2/credit:observed-effect/v1/nature:real`, `driver_code_stable` true, 0 void |

## 3. RESULTS

| `--games` | games | board-material | narration undeclared (raw) | protocol-diverged | boundaries identical | generated | wall |
|---|---|---|---|---|---|---|---|
| 1200 | 961 | **0** (was 0) | **0** (0) | 0 (was 0) | 10705/10705 | 08:30:30Z | 692 s |
| 1350 | 1069 | **1** (was 1) | **10** (11) | 12 (was 13) | 11840/11842 | 08:34:11Z | 914 s |
| 1950 | 1497 | **1** (was 2) | **23** (25) | 26 (was 28) | 16710/16714 | 08:38:17Z | 1155 s |

Board-material is `state.games − state.games_board_never_diverged`. Narration is quarantine's NARRATION
clause. Left / stayed / joined come from `state.first_board_divergences` (1 and 1 entries, cap 40) and
`first_divergences` (12 and 26 entries, cap 60), so both are the whole population, compared against
`git show HEAD:` of the three artifacts (release `a1c7dcd5696b`).

| predicted | actual |
|---|---|
| 1950 Cursed Body `…2635092694` LEAVE | **LEFT** (board and protocol) |
| 1350 Sucker Punch / Encore `…2636045527` STAY | **STAYED**, t15 → t15, same diff |
| 1950 Future Sight / Focus Sash `…2657391947` STAY | **STAYED**, t6 → t6, same diff |
| narration 1350 Cursed Body per hit `…2654469023` LEAVE | **LEFT** |
| narration 1950 Poison Point multi-hit `…2635518198` maybe | **LEFT** |
| joins: none | **none** |

| artifact | before | after | reading |
|---|---|---|---|
| `data/roster.items.json` | 06:15:33Z | 08:39:01Z | 148 MATCH, 0 DIFFER; anchors 22/22, 22 CAUGHT. Exit 0 |
| `data/roster.abilities.json` | 06:21:51Z | 08:42:38Z | 194 MATCH, 6 DEFERRED-BY-OWNER; anchors 63/63, 63 CAUGHT. Exit 0 |
| `data/roster.moves.json` | 06:28:15Z | 08:46:09Z | 494 MATCH, 3 DEFERRED-BY-OWNER; anchors 37/37, 37 CAUGHT. Exit 0 |
| `data/engine-diff.json` | 06:40:07Z | 08:46:59Z | 0 of 6000 at midpoint, top, bottom, idx01–idx14 (seed 20260804) |
| `data/all-mechanics-fire.json` | 06:32:50Z | 08:49:39Z | 4702 games, 0 threw. Moves STATE 2 → 1 (Clear Smog left; Axe Kick stays). Copycat left the diverged list |

`node engine/quarantine.js` (via `tools\lownode.cmd`, exit 0, 118 s): **`GATE: CLOSED — 1 of 8 GATING
clauses fail`**: whole-game BOARD-MATERIAL (0 / 1 / 1). Damage, roster items / abilities / moves,
coverage (412 of 412), mechanics (2 diverge, 1 declared, 1 below the shelf, 0 left) and no-open-defect
all PASS. NARRATION (RPRT) 0 / 10 / 23.

## 4. NARRATION BY MECHANISM (undeclared 10 at 1350, 23 at 1950)

Bucketed by hand from the full dumps (12 and 26 cards, the whole population), less the board-material
seed and the declared `fallenundefined` cards (`…2661010853`; `…2663628673`, `…2656799052`). Every card
that remains is one the last run bucketed, at the same turn with the same pair of lines.

| family | mechanism | 1350 | 1950 | games |
|---|---|---|---|---|
| **refusal lines** (10) | spread move with every foe fainted: ours prints an extra `-fail` | 2 | 1 | `…2656658836`, `…2657573463`, `…2634671092` |
| | Synchronize into an immune body: the authority's `-immune` is missing | 2 | 1 | `…2654088012`, `…2654041758`, `…2634370112` |
| | Coaching with no living ally: the authority's `-fail` is missing | – | 2 | `…2636014030`, `…2635841176` |
| | Chilly Reception `-fail` | – | 1 | `…2662362231` |
| | Sleep Powder into a sleeping body under Misty Terrain | – | 1 | `…2661874022` |
| **announcements** (9) | Roost's `-singleturn` missing | 1 | 3 | `…2658575001`, `…2656606681`, `…2634369113`, `…2662455751` |
| | Forewarn's `-activate` missing (owner-deferred) | 2 | 1 | `…2659123487`, `…2657893729`, `…2634615536` |
| | Mortal Spin's `[from]` on the Toxic Spikes removal | – | 1 | `…2659871951` |
| | Magician on a Focus Sash: extra `-enditem [silent]` | – | 1 | `…2662767282` |
| **line order** (13) | Perish Song `perish3` slot order | – | 2 | `…2634548064`, `…2635249755` |
| | Life Dew heal against the ally's `-immune` / `-fail heal` | – | 2 | `…2634231341`, `…2635082691` |
| | end-of-turn replacement or faint order | – | 2 | `…2663429418`, `…2634665687` |
| | reactions inside one hit (Recoil / boost; White Herb / Hospitality; lock-end confusion / Leftovers) | – | 3 | `…2656439218`, `…2654135529`, `…2659688578` |
| | residual order (Sandstorm chip; Salt Cure / Infestation) | 1 | 1 | `…2660691219` (baseline), `…2654068794` |
| | two Basculegion eating Sitrus in a different order | 1 | – | `…2635337217` |
| | mega evolution after a priority Helping Hand | 1 | – | `…2660691219` (pair-redirect-priority) |
| **per-hit** (1) | Dragon Darts: extra `-miss` at the ally | – | 1 | `…2662428145` |
| | **total** | **10** | **23** | |

Gone since the last run: Cursed Body per hit (per-hit, 1350) and Poison Point on a multi-hit (line
order, 1950). Both are covered by the 6.54.0 per-arrival reaction fix.

## 5. FILES CHANGED

- Code: `tests/roster.js` (three `break` blocks with their comments). No engine code.
- Data, written by the runs: `data/game-differential{,.g1350,.g1950}.json`,
  `data/roster.{items,abilities,moves}{,.prev}.json`, `data/roster.json`, `data/engine-diff.json`,
  `data/all-mechanics-fire.json`, `data/provenance-stamp.json`, `data/published-samples.json`.
- Docs: `CHANGELOG.md` (6.54.1, 6.55.0), `docs/RUNNING-NOTES.md` (two rows), `docs/ENGINE.md` (one
  section), `docs/ABRA-whitepaper.md` (the roster sentence now cites `8a4140de3eaa`; the items `differ` 1
  and its Greninjite clause are DELETED), the five ledgers restamped by `status.js --write`, and this report.
- Not touched: the held 7.0.0 documents and `docs/_reports/2026-09-11-*`. No git command. The dumps I
  created were moved to the session scratchpad. Every process I started ended on its own; none was killed.

## OWED, NOT RUN

1. **Sucker Punch into an Encored status click** (`…2636045527`, g1350, turn 15). The authority prints
   `-fail` and ours hits. Probe first.
2. **Focus Sash against a Future Sight payout** (`…2657391947`, g1950, turn 6). The authority consumes the
   Sash and leaves 1 HP; ours faints the holder. Probe first. Items 1 and 2 are the whole board-material
   clause.
3. **`tests/test-docs-current.js` reads 35 passed, 2 failed.** Every new entry in both failing clauses is
   in a held 7.0.0 document (deck, technical docs, MODELS, SUMMARY). None is in a file this pass wrote.
   The commit will need those documents set aside, as it did for 6.53.0.
4. **Narration**: 33 games across 17 mechanisms. The largest are Roost `-singleturn` (4) and the two
   `-fail` families (3 and 2).
5. **Not claimed:** a census-pinned before/after. It would need `a1c7dcd5696b` re-run under census
   `9f251986d34e`.
6. **Seen, not touched:** `data/_diag{41,46,46b,77}-*.json` (2026-09-04, untracked, not mine) trip the
   provenance ratchet line in `status.js`. `docs/_reports/2026-09-19-overnight-log.md` is untracked and
   is not mine. Both left in place.
