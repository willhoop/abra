# Living-docs pass for CHANGELOG 5.250.0 — eight documents brought current, gate green — 2026-09-04 (MEASURE)

Gate: `node tests/test-docs-current.js` → **23 passed, 0 failed, exit 0** (was 22/1 — the one red
was clause 2, six version-headed documents stranded at 5.249.0).

Files touched, all additions except six version-header lines:

```
docs/ABRA-deck-plain-english.md  +22 -1   CRLF
docs/ABRA-technical-docs.md      +20 -1   CRLF
docs/ABRA-whitepaper.md          +22 -1   CRLF
docs/DAMAGE-STAGES.md            +10 -1   CRLF
docs/MODELS.md                   +12 -1   CRLF
docs/SUMMARY.md                  +16 -1   CRLF
docs/ENGINE.md                   +80 -0   LF  (verified: 0 CRLF, 0 bare CR after the write)
docs/MEASURE.md                  +30 -0   CRLF
```

Nothing was committed. No `<!-- GENERATED -->` block was hand-edited — both ledger inserts land
immediately **after** the closing `<!-- /GENERATED -->`, verified by reading the boundary.
`data/docs-currency-baseline.json` was not edited and the ratchet did not move.

---

## WHAT WAS WRITTEN, AND WHERE THE EMPHASIS WENT

Every figure came out of the `## [5.250.0]` block. **No number was authored in this pass** and none
was recomputed; the two verification artifacts and `data/game-differential.json` were read only to
check the citation rule (below), not to source a figure.

**The headline is stated with the sequence intact in all eight documents.** Board-material
53 → 46 of 961, protocol 154 → 141, side-selection undeclared 80 → 78 with the ratchet lowered to
match, VOID 7, census 829/829, release `7ffc58da8ef8` → `252025cfcddc`. Each document says the run
reads **77 → 61 → 50 → 53 → 46** and each says in its own register that the step to 53 was a rise
and was the fix working. The deck's wording: *"We could write the evening as '77 down to 46' and it
would be true and it would also be edited."*

**The artifact caveat is attached to every whole-game figure, in the same sentence.** The 46 lives
in `data/verification/fix-batch-M6instr-defog.json`; `data/game-differential.json` still holds 77 of
961 and protocol 168 and is what the gate prints. Six of the eight documents carry a dedicated
*WHICH ARTIFACT HOLDS WHICH WHOLE-GAME FIGURE* block; SUMMARY carries it as a three-row table.

The four method items were written as the substance of the pass rather than as footnotes:

1. **M6 closed 13 of 14 and the 14th was never M6.** Written as a statement about UNITS: the
   fourteen were causes, not games; the class fell 22 → 9; the fourteenth is a stream-position
   defect wearing a confusion line and could not have moved. Board-material fell only 7 because
   **6 of the 13 games carry a second, already-counted divergence** — stated in every document as
   *a cause count and a game count are different quantities, and one may not be subtracted from the
   other.* MODELS.md carries the generalisation it will be judged by: a model report that closes N
   mechanisms and claims N games has made this exact error.
2. **The prediction scored 4 of 5 and the miss is the headline.** Predicted 39 (39–43), came in at
   46; protocol 140–148 → 141, VOID 7, census unchanged, Defog moving the pool by exactly zero.
   The cause of the miss is named as the caveat that was written INTO the prediction and then
   under-weighted, with the running record 2-of-3, 4-of-4, a one-game protocol miss, now 4-of-5.
3. **`PIN_DIGEST` moved `ccb365985023` → `bcb38e47d94f`.** Every document says the comparison spans
   a changed INSTRUMENT as well as a changed engine, that the 13 → 0 cause count survives that, and
   that **the board-material integer is not strictly one-variable and is not presented as if it
   were.**
4. **The three `sweepField` sites are not three copies.** Tidy Up is `target: 'self'`; folding it
   into the Defog fix would have collapsed its two bags into one and left the opponent's hazards
   standing. Written as *the `facts are global` rule correctly NOT applied* — the shared fact is the
   sweep, and which side each move names is a different question — with the note that both
   memberships are derived on every run so a new carrier fails by name. `docs/ENGINE.md` explicitly
   supersedes the "three copies of one selection" line in the 5.249.0 section below it rather than
   editing that dated block.

**The destroyed declaration rows are carried unsoftened in six documents**, including the deck and
SUMMARY: four uncommitted rows destroyed by a `git checkout --`, two restored verbatim, two
reconstructions labelled as such in the file and each quoting the original answer from the expired
key's own text, the code unmoved and verified by digest — and, stated plainly, that `git checkout --`
on a file another session has edited and not committed is **unrecoverable, because git holds nothing
to restore from.**

**Register coverage is given as the four numbers, with the 163 named as the gap:** of 219 open rows,
13 admitted markers, 43 `INSTRUMENT OWED`, 0 refused, **163 carrying neither, 43 of the 74 rows that
assert breakage among them.** Each document also says the declared half improved while the
undeclared half grew, because reporting only the first would be the flattering half.

**Still-open items are named, not implied**, in the whitepaper, the technical docs, the deck,
`ENGINE.md` and `MEASURE.md`: `tests/staged_board.js` 25 of 25 board-identical and exiting 1 on a
pre-existing red whose anchor is absent at `HEAD`; two premature-close candidates filed with evidence
and not reopened; four closures resting on markers never executed; M2 fenced rather than split; the
surviving ordering cause with no probe; the six second-cause games unattributed;
`node engine/status.js --write` not run.

---

## THE CITATION RULE WAS MEASURED, NOT ASSUMED

`docs_scan.js`'s `citationsIn` matches **top-level** `data/*.json` only — a nested path such as
`data/verification/fix-batch-M6instr-defog.json` is not a citation, so blocks naming it are judged by
the untraceable census instead. Both rules were checked per document before and after each write:

- **3b(b) cited-artifact mismatches: 66, unchanged.** Every block naming `data/game-differential.json`
  was checked against that artifact's number set first. Measured: it contains 46, 53, 61, 50, 77,
  141, 154, 168, 961, 78, 80, 22, 9, 13, 14, 219, 43, 74 and 7 — and does **not** contain 829 or 163,
  so those two figures were deliberately kept out of every block that names it.
- **3b(c) untraceable census: 35 across 6 documents, unchanged.** Every figure written is in the
  `## [5.250.0]` block, and a figure recorded in `CHANGELOG.md` is traceable by construction.
- Clause 1 (retracted figures), 1b, 2b, 3, 3c and 4 all unchanged.

---

## WHAT I DID NOT TOUCH

`docs/ROADMAP.md` shows a 2-line uncommitted change in the working tree. **It is not mine** — a
register-triage agent is live — and I did not open it for writing. `docs/OPS.md` and `docs/SEARCH.md`
were outside the brief and were not touched. Nothing under `engine/` was run or edited. No game was
played and no heavy run was started.

---

# OWED

- **`node engine/status.js --write` has still not been run**, so the `<!-- GENERATED -->` blocks at
  the top of `docs/ENGINE.md` and `docs/MEASURE.md` remain stamped `2026-09-04 06:54` and are one
  pass behind the engine. This pass deliberately did not hand-edit them. Whoever runs it next should
  expect the withheld/FAIL rows against the roster, `engine-diff.json`, `all-mechanics-fire.json` and
  `data/game-differential.json` that a new release always produces.
- **`data/game-differential.json` is still not republished**, so the published clause reads 77 of 961
  against a current measurement of 46. Every document naming 46 names the verification artifact in
  the same sentence, which is a mitigation and not a fix.
- **`CHANGELOG.md` was not edited.** This pass added no version and authored no number; if a
  documentation-currency line is wanted under 5.250.0, it is not there.
- **The PDFs were not rebuilt.** `docs/ABRA-whitepaper.pdf`, `docs/ABRA-deck-plain-english.pdf`,
  `docs/ABRA-technical-docs.pdf` and `docs/SUMMARY.pdf` are one version behind their `.md`.
- **Nothing was committed or pushed**, per the brief. Eight documents are current on disk only.
- **The two reconstructed declaration rows still need a re-read by their author.** That debt is now
  recorded in the whitepaper, the deck, SUMMARY, `ENGINE.md` and `MEASURE.md`; it is not discharged
  by being written down.
