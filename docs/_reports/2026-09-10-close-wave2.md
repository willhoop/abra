# CLOSE PASS FOR 5.277.0, WAVE 2 — THE CENSUS ROW THAT PINNED A BUG IS RE-AIMED (835/835, NO ENGINE CHANGE), EVERY GATE ARTIFACT SITS ON `7d66b526659e`, 8 OF 9 CLAUSES PASS, AND THE DOCS GATE STAYS 34/1 FOR A REASON THAT IS NOT THE CENSUS

MEASURE. 2026-09-09 23:56 UTC → 2026-09-10 00:46 UTC. The only agent running; nothing else wrote the tree.
Historical by construction; superseded by the register rows it feeds. **No commit was made** — the coordinator
commits after verifying. Every heavy step ran through `cmd /c tools\lownode.cmd`; each was judged by output size
and the artifact's `generated` stamp. Logs are in the session scratchpad (`census-run1.log`, `census-run2.log`,
`amf-step2.log`, `status-step3.log`, `run-all.log`, `gate-fail-silent.log`, `docs-current-*.log`, `no-silent*.log`).

---

## 1. THE CENSUS ROW — `tests/test-mechanics.js` `move/trickSwapsItems`

**The fixture change.** Batch Y §0 was right: the third arm staged a Gengarite on a Milotic and asserted Trick
"moves nothing" — the engine's old coarse guard (`TAGS.has('item', …, 'megaStone')` on either body → refuse),
which batch Y replaced with the authority's rule (`onTakeItem` refuses only the matching mega's species). The probe
went MISSING on a CORRECT engine.

Applied (CRLF preserved; the file's pre-existing 146 uncommitted lines — the five screen/Compound Eyes probes —
untouched):

- `stage(myItem, foeItem, click, foeSpecies = 'milotic')` — a species parameter on the helper.
- The holder is DERIVED: `require(D('data','tags.json')).items.gengarite.params.megaStone.into` → its one key,
  `Gengar`. Never typed; the probe throws if the artifact carries no `into`.
- Third arm: the stone on that body — `stone.theirs === 'gengarite' && stone.mine === 'quickclaw'` (refused).
- A fourth arm kept as the CONTROL the old probe lacked: the same stone on a Milotic SWAPS —
  `foreign.theirs === 'quickclaw' && foreign.mine === 'gengarite'`. This is the arm that says the coarse guard is gone.

**Census, writing mode.** The file's header says it writes on every run and refuses only under a `DELIBERATE_BREAK`
MEDFAILS flag; none was set.

| run | command | result | `generated` |
|---|---|---|---|
| 1 | `tools\lownode.cmd tests\test-mechanics.js` | **835 probed, 835 live, 0 missing**, `run_ok` true, exit 0, 864 lines | `2026-09-09T23:59:14.939Z` — 46 s short of the rollover |
| 2 | same, after 00:00 UTC | **835 probed, 835 live, 0 missing**, `run_ok` true, exit 0, 864 lines | **`2026-09-10T00:00:33.577Z`** |

The row in the artifact:

```
tag trickSwapsItems  live true  armed true  directCall false
detail: Trick: user quickclaw -> (none), target (none) -> quickclaw; Corrosive Gas leaves the target (none);
        Trick at a Gengarite on a Gengar (derived from tags.json megaStone.into) moves nothing (quickclaw / gengarite);
        the same stone on a Milotic swaps (gengarite / quickclaw)
```

Ratchet floors unchanged (`unarmed` 0, `directCall` 1). Step-1 bytes copied aside before the suite:
sha256 `bfdaf0d5283a`.

---

## 2. `all_mechanics_fire.js` ON `7d66b526659e` WITH THE FRESH CENSUS PINNED

Pin: `data/verification/census-pin-bfdaf0d5283a.json` — a byte copy of the step-1 census (sha256 prefix is the
name; batch Y's `c716f46ab0a7` verified against the same scheme). Command:

```
SHOWDOWN_PATH=C:/Users/willj/Projects/Pokemon/pokemon-showdown
tools\lownode.cmd engine\all_mechanics_fire.js --kind all --write --release 7d66b526659e --census data/verification/census-pin-bfdaf0d5283a.json
```

Exit 0, 196 lines, **1313 games played, 0 threw, 0 sheets could not be assembled**. Artifact `generated`
`2026-09-10T00:01:54.770Z`, `engine_release` = `release` = **`7d66b526659e`**, `engine_release_cut`
`2026-09-09T23:38:55Z`, `showdown_commit` `20ad99ffc9a5…` = `PINNED_COMMIT`.

| field | batch Y | this run |
|---|---|---|
| abilities exist / fired / diverged | 316 / 104 / 1 | **316 / 104 / 1** |
| items exist / fired / diverged | 148 / 64 / 0 | **148 / 64 / 0** |
| moves resolved / STATE / ANNOUNCEMENT-ONLY / NO-DIVERGENCE | 495 / 4 / 4 / 488 | **495 / 4 / 4 / 488** |
| games / threw / unassembled | 1313 / 0 / 0 | **1313 / 0 / 0** |

**One thing the brief expected that the instrument does not do: the artifact stamps NO census digest.** `--census`
is honoured — `all_mechanics_fire.js` requires `game_differential.js`, whose module-level
`CENSUS_PIN = flag('--census', null)` reads the shared argv, and `steering.js` resolves the pin; the run log says
`census-coverage-seeking/v1 PINNED to data/verification/census-pin-bfdaf0d5283a.json … identical to the live census;
it steers 764 entity sets; 71 census rows steer nothing`. But GD's `census_pinned_to` (`game_differential.js:7249`,
`:7293`) is written to GD's own artifact, and `data/all-mechanics-fire.json` carries no census key at all (the only
"census" in its stamps is `source_digests['data/rollout-switch-census.json']`). So the pin is verifiable from the
log and not from the artifact. Recorded in `docs/MEASURE.md` as owed; not fixed here (the brief said confirm, not
build).

Also in the log, benign but oddly phrased: `EXEMPTION RECONCILED: 75 of 75 out-of-scope rows carry a FIRED verdict
in data/roster.items.json — that artifact was cut at release 7d66b526659e on 2026-09-09 — the coverage is
per-entity and it is NOT this run's release`. The roster artifact IS on this run's release; the sentence is
generic. Noted, not chased.

---

## 3. `engine/status.js --write`

`tools\lownode.cmd engine\status.js --write` — exit 0, 816 lines, `data/provenance-stamp.json` rewritten by it
(mtime 20:04 local); `data/quarantine-stamp.json` NOT touched by it (mtime stayed 18:29 local — see §5). Ledgers
restamped: `docs/ENGINE.md`, `MEASURE.md`, `OPS.md`, `ROADMAP.md`, `RUNNING-NOTES.md`, `SEARCH.md`, `WEB.md`.

The gate clause lines, verbatim (first line of each; the wrapped continuation is in `status-step3.log`):

```
QUARANTINE — MEDICHAM IS NOT CORRECT, SO EVERY FIGURE DOWNSTREAM OF IT IS WITHHELD
  64 artifacts are downstream and are not printed below. The gate is computed, not set:
    PASS  game differential              clean at BOTH corners of the damage roll: midpoint 0 of 6000, top 0/6000,
                                         bottom 0/6000, idx01 0/6000, idx02 0/6000, idx03 0/6000, idx04 0/6000, idx05
    PASS  deliberate roster / items      clean: 142 of 148 tested
                                         READ FROM roster.items.json, 23 min old
    PASS  deliberate roster / abilities  clean: 139 of 201 tested. 14 row(s) count in NEITHER column — the control arm
                                         is itself a live ability: aftermath, angerpoint, battlebond, damp, justified,
    PASS  deliberate roster / moves      clean: 487 of 498 tested
                                         READ FROM roster.moves.json, 23 min old
    PASS  coverage / every used mechanic is measured by something clean: all 412 moves above 25 clicks are measured by
                                                                  the roster or the census
    PASS  whole-game differential / BOARD-MATERIAL — games whose boards part BOARD-MATERIAL: 0 of 961 games. Every
                                                                             compared turn boundary in every game
    FAIL  whole-game differential / NARRATION — protocol divergence with no board effect NARRATION-ONLY: 9 of 961 =
          0.9% of games diverge in NARRATION and never part a board, across 10 cause(s) (10 narration-only raw,
          less 1 declared and 0 cleared on decision impact). Mode A pins every die on both sides, so each one is a
          RULE they disagree about, not noise. … One game of 961, turn 11. CLOSETED BY Will 2026-08-28 (ROADMAP #440)
    PASS  mechanics / each one staged and compared against showdown every mechanic anybody plays agrees with the
                                                                    authority: 5 diverge, 1 are declared, 4 are below
    PASS  no open, known engine defect   clean: no open row names an instrument that is RED — no open defect is backed
                                         by a failing measurement (123 verdict(s) read) 8 open row(s) declare NOT A
```

**8 of 9 PASS.** BOARD-MATERIAL **0 of 961, 0 void** (`state.games` 961, `state.games_board_never_diverged` 961,
`state.games_void_excluded` 0, `mid_void.void_games` 0). NARRATION **9 of 961**, 10 causes raw less the one
closeted (#440, the Perish `|upkeep|` drain), `mid_void.diverged_among_usable` 10. Both whole-game clauses read
`data/game-differential.json` on **`7d66b526659e`** (generated 2026-09-09T23:50:36Z — batch Y's clean re-run).
Census **835/835**. Provenance line in `docs/MEASURE.md`'s block: **`177 unsafe, 2 void (declared), 43 possibly
stale, 33 ok, 0 missing`** — derived, not NOT DERIVED. PROVENANCE section: 2 figures WITHHELD (interaction matrix,
release ladder — both UNSAFE, pre-existing). **No clause reads MEASURED AGAINST A DIFFERENT ENGINE and none reads
WITHHELD.**

Two cosmetic observations, recorded and not fixed: (1) the ENGINE block prints `835/835 probed mechanics live, 0
missing (census 2026-09-09 20:00)` under a legend that says `Times are UTC` — `20:00` is the LOCAL rendering of the
`00:00:33Z` run; (2) the first line of the log is `FEATURE SEMANTICS CHECK FAILED — data/policy-weights.json`, the
standing refit gate, unchanged.

---

## 4. RECORD

Every figure below was re-read from its artifact before it was written (§3 above and
`node -e` against `data/game-differential.json`, `data/all-mechanics-fire.json`, `data/mechanics-census.json`).

- **`docs/RUNNING-NOTES.md`** — two rows inserted above the existing `[5.277.0]` rows, newest first:
  - `## [5.277.0] — 2026-09-10 — close pass …` (MEASURE): the fixture re-aim, the pinned AMF re-run, status,
    run-all. **Basis.** unchanged. **Supersedes.** ~~835 probed / 834 live / 1 missing~~ → 835 / 835 / 0.
  - `## [5.277.0] — 2026-09-09 — narration batch Y …` (ENGINE): the four causes, the pins verbatim, `state.games`
    961 / `state.games_board_never_diverged` 961 / `state.games_void_excluded` 0 / `mid_void.void_games` 0 /
    `mid_void.diverged_among_usable` 10 → BOARD-MATERIAL 0 of 961, NARRATION 9 of 961 after the one closeted row,
    10 causes. **Supersedes.** ~~NARRATION 13 of 961, 14 causes~~ → 9 of 961, 10 causes; release
    ~~`b0f5c159c46e`~~ → `7d66b526659e`. The batch's census figure is written STRUCK (`~~835 probed / 834 live /
    1 missing~~`) so the citation rule cannot read it as a claim about today's artifact.
  - The pre-existing row citing census 830 (now line 270) was NOT edited.
- **`CHANGELOG.md` `## [5.277.0]`** — Changed: gate artifacts re-measured on `7d66b526659e` (the fields above; the
  AMF counts; `gate_fail_and_silent` exit 0; `test-pinch-family` / `test-counter-init`); ROADMAP #559 closed, #561
  filed+closed, #562 filed. Fixed: batch Y's four causes (one bullet, each shown red on `b0f5c159c46e` first); the
  `trickSwapsItems` fixture. The existing Notes bullets were left as written.
- **`docs/ROADMAP.md`** — **#559 CLOSED 2026-09-10**: the card (`pair-protect-bust 2654619049 vs 2654751965`, t4)
  is batch Y §2b — a Metagross-Mega holding its own Metagrossite, the receiver-side `TakeItem` refusal; now
  `-fail` + `[still]` via `stoneRefusesBody`, probe `tests/probe_trick_refusal.js` red then green.
  `engine/gate_fail_and_silent.js` on the current artifact: **CLEAN, exit 0** (`[0 clean, 1 live at or under the
  pin, 2 cannot answer, 3 REGRESSION]`). Recorded beside it, not papered: the gate keys its sample on the census
  digest and prints `this run: census c716f46ab0a7 / pool 0d103fb9fa87 / 961 games -> A DIFFERENT SAMPLE, so no
  movement is attributable` against its seeded `098de5770623` — same pool, same 961 games, empirical steering
  (census credited only). The VERDICT is what the row's VERIFIED BY asks for; the MOVEMENT 1 → 0 is attributed by
  the card's seeds leaving `first_divergences`, not by the gate. **#561** filed and closed (the fixture; owner
  MEASURE). **#562** filed, open (Future Sight's payout draws no `acc` die where the authority draws one on a
  printed 100 — die-count, not board; owner ENGINE). `tests/test-roadmap-register.js` 3/0 after.
- **`docs/MEASURE.md`** — one dated section outside the generated block (this pass, and the docs-gate finding in §6).
- **`docs/ENGINE.md`** — nothing added. Batch Y already wrote its section (line 170, "the hand list") and its report
  proposes no further ENGINE.md row.

---

## 5. `tests/run-all.js`

`tools\lownode.cmd tests\run-all.js`, foreground → moved to background by the harness at 600 s; started
00:13:06 UTC, finished 00:43:20 UTC (30 min, not the 10–12 expected — `provenance.js` alone took 84 s and
`artifact-rerunnable` 39 s). 657 lines. `RUN-ALL — 177 checks discovered (150 in tests/, 27 engine gates)`.

**Summary line: `156 passed, 14 failed, 7 waived, 0 skipped`** (wrapper exit 1).

The 14 red, by name, with the one-line reason from the log:

| check | reason |
|---|---|
| `tests/test-artifact-rerunnable.js` | NEW: `roster.spine.json` — release `b730e44f3314` has been MODIFIED since it was cut (1 of 5) |
| `tests/test-board-browser.js` | 13/1 — `switchDiesFirst` worst gap 0.500000 (and `killIsRoll` 0.486, `dmgFrac` 0.310) |
| `tests/test-claim-truth.js` | `engine/smogon_coverage.js -> data/smogon-coverage-2026-08.json`: "it is not so" |
| `tests/test-docs-current.js` | 34/1 — see §6 |
| `tests/test-fixture-legality.js` | 2 FAILED — `[PAIRING] Snorlax can't learn Double Team` (`probe_endturn_clock_order.js`), `Snorlax can't learn Whirlwind` (`test-encore-fail-silent.js`), `Steelix can't learn Explosion` (`probe_selfdestruct_winner.js`) |
| `tests/test-mutation-coverage.js` | 5/1 — coverage artifact measured `6fb9ebd3b704`, engine is `7d66b526659e` (notice says deliberately not a failure; another clause is red) |
| `tests/test-no-silent-failure.js` | 1 NEW value-manufacturing catch, `tests/probe_delayed_hit_immune.js:259` — **fixed in §6, exit 0 now** |
| `tests/test-policy-promote.js` | 15/2 — staged 7 of 12 artifacts; an unproduced artifact stopped the store being staged |
| `tests/test-quality.js` | 31/1 — clean share 34.7% now vs 28.0% recorded (drift 6.7 pts, tolerance 3); store 67,384 → 92,431 |
| `engine/selftest.js` | 24/1 — 4 files read the ladder store with neither a clean filter nor RAW-STORE-OK (`joint_click_census.js`, `medicham2-browser.js`, `mega_sets_from_sheets.js`, …) |
| `engine/conformance.js` | 4 convention regressions — no opening comment: `engine/durable-ingest.js`, `engine/next_regulation_ingest.js`, `tests/test-next-regulation.js`, `tests/test-workflow-paths.js` |
| `engine/provenance.js` | UNSAFE artifacts (177); `exploitability-holdout.json` revoked on shape |
| `engine/sanity_check.py` | 94/2 |
| `engine/em_validation.js` | the amplified regime's censoring bias did not exceed its own noise floor |

Also printed and not counted: `FAIL — UNACCOUNTED-FOR CHECK. 164 file(s)` (the `tests/probe_*.js` population and
`engine/docs_scan.js`), and 38 PENDING-WIRE with reasons. The 7 WAIVED are Will's of 2026-09-09 by name:
`test-forced-switch`, `test-site-data-fresh`, `test-site-sync`, `test-team-preview-race`,
`test-web-quarantine-loaders`, `test-web-status`, `test-wiring`.

None of the 14 is new to this wave except `test-no-silent-failure` (batch Y's untracked probe; fixed) — I did not
diff the list against a previous run, so "pre-existing" is not claimed for the other 13 beyond what their own
reasons say.

**Reverted (the suite's incidental rewrites, none of which was modified before the suite ran — `git status` on the
list was empty at 00:13):** `git checkout --` on `data/damage-validation.json`, `data/forme-assert.json`,
`data/game-diff.json`, `data/job-costs.jsonl`, **`data/quarantine-stamp.json`** (rewritten by the suite at 20:42
local; my step-3 status did NOT write it — mtime 18:29 before and after step 3 — so it is a suite rewrite and was
reverted), `data/regulation-usage.json`, `data/rulebook-collision.json`, `data/switch-back-renamed.json`,
`data/tag-consumption.json`, `data/verification/engine-diff.suite.json`. `data/open-work.json` was on the list and
was NOT modified; left alone.

**Restored:** `data/mechanics-census.json` — the suite rewrote it (`generated 2026-09-10T00:21:25Z`, same
835/835/0, sha `b8f589deb423`); my step-1 bytes copied back, **sha `bfdaf0d5283a`, equal to the pin file**.

**Kept:** `data/provenance-stamp.json` (written by my step-3 status). **Left and reported, not on the brief's
list:** `data/conformance.json` — modified by `engine/conformance.js` during the suite (`generated
2026-09-10T00:37:33Z`, +17/−2 lines); not reverted because the brief said "nothing else". The coordinator decides.

---

## 6. GATES

| gate | result |
|---|---|
| `node tests/test-docs-current.js` | **34 passed, 1 failed** — before this pass, after my rows, and after the revert. NOT the 35/0 expected; see below |
| `node tests/test-docs-quarantine.js` | exit 0, all checks passed |
| `node tests/test-roadmap-register.js` | exit 0, 3/0 (528 items) |
| `node tests/test-no-silent-failure.js` | exit 1 → **exit 0**: the one NEW catch (`tests/probe_delayed_hit_immune.js:259`, `ef292e653f8b`, `addr = null;`) was in batch Y's untracked probe. Read: it swallowed a thrown `G.midAddresses()` into the same `null` that means "no reader", so a broken reader and an absent one were one fact. Made to speak — `addr` unchanged, `console.error('probe_delayed_hit_immune: midAddresses() threw -- ' + e.message)` with a comment. `node --check` parses; `NEW since the baseline 0` |
| `git status --short \| wc -l` | **49** before this report file (50 with it) |

**Why the docs gate is 34/1 and not 35/0.** The brief expected the red to be the RUNNING-NOTES row citing census
830 and to clear when the CENSUS was regenerated on a later date. The actual accusations, identical before and after
this pass (`baseline 45, now 47`):

```
docs/RUNNING-NOTES.md:110  14   not in data/game-differential.json  (field mid_void.diverged_among_usable reads 10)
docs/RUNNING-NOTES.md:270  830  not in data/game-differential.json
```

(:96 and :256 before my two rows shifted them.) Both are judged against **`data/game-differential.json`**, not the
census: the first is the 5.276.0 row's `mid_void.diverged_among_usable` 14, which batch Y's re-run moved to 10; the
second is the 5.270.0 row's "census 830 rows" sitting in a sentence that also cites the differential. The rule
(`engine/docs_scan.js citationMismatches`) files a figure as PREDATING — reported, not gated — only when every cited
artifact's `generated` DATE is strictly later than the row's date; both rows are dated 2026-09-09 and the
differential is `2026-09-09T23:50:36Z`, so they are judged. The census date is irrelevant to them. They clear when
`data/game-differential.json` is regenerated on a later date. No differential was run in this pass because none was
asked for and it rewrites a gate artifact; the rows were not edited because the page is a log. My two new rows
introduced **zero** new accusations.

Untracked at close (15): `data/_narrationY-after-dump.json`, `data/verification/_prediction-2026-09-09-batch-Y.json`,
`data/verification/census-pin-bfdaf0d5283a.json` (mine), `data/verification/census-pin-c716f46ab0a7.json`,
`docs/_reports/2026-09-09-{fix-rows-wave2,narration-batch-Y,raw-shard-cap,roster-scope-by-carrier,screen-probes}.md`,
`docs/_reports/2026-09-10-close-wave2.md` (this), `tests/probe_{damaginghit_walk,delayed_hit_immune,replacement_tie,screens_infiltrator,trick_refusal}.js`.
Nothing deleted.

---

## OWED, NOT RUN

- **The commit** — sole publisher's job. 35 tracked files modified, 15 untracked (list above);
  `data/releases/7d66b526659e` is gitignored and batch Y noted the previous wave tracked its release with `git add -f`.
- **`data/game-differential.json` regenerated on a date after 2026-09-09**, same pins as batch Y
  (`--steering empirical --release 7d66b526659e --arm middle --end-state --state --games 1200 --team-store
  data/team-pool-frozen --turns 50 --census <pin>`), which is what clears `tests/test-docs-current.js` to 35/0.
  Expect identical fields but `generated`; batch Y showed two such runs identical field for field.
- **A census digest stamp in `data/all-mechanics-fire.json`** — the pin is honoured and only the log proves it.
- **`data/conformance.json`** — suite rewrite, not on the revert list; revert or keep is the coordinator's call.
- **The 13 other reds** in §5, each with its reason; none is this pass's to fix and none is filed as "known".
- The standing priority — leaf calibration — is unchanged and unserved by this pass; it is behind the gate.
