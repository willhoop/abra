# Tailwind side-clock ordering — shelved 2026-08-24

- **The row (#355) now reads SHELVED ON WILL'S RULING**, quoted and dated: *"tailwind coming out in the wrong order doesnt matter, put it into the closet with that note and move on."* It carries his reasoning, checked rather than accepted — Tailwind resolves at residual **order 26** (weather 1, poison 9, burn 10, Perish Song 24 are all behind it, so no damage remains to land), the two Tailwind handlers are **adjacent** so nothing runs between them that could read the difference, the handler list is speed-sorted **once** before the walk (`sim/battle.ts:507`), and the speed consequence is not applied until the next turn's choices on both sides. Which Tailwind ends first cannot affect a game.
- **It is DEFERRED, not DECLARED.** It stays a real, deterministic narration divergence that we chose not to fix. It was NOT added to the declared kinds in `engine/quarantine.js` — that file was not touched — so it is still counted UNDECLARED, still red, and still named on every run. A deferral that could open a gate is the failure this whole distinction exists to stop.
- **No gate figure moved.** Before and after the edit the row is `closed:false, brokenClaim:false`; the register reads 407 rows / 234 open / 65 asserting breakage in both states, and `status.js --write` changed nothing in the four ledgers except their stamp line. It is narration, so it already sat outside the board-material bar Will set on 2026-08-22.
- **Correction 1 — a coin does NOT decide it.** The row's earlier derivation ended *"A COIN DECIDES ON THE AUTHORITY"*. With `prng.shuffle` held at the identity — which `game_differential.js` installs in **every** arm — the authority still reverses the pair on the right board. The reversal is deterministic, so the pair is comparable and IMPOSSIBLE TO COMPARE was correctly refused as a filing for it.
- **Correction 2 — the remaining cause is the handler LIST, not the sort.** The speed sort is now correct at all four sites (#422). What our walk cannot reproduce is the authority's full residual handler list: its selection sort **swaps** rather than shifts, so sorting the front displaces the tail, and every held item adds a handler that participates in that displacement. The same cause covers two screens expiring on both sides in one turn.
- **Gates run:** `tests/test-roadmap-register.js` 3/3 green, `tests/test-register-reality-readonly.js` 10/10 green, `engine/status.js --write` exit 0. Nothing was played, nothing was committed, no CHANGELOG entry written.
