# 2026-09-09 — record-keeping pass for 5.275.0: the three fix agents' rows landed once each

MEASURE. Dated findings record, not state; superseded by the rows it landed. Nothing committed; no
game played; `tests/run-all.js` not run.

## What landed, where

| file | what | placement |
|---|---|---|
| `docs/RUNNING-NOTES.md` | four `## [5.275.0]` rows — retractions, store recovery, Smogon cron, the two review documents | newest-first, above the `[Unreleased]` batch X row (lines 72-98). Bullets written as single lines so the retraction registry pairs each `~~…~~` with its `retracted` on one line. The recovery row's `**Supersedes.**` names the 2026-09-06 sharding row's "Supersedes. Nothing." as the thing it supersedes. The retraction row keeps the report's wording exactly: `35.9%`, `71.6%`, `1,205` register; `52.4%` and `1280 / 1267` are written in CHANGELOG only, because struck through in a scanned document they would accuse `docs/MEASURE.md:6271-6272, 6342` and `docs/ENGINE.md:35712`. |
| `CHANGELOG.md` | `## [5.275.0] — 2026-09-09`, Added / Fixed / Removed / Notes | top of file, above 5.274.0. Notes state why it is a MINOR: 22 numeric values withdrawn or replaced across 21 locations in 7 documents plus the corpus count, under an unchanged basis; nothing re-ran a generator. |
| `docs/ROADMAP.md` | `#550` — the store drop, the two guards that missed it, the `HEAD~1` guard that now exists, status cell `open — …` | after `#549` (line 1444); next free id was 550 (`#545`-`#549` are the highest). Three-cell row like its neighbours (the header says two columns; the rows carry a status cell). |
| `docs/OPS.md` | dated section: the store drop and recovery, the cron, what OPS owes | above the 5.246.0 section, outside the generated block |
| `docs/MEASURE.md` | dated section: the retractions (pointing at the §5e correction note the retraction agent already wrote, not repeating it) and the docs-gate blind spot at `engine/docs_scan.js:626` (QUALIFIED skips a whole paragraph) and `:496` (set-membership) — FILED, NOT FIXED | above the 2026-09-08 CRLF section, outside the generated block |
| `docs/ENGINE.md` | dated section: the three `low-identity` void games are the three board-parted games, `mid_void.void_game_tags[].board_parted_at_turn` 4 / 6 / 3 on release `b730e44f3314`, seeds named — FILED, NOT FIXED | above BATCH X, outside the generated block |
| `docs/SEARCH.md`, `docs/WEB.md`, `data/provenance-stamp.json` | touched only by `status.js --write` / `provenance.js`. The stamp file's CONTENT is unchanged (`git diff --ignore-cr-at-eol` is empty); provenance.js rewrote it CRLF (0 → 1,015 CR bytes), which `core.autocrlf` normalises back to the identical blob on commit. Left as is. | — |

## The version stamp

`grep -rn "5\.274\.0"` over `*.js *.json *.md *.html *.py *.yml`, excluding `node_modules`, CHANGELOG,
`_reports` and RUNNING-NOTES, finds **two** hits and **neither is a primary-artifact stamp**:

- `data/docs-currency-baseline.json:5` `"changelog_top_at_baseline": "5.274.0"` — a provenance key the
  test writes itself on a ratchet move (it did not move this run: "no ratchet movement"). Not hand-edited.
- `docs/ARCHITECTURE-REVIEW-2026-09-09.md:418` — a dated review saying what the CHANGELOG top WAS when
  it was written. A dated record is not edited.

There is no `package.json` version, no `@version` line, and `../portfolio/build/check_projects.py` does
not exist. The living documents carry `Version 5.266.0` (their last fold-in) and the derived version
rule (`tests/test-docs-current.js` clause 2) checks them against the last MAJOR floor (5.0.0), not against
the top; clause 5d checks the rows' `[5.275.0]` against the CHANGELOG top. So **nothing was bumped**, and
the gate is green on that basis: `CHANGELOG has a top version (5.275.0)`, `7 of 55 row(s) matched a
CHANGELOG release; top 5.275.0 is a minor bump`.

## Gate and restamp

- `node tests/test-docs-current.js`: **33 passed, 0 failed** before the restamp and again after it.
  Backlog owed to the next major **55 of 100** (was 51; the four new rows). No ratchet movement.
- `cmd /c tools\lownode.cmd engine\status.js --write`, first run: exit 0, **860 lines**, all five stamps
  moved 08:09 → 13:31 — **but `engine/provenance.js` died at the default heap** (`FATAL ERROR:
  Ineffective mark-compacts near heap limit`, ~2,040 MB) and every ledger's generated block read
  `provenance: NOT DERIVED`, where the 08:09 block had read `182 unsafe, 2 void, 40 possibly stale, 31 ok`.
  That hole was NEW to this restamp, so it was not left. `provenance.js:1053` reads `games.bo3.jsonl`
  whole as UTF-8 and splits it into lines; the recovery took that file from 25,522 to 32,801 rows
  (298 MB), which is the plausible cause.
- Second run with `NODE_OPTIONS=--max-old-space-size=4096` (one process): exit 0, **837 lines**, 3 min
  1 s, no FATAL, stamps 13:36, **`provenance: 182 unsafe, 2 void (declared), 35 possibly stale, 36 ok,
  0 missing`** — five artifacts moved from possibly-stale to ok against the 08:09 block. The generated
  blocks in the tree are from this run.
- The OPS block reads `store: 81269 games` while `data/games.ladder.jsonl` holds 92,379 rows (and bo3
  32,801, both written 13:05). The line is read from `data/live.js`, a generated artifact stamped before
  the recovery; the gap is exactly the 11,110 recovered games. Not regenerated here.

## Not as the brief described

- `docs/ARCHITECTURE-REVIEW-2026-09-09.pdf` does not exist; only the thesis review has a PDF. The rows
  and CHANGELOG say so (the `.md` alone, the thesis review "with its `.pdf`"). The architecture review
  `.md` was still growing while this pass ran (81,885 B at 13:21, 100,007 B at 13:24).
- The retraction agent's note in `docs/MEASURE.md` is an inline correction at §5e (line ~5338), not a
  dated section; my MEASURE section points at it rather than repeating it.

## OWED, NOT RUN

- **Commit, by name** — 21 untracked files and 21 modified; never `-A`/`-u`. The five shards, the
  observed twin, the two reviews and the ten reports must be added explicitly; the pre-commit hook
  will require the notes rows, which are now present.
- **`engine/provenance.js` cannot run inside `status.js --write` at the default heap on the recovered
  store.** Either `status.js` spawns it with `--max-old-space-size=4096` or `provenance.js:1050-1053`
  streams the bo3/ots stores instead of `readFileSync(...).split('\n')`. Until one of those lands, a
  plain `node engine/status.js --write` silently restamps every ledger with `provenance: NOT DERIVED`
  and exits 0. No register row written for this — MEASURE, next pass.
- **`data/live.js` is stale against the recovered store** (81,269 vs 92,379 ladder rows). OPS regenerates
  it on the next publish; the OPS block will read the old number until then.
- **Two filed defects have ledger notes and no register row**: the three void games (ENGINE) and the
  docs-gate blind spot at `docs_scan.js:626` / `:496` (MEASURE). `open_work.js` will not print them until
  they have one.
- The hand-typed `RETRACTED` entries for `0.7048`, `52.4%`, `0.687`, `1280 / 1267` proposed in
  `docs/_reports/2026-09-09-fix-retractions.md` §D — not written (no test edits in this pass).
- `git add data/smogon-priors.observed.json` before the next cron firing (2026-09-11 07:00 UTC).
