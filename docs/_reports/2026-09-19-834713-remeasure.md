# The gate on `834713ccb303`, the three lattices, and the held-out 12,000-game draw — 2026-09-19, ENGINE (measure only)

This is a findings record, not a living document. It is not current state and is not cited as such.
`node engine/status.js` and `node engine/quarantine.js` hold current state. **No engine, instrument or
test byte changed in this pass; nothing was fixed and no release was cut.**

---

## 0. VERDICT

- **The gate reads `GATE: CLOSED — 3 of 10 GATING clauses fail` on `834713ccb303`.** It read
  `OPEN, 10 of 10` on `18773c22878f` one pass ago. **Nothing about the engine got worse — the
  comparator stopped being blind.** 6.72.0 retired the `ability-announcement` equivalence, so
  `|-ability|` lines are compared for the first time in 44 days, and two mechanisms that were
  invisible are now counted in three separate clauses.
- **Board-material is ZERO on all three gate lattices**, as before: **0 of 961**, **0 of 1069**,
  **0 of 1497** at `--games` 1200 / 1350 / 1950. That clause PASSES.
- **The narration clause is RED on all three and it is essentially ONE mechanism.** Undeclared
  narration **23 / 30 / 33**, and of the 89 protocol divergences in the three dumps, **75 are Cloud
  Nine's arrival announcement**, **11 are Super Sweet Syrup announced with a `|boost` field the
  authority does not write**, and **3 are the declared Supreme Overlord `fallenundefined`**. There is
  no third mechanism on any gate lattice.
- **The mechanics clause is RED for the same reason** — 14 of 15 diverging staged mechanics are
  played and uncleared, and every one of the 15 is Cloud Nine, Super Sweet Syrup, or a row staged on
  a body that happens to carry Cloud Nine. **No staged mechanic diverges for any other cause.**
- **The roster / abilities clause is RED for two INSTRUMENT reasons, not an engine one.** 196 of 200
  MATCH, 0 DIFFER, 0 DID-NOT-FIRE, 0 COULD-NOT-STAGE. It fails because (a) the `ability/residual` red
  plant's anchor is DEAD — 6.73.0 inserted a line inside the two-line anchor string — and (b) two of
  the three `ANNOUNCEMENT-ONLY` receipts are refused, because `anticipationSilentRestored` and
  `friskSilentRestored` have never been added to `DELIBERATE_BREAK` in `tests/test-mechanics.js`.
- **The held-out draw improved by more than half: 34 board partings → 15**, on the SAME lattice
  (pool `e398641bda45`, same seeds). Matched game by game: **20 of the prior 34 closed, 1 joined, 14
  stand.**
- **The corner arms at `--games 1200` part NO boards** — 0 and 0, against 0 and 1 last pass.
- Damage differential **0 of 6,000** at every index. Census **977 live / 0 missing**, unmoved.

## 1. PINS — every run in this report shares them

| pin | value |
|---|---|
| release | `834713ccb303`, first cut `2026-09-20T02:14:22.748Z`, 4 cut events, HEAD `3fa44ea3`, CHANGELOG 6.73.0. `node engine/engine_release.js list` reads **`0 of 27 files have moved since`** before and after every run |
| showdown | commit `20ad99ffc9a5a4a4e8fb56ab04ad8e4255b3f2b4`, stamped on every artifact |
| census (the steering pin) | `data/mechanics-census.json` as it stood at HEAD: generated `2026-09-20T02:12:33.098Z`, **977 live / 0 missing**, file digest **`bdc8ee177ddb`**. Every differential artifact below stamps `steering.input_digest` `bdc8ee177ddb`, including the 12,000-game draw and both corners. The census was regenerated AFTER every differential run (§6) |
| team store | `--team-store data/team-pool-frozen`. Pools: `0d103fb9fa87` (1200 and both corners), `7e7a37ded7fc` (1350), `a5ce76242f8d` (1950), **`e398641bda45`** (12,000) — the same four digests as the previous pass, so the 12,000-game draw is the SAME lattice and is comparable game by game |
| driver | `steering.driver_code.digest` **`f4b3b2a5c270`** on every run, `driver_code_stable: true` on each. **IT MOVED from `faf70ecbca71`**, because 6.72.0 edited `engine/game_differential.js`. By this project's own rule the two passes' lattices are NOT automatically comparable; where a comparison is made below it is made on SEEDS, not on a count |
| mode | `A/middle/pins:de38d17e15a2/credit:observed-effect/v1/nature:real` on the four middle runs; `A/top-tie-first/pins:7759a509491f/…` and `A/bottom-tie-first/pins:844515f6a72a/…` on the corners. `PIN_DIGEST` `de38d17e15a2` is unmoved |
| flags | `--steering empirical --release 834713ccb303 --arm <arm> --end-state --census data/mechanics-census.json --team-store data/team-pool-frozen --games <N> --write --out <path> --dump-games <400\|4000> --dump-out <relative path>`. `turns_cap` **50** in every artifact. **`--games` is part of the sample definition, not a budget**, and is stated on every row |
| launcher | A node script written in this session's scratchpad (`r8347_run.js`; no file of that name existed) spawns `cmd.exe /c tools\lownode.cmd <argv>` — the argv route `tools/lownode.cmd`'s own header documents — with `SHOWDOWN_PATH=C:/Users/willj/Projects/Pokemon/pokemon-showdown`. The wrapper derives `ABRA-HEAP: 8192` from `engine/game_differential.js`'s own header, so no flag was typed |
| concurrency | Every process was mine; I was the only agent. The three lattices ran together; the 12,000-game run overlapped the two corners, the battery, the damage differential and the roster chain, none of which reads or writes an artifact another was writing. The census regeneration, `quarantine.js` and `coverage.js` ran strictly after every writer had exited |

## 2. THE THREE GATE LATTICES — board **0 / 0 / 0**, narration **23 / 30 / 33**

| `--games` | artifact | games | **board-material** | protocol diverged (raw) | **undeclared narration** | threw | boundaries identical | generated | wall |
|---|---|---|---|---|---|---|---|---|---|
| 1200 | `data/game-differential.json` | 961 | **0** | 23 | **23** | **0** | 10716 / 10716 | 03:09:01Z | 422 s |
| 1350 | `data/game-differential.g1350.json` | 1069 | **0** | 31 | **30** | **0** | 11856 / 11856 | 03:09:21Z | 441 s |
| 1950 | `data/game-differential.g1950.json` | 1497 | **0** | 35 | **33** | **0** | 16725 / 16725 | 03:11:02Z | 534 s |

Board-material is `state.games` − `state.games_board_never_diverged`, never a capped list and never
`by_cause_totals.games_board_material`. Undeclared narration is `narrationClause`'s own subtraction
(raw less the declared Supreme Overlord row). `state.board_parted_before_the_protocol_did` is **0** on
all three. `games_void_excluded` **0**, `games_cut_off_by_the_turn_cap` **0**.

**EVERY REMAINING NARRATION CAUSE, BUCKETED BY MECHANISM FROM THE FULL DUMP** — not from
`state.first_divergences`, which is capped. The dumps hold 23 / 31 / 35 rows against
`of_diverged` 23 / 31 / 35, so these ARE the populations:

| mechanism | 1200 | 1350 | 1950 |
|---|---|---|---|
| **Cloud Nine's arrival announcement — the authority writes `\|-ability\|BODY\|Cloud Nine` and we write nothing** | 19 | 27 | 29 |
| **Super Sweet Syrup announced WITH `\|boost` — the authority's line is bare** | 4 | 3 | 4 |
| DECLARED — Supreme Overlord `fallenundefined` (subtracted) | 0 | 1 | 2 |
| anything else | **0** | **0** | **0** |

**Both are read out of the authority's own source, not from memory:**

- `data/abilities.ts:536` — `cloudnine.onSwitchIn(pokemon) { this.add('-ability', pokemon, 'Cloud Nine'); … }`.
  Air Lock is the identical handler at `data/abilities.ts:93`. **Neither is overridden in
  `/data/mods/champions/abilities.ts`** — grepped, no match for `cloudnine`, `airlock` or
  `supersweetsyrup` in the mod.
- `data/abilities.ts:4708` — `supersweetsyrup.onStart` writes
  `this.add('-ability', pokemon, 'Supersweet Syrup')` with **no** `'boost'` marker, where
  `intimidate` writes the same line **with** it. Our engine routes both through the shared entry-drop
  site, which passes `'boost'` unconditionally (`engine/medicham2-browser.js:22865`,
  `TR.ab(m, m.ability, 'boost')`).

*(A lead, not a diagnosis, and not fixed here — this pass changed no byte.)*

## 3. THE GATE, CLAUSE BY CLAUSE

`node engine/quarantine.js` at HEAD bytes, run once, after every artifact below had been written and
with no other writer live. Exit 0. **`GATE: CLOSED — 3 of 10 GATING clauses fail`.**

| clause | reading |
|---|---|
| game differential (damage) | **PASS** — midpoint 0 of 6000, top 0/6000, bottom 0/6000, idx01–idx14 0/6000, seed 20260804 |
| deliberate roster / items | **PASS** — clean, 148 of 148 tested |
| deliberate roster / abilities | **FAIL** — **0 DIFFER, 0 DID-NOT-FIRE, 196 of 200 tested.** Two instrument faults: **1 red demonstration did not behave as its rule predicted** (`ability/residual`, anchor dead), and **2 ANNOUNCEMENT-ONLY rows have no good receipt** (`ability:anticipation`, `ability:frisk`) |
| deliberate roster / moves | **PASS** — clean, 496 of 497 tested |
| coverage / every used mechanic is measured | **PASS** — all 412 moves above 25 clicks are measured by the roster or the census |
| board leaves / nothing uncompared at a boundary | **PASS** — 0 standing, 56 compared, `BS.snapshot` has 1 call site |
| whole-game BOARD-MATERIAL, on EVERY lattice | **PASS** — `ZERO ON EVERY LATTICE`: 0 of 961, 0 of 1069, 0 of 1497 |
| whole-game NARRATION, on EVERY lattice | **FAIL** — `NON-ZERO ON 3 OF 3`: **23 of 961** (13 causes), **30 of 1069** (17), **33 of 1497** (23). This clause GATES now, by its own rule, because board-material reads zero |
| mechanics / each staged and compared | **FAIL** — **14 of 15 diverging mechanics are played and uncleared** (moves 7, abilities 6, items 2; 3 shelved by the owner, 1 below reach). **0 never fired.** PROOF: every in-scope row proven; owner-excused **1** (`ability:illusion`). CONTROL WATCH 42 live-control rows, **42 earned, 0 unearned**. CONTROL ARMS **17 of 841 parted, board-material 0** — all 17 announcement-only |
| no open, known engine defect | **PASS** — no open row names a RED instrument (194 verdicts read); 11 NOT-A-DEFECT rows excused |

**THE 15 DIVERGING STAGED MECHANICS ARE THE SAME TWO MECHANISMS AS §2**, read straight off
`data/all-mechanics-fire.json`:

| kind | rows | cause |
|---|---|---|
| moves | `bravebird`, `cottonguard`, `defog`, `featherdance`, `moonblast`, `pluck`, `sing` (7) | `event missing from medicham2 :: \|-ability\|p1a\|cloudnine <> \|turn\|1` — the fixture's body carries Cloud Nine |
| abilities | `cloudnine`, `earlybird`, `insomnia`, `sweetveil`, `vitalspirit` (5) | the same Cloud Nine line |
| abilities | `supersweetsyrup` (1) | `-ability field 4` |
| items | `altarianite`, `habanberry` (2) | the same Cloud Nine line |
| shelved by the owner | `bittermalice`, `nightdaze`, `illusion` (3) | Illusion closet, `switch: a different body` |

**THE TWO ROSTER-ABILITY FAULTS, NAMED.**

1. **The `ability/residual` red plant has a dead anchor.** Its patch anchors on the two-line string
   `if(_be&&_be.boosts&&m.boosts&&!m._newlySwitched)for(const k in _be.boosts){`
   (`tests/roster.js:12869`). 6.73.0 inserted `const _ber=abilityBoostRun(m,m.ability,{isSecondary:false});`
   between those two lines (`engine/medicham2-browser.js:48922-48924`), so the anchor now matches **0
   times** and the plant asserts nothing. The artifact says so itself: `anchor_dead: true`, `reds`
   64 ok of 65. **This is the same failure shape as 6.60.0's dead anchor** — an engine fix moving the
   line a plant aims at.
2. **Two announcement receipts are refused.** `anticipationSilentRestored` and `friskSilentRestored`
   are **not** in `const DELIBERATE_BREAK` in `tests/test-mechanics.js` — verified by parsing that
   list (119 names; `forewarnSilentRestored` IS there, the other two are not) and by
   `git show` at `3fa44ea3`, `616dc1c8` and `9785e362`, where they are absent in all three.
   `ability:anticipation` fails a second check as well: **its census row quotes no literal
   `|-<event>|` line**, only counts. **This is not a regression this pass introduced** — the
   committed `data/roster.abilities.json` at HEAD was generated `2026-09-19T22:15:41Z` on release
   `18773c22878f` and carries **no ANNOUNCEMENT-ONLY row at all**, so the gate had never evaluated
   these receipts against main. The 6.71.1 acceptance was measured in a worktree and did not survive
   into a committed artifact.

Two things the gate printed that are not clauses and are reported rather than filed:

- **`STALE VERDICTS`** — `data/register-reality.json` generated `2026-09-12T11:43:20Z`, older than
  `docs/ROADMAP.md` (`2026-09-19T15:16:30Z`). **18 open rows assert breakage with no instrument that
  decides them** (#397, #400, #175, #207, #284, #285, #67, #365, #367, #393, #421, #423, #441, #473,
  #495, #507, #530, #550) and **2 name an instrument that is GREEN** (#310, #442). Re-run:
  `node engine/register_reality.js`.
- The declared register's Supreme Overlord row **matched nothing on the 1200 lattice** and the clause
  says so: *"a declaration that covers no cause is a claim that has quietly become false"*. It DID
  match on 1350 (1) and 1950 (2).

`node engine/coverage.js`:

| row | now (was, on `18773c22878f`) |
|---|---|
| board leaves compared | 56 (56); 0 uncompared leaves can stand at a boundary |
| uncomparable leaves with a firing writer | 23 of 24 (23 of 24) |
| move leaves whose EFFECT was exercised | 7 of 11 (7 of 11) |
| staged mechanics that fired | **844 of 845** (844 of 845) |
| fired mechanics with a board compared | **844 of 845** (844 of 845) |
| tags with an engine consumer | **303 of 303**, 0 unread (299 of 301 two passes ago; 9 of 312 tags have no in-scope carrier) |
| tags with a census probe | 303, 0 unprobed |
| turn boundaries compared | 10716 (10716) |
| entities exercised in a real game | **871 of 915** (861 of 902) |

## 4. THE HELD-OUT DRAW — `--games 12000`, **34 → 15 BOARD PARTINGS**

One run, same pins as §1, written to its own files so no gate artifact was touched:
`--out data/verification/game-differential.g12000.json`,
`--dump-games 4000 --dump-out data/verification/_8347_dump_g12000.json`. Started 03:11:31Z, finished
03:43:01Z (1,879 s), exit 0.

| quantity | this pass | previous pass (`18773c22878f`) |
|---|---|---|
| games | **7,182 usable** (+1 void, 7,183 before exclusion); 0 cut off by the cap | 7,178 usable (+5 void) |
| **board-material** | **15** = 7,182 − 7,167. `wholeGameClause` on this artifact: **16 of 7183** (the 15 plus 1 void game whose `mid_void` tag carries a `board_parted_at_turn`) | 34 (clause: 36 of 7183) |
| **undeclared narration** | `narrationClause`: **236 of 7183 across 132 causes** (259 narration-only raw, less 23 declared, 0 cleared on decision impact) | 56 of 7183 across 66 causes — **NOT COMPARABLE**: `-ability` lines were invisible then |
| protocol diverged | **274** in `state`; the dump holds 274 rows against `diverged_in_arm` 275 (the 1 void game) | 111 |
| threw | **0** | 0 |
| turn boundaries | 79,616 identical of 79,676 compared | 79,474 of 79,623 |
| board parted before the protocol did | **0** | 3 |
| pool | `e398641bda45` | `e398641bda45` — **the same lattice** |

**THE COMPARISON IS MADE ON SEEDS, NOT ON THE TWO COUNTS.** The pool digest, the `--games`, the arm,
the census pin and the team store are identical, so the two runs play the same games. Matching the 34
prior seeds against the 15 now:

- **20 CLOSED** — `…2663420926`, `…2655134691`, `…2657156895`, `…2657243554`, `…2657252654`,
  `…2658731303`, `…2660074844`, `…2654352872`, `…2654505540`, `…2656656403`, `…2657742695`,
  `…2660280251`, `…2655368189`, `…2658618561`, `…2662400605`, `…2634939984`, `…2659722527`,
  `…2658356295`, `…2658772547`, `…2660534650`.
- **1 JOINED** — `…2657780674` (`omit-intimidate`, turn 8): `|-activate|p1a|protect` where the
  authority resolves `|move|p1a|screech`, and the target's Defence stage and HP part. It is in the
  Protect family, which 6.73.0 edited.
- **14 STAND.**

**THE 15 BOARD PARTINGS, GROUPED.** The grouping is mine, from the board leaves and the game's first
protocol divergence; the counts are exact, the attribution is a lead rather than a diagnosis. **A game
records only its FIRST divergence, and the first PROTOCOL divergence is not necessarily the cause of
the board parting.**

| mechanism | games | which |
|---|---|---|
| **Protect: it activates here and the authority resolves the move** (Parting Shot, Soak, Screech, Taunt) | **4** | 1, 3, 6, 9 |
| **a burn landing in one engine and not the other** (Spicy Spray, Flame Body, a bare `-status`) | **3** | 2, 12, 13 |
| Armor Tail refusing Fake Out (and a Tailwind that goes up on one side only) | 1 | 7 |
| accuracy: a `-miss` one engine does not draw | 1 | 4 |
| a boost that lands in the opposite direction (`-unboost` vs `-boost`) | 1 | 5 |
| Bug Bite's steal-eat against Rough Skin's KO | 1 | 8 |
| Heal Block refusing Drain Punch | 1 | 10 |
| flinch: whether the click happens at all | 1 | 11 |
| Cursed Body's Disable on Struggle | 1 | 14 |
| Trick Room: a different move resolves in `move` field 3 | 1 | 15 |

Every parting game, with its lattice seed, its first-divergence line and both board leaves:

```text
 1. baseline  board turn 8  protocol turn 8  seed gen9championsvgc2026regmbbo3-2659324893
    first divergence: unrelated event mismatch :: |-activate|p1a|protect <> |move|p1a|partingshot
    board: p1.party.talonflame.hp   us=140  showdown=153
    board: p1.active[0].species   us="talonflame"  showdown="hatterene"
    board: p1.active[0].hp   us=140  showdown=132
    board: p1.active[0].maxhp   us=153  showdown=132
    board: p1.active[0].item   us="sharpbeak"  showdown="focussash"
    board: p1.active[0].types   us="fire/flying"  showdown="fairy/psychic"
    board: p1.active[0].ability   us="galewings"  showdown="magicbounce"
    board: p1.active[0].stall   us=0  showdown=3

 2. omit-weather  board turn 9  protocol turn 9  seed gen9championsvgc2026regmbbo3-2654574813
    first divergence: extra event emitted by medicham2 :: |faint|p2b <> |-status|p1b|brn|[from]spicyspray
    board: p1.party.sinistcha.hp   us=75  showdown=84
    board: p1.party.sinistcha.status   us="brn"  showdown=""
    board: p1.active[1].hp   us=75  showdown=84
    board: p1.active[1].status   us="brn"  showdown=""

 3. omit-weather  board turn 1  protocol turn 1  seed gen9championsvgc2026regmbbo3-2659164097
    first divergence: unrelated event mismatch :: |-activate|p1a|protect <> |move|p1a|soak
    board: p2.party.bellibolt.types   us="water"  showdown="electric"
    board: p2.active[0].types   us="water"  showdown="electric"

 4. omit-intimidate  board turn 5  protocol turn 5  seed gen9championsvgc2026regmbbo3-2655224585
    first divergence: unrelated event mismatch :: |-damage|p1b|H/H <> |-miss|p2b|p1b
    board: p1.party.garchomp.hp   us=116  showdown=84
    board: p1.active[1].hp   us=116  showdown=84

 5. omit-intimidate  board turn 9  protocol turn 9  seed gen9championsvgc2026regmbbo3-2657170022
    first divergence: unrelated event mismatch :: |-unboost|p1b|spa|1 <> |-boost|p1b|spa|1
    board: p1.party.archaludon.boosts.spa   us=2  showdown=0
    board: p1.active[1].boosts.spa   us=2  showdown=0
    board: p2.party.metagross.hp   us=1  showdown=77
    board: p2.active[0].hp   us=1  showdown=77

 6. omit-intimidate  board turn 8  protocol turn 8  seed gen9championsvgc2026regmbbo3-2657780674   [JOINED THIS PASS]
    first divergence: extra event emitted by medicham2 :: |-activate|p1a|protect <> |move|p1a|screech
    board: p2.party.sinistcha.hp   us=47  showdown=96
    board: p2.party.sinistcha.boosts.def   us=-2  showdown=0
    board: p2.active[0].hp   us=47  showdown=96
    board: p2.active[0].boosts.def   us=-2  showdown=0

 7. omit-intimidate  board turn 1  protocol turn 1  seed gen9championsvgc2026regmbbo3-2659757084
    first divergence: unrelated event mismatch :: |-resisted|p1a|1 <> |cant|p1b|armortail|fakeout
    board: p1.tailwind   us=3  showdown=0
    board: p1.party.aerodactyl.hp   us=53  showdown=43
    board: p1.active[0].hp   us=53  showdown=43
    board: p1.pp[0].tailwind   us=1  showdown=0

 8. omit-intimidate  board turn 13  protocol turn 13  seed gen9championsvgc2026regmbbo3-2660750080
    first divergence: unrelated event mismatch :: |-enditem|p1a|sitrusberry|[from]stealeat|[move]bugbite <> |-damage|p2a|0fnt|[from]roughskin
    board: p2.party.scizor.hp   us=0  showdown=34
    board: p2.party.scizor.fainted   us=true  showdown=false
    board: p2.party.scizor.status   us="fnt"  showdown=""
    board: p2.active[0].hp   us=0  showdown=34
    board: p2.active[0].fainted   us=true  showdown=false
    board: p2.active[0].status   us="fnt"  showdown=""
    board: p2.active[0].ate_berry   us=0  showdown=1

 9. pair-protect-bust  board turn 14  protocol turn 14  seed gen9championsvgc2026regmbbo3-2658446009
    first divergence: extra event emitted by medicham2 :: |-activate|p2a|protect <> |move|p2a|taunt
    board: p1.active[1].vol.taunt   us=3  showdown=0

10. pair-protect-bust  board turn 3  protocol turn 3  seed gen9championsvgc2026regmbbo3-2661429975
    first divergence: unrelated event mismatch :: |cant|p2a|healblock|drainpunch <> |move|p2a|drainpunch
    board: p1.party.slowbro.hp   us=137  showdown=170
    board: p1.active[0].hp   us=137  showdown=170
    board: p2.pp[0].drainpunch   us=1  showdown=0

11. pair-redirect-priority  board turn 4  protocol turn 4  seed gen9championsvgc2026regmbbo3-2655714014
    first divergence: unrelated event mismatch :: |move|p1a|shedtail <> |cant|p1a|flinch
    board: p1.pp[0].shedtail   us=1  showdown=2

12. pair-redirect-priority  board turn 11  protocol turn 11  seed gen9championsvgc2026regmbbo3-2656306845
    first divergence: event missing from medicham2 :: |-status|p2b|brn <> |upkeep
    board: p2.party.whimsicott.hp   us=108  showdown=100
    board: p2.party.whimsicott.status   us=""  showdown="brn"
    board: p2.active[1].hp   us=108  showdown=100
    board: p2.active[1].status   us=""  showdown="brn"

13. pair-speedctrl  board turn 5  protocol turn 5  seed gen9championsvgc2026regmbbo3-2655141321
    first divergence: event missing from medicham2 :: |-status|p1a|brn|[from]flamebody <> |-weather|sunnyday|[upkeep]
    board: p1.party.toxapex.hp   us=49  showdown=42
    board: p1.party.toxapex.status   us=""  showdown="brn"
    board: p1.active[0].hp   us=49  showdown=42
    board: p1.active[0].status   us=""  showdown="brn"

14. pair-speedctrl  board turn 13  protocol turn 13  seed gen9championsvgc2026regmbbo3-2656401580
    first divergence: extra event emitted by medicham2 :: |-damage|p2b|H/H|[from]recoil <> |-start|p2b|disable|struggle|[from]cursedbody
    board: p2.active[1].vol.disable   us=3  showdown=0

15. pair-speedctrl  board turn 10  protocol turn 10  seed gen9championsvgc2026regmbbo3-2663796709
    first divergence: move field 3 :: |move|p2a|trickroom <> |move|p2a|psychic
    board: field.trickroom_turns   us=3  showdown=0
    board: p2.pp[0].trickroom   us=1  showdown=2
    board: p2.pp[0].psychic   us=1  showdown=0
```

**ALL 274 PROTOCOL DIVERGENCES OF THE HELD-OUT DRAW, BUCKETED BY MECHANISM** from the full dump
(274 = 259 narration-only + 15 board-material; the dump is not capped here — 274 rows against
`of_diverged` 275, the difference being the one void game):

| mechanism | games |
|---|---|
| **Cloud Nine's arrival announcement we never write** | **167** |
| a bare `\|-fail\|` the two engines do not agree on (16 where we write none, 6 on a different body, 4 other) | **26** |
| DECLARED — Supreme Overlord `fallenundefined` | 23 |
| **Super Sweet Syrup announced with `\|boost`, the authority bare** | **13** |
| Protect activates here and the other engine resolves the move | 9 |
| the charge volatile (Electromorphosis / Dual Wingbeat): its `-start` / `-end` line order | 7 |
| recoil `-damage`: the value or the order | 3 |
| Tailwind `-sideend`: which side ends first | 3 |
| perish counter line | 2 |
| a priority-refusing ability: which one refuses (Queenly Majesty vs Armor Tail) | 2 |
| thaw `-curestatus` order | 2 |
| Mold Breaker's arrival announcement we never write | 1 |
| flinch | 1 |
| remaining singletons | 15 |

**The ability-announcement class is 181 of 274 — 66% of everything, and 68% of the narration.** It is
also the whole of the gate's narration clause and the whole of its mechanics clause. Nothing else in
this run is within an order of magnitude.

## 5. THE CORNER ARMS — `--games 1200`, board **0** and **0**

Same pins, `--arm top-tie-first` and `--arm bottom-tie-first`, written to
`data/verification/game-differential.g1200.<arm>.json` so no gate artifact moved. A corner run
measures the corner because `RUN_PRIMARY` is `ARMS_RUN[0]` (the 2026-09-10 fix).

| `--games` | arm | games | **board-material** | protocol diverged (raw) | boundaries | pool |
|---|---|---|---|---|---|---|
| 1200 | middle | 961 | **0** | 23 | 10716 / 10716 | `0d103fb9fa87` |
| 1200 | top-tie-first | 961 | **0** | 31 | 12816 / 12816 | `0d103fb9fa87` |
| 1200 | bottom-tie-first | 961 | **0** | 35 | 9054 / 9054 | `0d103fb9fa87` |

Previous pass on `18773c22878f` at the same `--games`: **0** and **1**. `board_parted_before_the_protocol_did`
is 0 on both corners. The corner protocol counts are RAW — no per-corner declared subtraction was
computed. **Both corners are dominated by the same class**: top is 19 Cloud Nine + 4 Super Sweet Syrup
+ 1 declared + 7 other; bottom is 20 Cloud Nine + 4 Super Sweet Syrup + 1 Mold Breaker + 1 declared +
9 other. The corner-only causes are singletons: a Skill Swap / Flash Fire `-end` ordering, a Castform
`-formechange` against `-weather|none`, a Pickpocket `-enditem` against a faint, a
`cant|taunt|reflect` vs `cant|par`, a Quick Claw `-activate`, a Safeguard `-activate`.

**The corners part no board on this release, so the corner arms have nothing to report beyond
narration** — the first pass in which that is true.

## 6. THE STAGED BATTERY, THE ROSTER, THE DAMAGE DIFFERENTIAL AND THE CENSUS

| artifact | generated | reading |
|---|---|---|
| `data/all-mechanics-fire.json` | 03:12:59Z | **4,634 games, 0 threw, 0 sheets unassembled, `red_ok: true`**, release `834713ccb303`. **Abilities:** 200 in scope, **FIRED 199**, DID-NOT-FIRE **0**, SHOWDOWN-ONLY 0, MEDICHAM-ONLY 0, **diverged 6 unshelved** (`cloudnine`, `earlybird`, `insomnia`, `supersweetsyrup`, `sweetveil`, `vitalspirit`); the one row not FIRED is `illusion`, closeted. **Moves:** 497 in scope, **497 resolved on both engines**, 0 resolution disagreements, **7 diverged unshelved** (all the Cloud Nine line), 2 shelved and diverging. **Items:** **148 FIRED**, **2 diverged** (`altarianite`, `habanberry` — the Cloud Nine line). `control_watch.fired_on_live_control_unearned` **[] (0)** over 42 live-control rows. `control_arm_partings`: 841 rows with a control arm, **17 parted, `board_material` 0**, all 17 announcement-only |
| `data/roster.items.json` | 03:22:50Z | **148 FIRED-AND-BOARDS-MATCH**; 0 DIFFER / DID-NOT-FIRE / COULD-NOT-STAGE / CONTROL-NOT-QUIET. Reds **22 of 22 caught**. Exit 0 |
| `data/roster.abilities.json` | 03:24:04Z | **196 MATCH**, **3 ANNOUNCEMENT-ONLY** (anticipation, forewarn, frisk), **1 DEFERRED-BY-OWNER** (illusion); 0 DIFFER / DID-NOT-FIRE / COULD-NOT-STAGE. Reds **64 of 65** — `ability/residual` has a dead anchor. **Exit 1** |
| `data/roster.moves.json` | 03:24:44Z | **496 MATCH**, **1 DEFERRED-BY-OWNER** (copycat), 0 otherwise. Reds **37 of 37 caught**. Exit 0 |
| `data/engine-diff.json` | 03:17:05Z | `--n 6000`, seed 20260804: requested 6000, **compared 6000, agreed 6000, disagreed 0**, `band_missing` 0, both corner arms 0 of 6000. Release `834713ccb303` |
| `data/mechanics-census.json` | 03:45:56Z | **977 live / 0 missing, `probed` 984, `armed` 977, `unarmed` 0, `threw` 0, `run_ok: true`** (the run prints "977 live, 0 missing, 977 probed"; the artifact's `probed` counts 984 probe invocations). Regenerated AFTER every differential run, so the steering pin `bdc8ee177ddb` is the census as it stood at HEAD; the regenerated file digests `9a373e81f281` and **no row's verdict moved** (977 live / 0 missing either side) |

## 7. FILES CHANGED

- **Code: none.** No engine, instrument or test file was edited, and no release was cut —
  `data/releases/834713ccb303/cuts.jsonl` still holds the same 4 events it held before this pass.
- **Data, rewritten by the runs:** `data/game-differential{,.g1350,.g1950}.json`,
  `data/all-mechanics-fire.json`, `data/roster.{items,abilities,moves}{,.prev}.json`,
  `data/roster.json`, `data/engine-diff.json`, `data/published-samples.json`,
  `data/mechanics-census.json`, `data/verification/game-differential.g1200.{top,bottom}-tie-first.json`,
  `data/verification/game-differential.g12000.json` (all three of those were already tracked from the
  previous pass), and `data/provenance-stamp.json` by `status.js --write`.
- **Data, new and untracked:** six `--dump-out` files under `data/verification/`. The
  three gate dumps were written to relative paths `data/_8347_dump_g<N>.json` as the brief asked and
  then moved into `data/verification/` so `data/` stays legible. **Nothing was deleted.**
- **Docs:** this report; `CHANGELOG.md` 6.74.0; one `docs/RUNNING-NOTES.md` row (MINOR,
  **Basis.** unchanged); one `docs/ENGINE.md` section and its hand list; the five ledgers restamped by
  `node engine/status.js --write`.
- **White paper: NOT TOUCHED, and the reason is stated.** Its "GATE: OPEN" and its corner-arm figures
  (`16 of 961`, `15 of 961`) both sit inside DATED blocks that name their release — `cbd510bc2b13`
  and the 6.0.0 block, which already carries an explicit 2026-09-11 retraction at the top of the file
  saying the gate is closed and that block is dated evidence. No present-tense figure in that
  document is superseded by this run.
- **Held documents (technical docs, MODELS, SUMMARY, deck): NOT TOUCHED**, neither the index nor the
  working copy. Each carries its gate figures stamped with a HEAD and a date (`bf2d594f`,
  `b8f7d999`, 2026-09-11) inside dated blocks, so none is superseded as written. Will's held drafts
  were never read past `git show HEAD:`.
- `docs/_reports/2026-09-19-700-draft/` was not touched.
- **git:** no command that writes was run. Nothing staged, committed or pushed.
- **Processes:** all 13 were started by me, all exited on their own. Exit codes: 0 everywhere except
  `tests/roster.js --stage abilities`, which exits 1 on the dead plant. Nothing was killed.

## OWED, NOT RUN

1. **THE FIX WAVE IS ONE MECHANISM AND IT IS THE WHOLE GATE.** Cloud Nine (and its identical twin Air
   Lock, `data/abilities.ts:93`) announces on `onSwitchIn` and we write nothing; Super Sweet Syrup
   announces with a `|boost` marker the authority does not write. Together they are **181 of 274**
   held-out divergences, **86 of the 89** gate-lattice divergences (the other 3 are the declared
   row), and **all 15** diverging staged mechanics — 14 on the Cloud Nine line, the 15th being Super
   Sweet Syrup itself. Each needs a probe shown RED first.
2. **THE ROSTER'S `ability/residual` PLANT IS DEAD AND MUST BE RE-AIMED**, not deleted. Anchor at
   `tests/roster.js:12869`; the line it aimed at is now two lines apart in
   `engine/medicham2-browser.js:48922`. Until then that rule asserts nothing and the abilities stage
   exits 1.
3. **THE TWO ANNOUNCEMENT KNOBS ARE NOT DECLARED.** `anticipationSilentRestored` and
   `friskSilentRestored` belong in `const DELIBERATE_BREAK` in `tests/test-mechanics.js`, and
   Anticipation's census row needs to quote the authority's literal line rather than counts. Both are
   one-line declarations; neither is an engine change.
4. **14 BOARD PARTINGS STAND IN THE HELD-OUT DRAW**, in ten mechanisms, four of which carry seven of
   them — Protect's activation (4), a burn landing in one engine only (3). Seeds are in §4.
5. **THE GATE'S LATTICE SET IS A MEASURE DECISION, NOT ENGINE'S.** 1200 / 1350 / 1950 find zero board
   partings; 12,000 games on the same release, census, pool and arm find 15.
6. **THE NARRATION FIGURES ARE NOT COMPARABLE ACROSS 6.72.0.** Every narration count published before
   that release was taken with `|-ability|` invisible. 6.72.0's row already declares this; the
   board-material figures ARE comparable and are compared on seeds in §4.
7. **`data/register-reality.json` is STALE** (2026-09-12 against a ROADMAP that moved 2026-09-19).
   `node engine/register_reality.js` is owed before the open-defect clause's PASS means what it says.
8. **No per-corner declared subtraction.** The corner protocol counts in §5 are raw.
9. **THE DOCUMENTATION DEBT IS AT 98 OF 100** with this pass's row counted (97 before it) —
   `node engine/docs_scan.js --owed`. **Two rows from the cap**, at which the build FAILS. Clearing it is a DOCUMENT PASS at
   any version, not a major, and it is not ENGINE's to take.
10. **`docs/ENGINE.md` still carries `CHANGELOG <<VER>>` placeholders** in some worktree-pass headings,
    and `CHANGELOG.md` is still missing its `## [6.65.0]` heading. Both were reported on 2026-09-19
    and neither is repaired here.
11. **`tests/test-docs-current.js` was not run against the working tree**, which holds Will's held
    7.0.0 drafts. I edited none of those files.
