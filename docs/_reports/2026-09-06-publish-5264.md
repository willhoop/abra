# PUBLISH PASS 5.264.0 — THE DICE-ADDRESS FIX REACHES THE LIVING DOCUMENTS

2026-09-06, MEASURE. Historical record. Not maintained, not current state, superseded by whatever
`node engine/status.js` prints.

---

## VERDICT

**New version 5.264.0.** The 5.263.0 publication was one run behind `data/game-differential.json`;
this pass discharges it. **No figure in the brief disagreed with its artifact.** Every number below
was re-read from the file that holds it, on a settled tree with no other agent running.

---

## 1. WHAT THE ARTIFACTS SAY, RE-READ THIS PASS

| question | artifact | reading |
|---|---|---|
| the clause that GATES | `data/game-differential.json` | board-material **27 of 961** — `state.games` 961 less `state.games_board_never_diverged` 934. Both operands read, not the difference |
| the clause that REPORTS | the same artifact | protocol first divergence **93** — `state.protocol_diverged_games` |
| the narration clause | the same artifact | **70 of 961** — `state.protocol_diverged_board_never_did` 71, less the 1 declared row, across 69 causes. Unmoved |
| the uncaused games | the same artifact | **5** — `state.board_parted_before_the_protocol_did` |
| the reconciliation the brief named | the same artifact | 93 less 71 = **22** games that parted a board and also diverged in protocol; 22 + 5 = **27**. It closes |
| turn boundaries | the same artifact | **10452** identical of **10541** compared |
| the receipts | the same artifact | `mid_void.selfdrop_enters` 2235, `mid_void.selfdrop_draws` 1925, `mid_void.selfdrop_knob` false; authority-only `any` addresses **18**, `identical` verdicts **702** |
| the conditions | the same artifact | release `d9e551ed0d5a`, 961 games, cap 20, arm `middle`, steering `empirical-click/v1`, `--end-state`, pool `data/team-pool-frozen`, `driver_code_stable` true |
| the control arm | `data/verification/game-differential.selfdrop-shared.json` | 961 less **927** = **34**, protocol **100**, narration **70**, **10429** of **10539** boundaries, 5 void, 1 threw, **717** authority-only addresses, **669** `identical` verdicts, `mid_void.selfdrop_knob` true, `mid_void.selfdrop_draws` 0. Same release, same pins `de38d17e15a2` |
| the census | `data/mechanics-census.json` | **829** live of **829** probed, **0** missing, `run_ok` true |
| the damage differential | `data/engine-diff.json` | **0 of 6000** at the midpoint and at each of the sixteen band indices, seed 20260804, release `d9e551ed0d5a` |
| the roster | `data/roster.{items,abilities,moves}.json` | 140 / 129 / 475 tested, all on release `d9e551ed0d5a` |

**THE TRAP THE BRIEF NAMED WAS CHECKED ON BOTH OPERANDS, NOT ONE.** `games_board_material` is not the
bar; the bar is `state.games` less `state.games_board_never_diverged`, and the second operand moved
927 → 934 when the artifact was republished. That is exactly the operand that turned
`tests/test-docs-current.js` red for the previous pass. The same check was made on the narration
side: the raw `protocol_diverged_board_never_did` is 71 and the published figure is 70, because one
declared row subtracts.

## 2. THE GATE, IN ONE FRAMING

`engine/status.js` computes **nine** clauses. **Two fail** — the whole-game BOARD-MATERIAL clause and
the whole-game NARRATION clause, both on measured counts. **Eight of the nine GATE**; narration
reports and does not hold the gate shut, by Will's call of 2026-08-22. So of the gating clauses
**one fails**, and the gate reads CLOSED.

"Seven of nine" and "one of eight" are the same state counted over two different sets. They are
never printed side by side in this pass, because side by side they read as a contradiction to
anyone who has not read the clause list.

## 3. THE COMPARABILITY HANDLING — THE PART THAT IS MEASURE'S

- `engine/arms_comparable.js` answered **NOT COMPARABLE** for the prior artifact against the new one,
  naming both causes: the instrument file `engine/game_differential.js` moved, and the pins moved
  `bcb38e47d94f` → `de38d17e15a2` because the addressing contract is hashed into `PIN_DIGEST`.
  **The refusal was honoured. The fall was not published off that pair.**
- The delta is measured against a **third arm** — identical bytes, identical pins, restore knob
  `MEDI_MID_SELFDROP_SHARED=1` armed — which the same tool calls **COMPARABLE** and which reproduces
  the superseded publication in every field listed in the table above. That is what makes 34 → 27
  attributable to one cause rather than to a changed ruler.
- **The control's blind spot is published rather than assumed away.** `arms_comparable` cannot see an
  environment variable, so it calls the pair COMPARABLE while the arms differ by exactly the variable
  under test. Both artifacts therefore declare the difference themselves in `mid_void.selfdrop_knob`
  and `mid_void.selfdrop_draws`. A ruler that agrees because it cannot see is this division's
  standing failure mode, and the answer is a self-declaring artifact rather than a footnote.
- **The prediction card read ten of twelve**
  (`data/verification/_prediction-selfdrop-address.json`, written before the measuring run).
  Board-material called 30 against a measured 27, inside a registered band of 26 to 34; and the
  claim that the new `sdrop` bucket takes only `random(100)` is false on the pool, because
  `outrage|random(2,4)` enters it through the ELSE branch of `selfDrops`. Inert — a range-form draw
  is pinned and consumes no shared address — and the probe assertion was tightened.
- **Sleep is REFUTED and freeze did not move**, both still open as separate defects. They are written
  as refutations, not as fixes, because both were registered under `not_claimed` before the run.

## 4. WHAT WAS WRITTEN

| file | what |
|---|---|
| `CHANGELOG.md` | the 5.264.0 entry — Fixed, Changed, Notes; the third arm's reproduction is written out digit by digit so that the superseded counts stay traceable |
| `docs/ABRA-whitepaper.md` | version header and a 5.264.0 block set: the publication, the cause, the sub-family verdicts, the comparability handling, the prediction card, the narration column, the gate, the next target |
| `docs/ABRA-deck-plain-english.md` | version header and the plain-English account, including why the improvement could not be claimed off the obvious pair |
| `docs/ABRA-technical-docs.md` | version header and an ASD-STE100 block: publication, defect, action, result, scope, sub-family verdicts, comparability, prediction record, receipts, gate, next defect, how to reproduce |
| `docs/SUMMARY.md` | version header, a new question/artifact/answer table at 27 / 93, and the superseded counts restated |
| `docs/MODELS.md` | version header and a 5.264.0 block: no model changed, the release did not move, everything stays withheld |
| `docs/DAMAGE-STAGES.md` | version header and a note that the damage roll is drawn inside `getDamage` and is not in the `any` bucket the fix touched, so the sixteen band indices are unaffected |
| `docs/ENGINE.md` | a POINTER, not a second account — the section the dice-address agent wrote is left exactly as written, with a note that the publication it recorded as owed is discharged |
| `docs/MEASURE.md` | this division's ledger section: the readings, the two framings of the gate, the comparability handling, and what this pass did not do |
| the PDFs | 11 rebuilt; `node build/build_pdfs.js --check` reads *every document has a current PDF* |

`node engine/status.js --write` was run and stamped all five division ledgers. **No
`<!-- GENERATED -->` block was hand-edited.** No handoff document was written.

## 5. GATES

| gate | result |
|---|---|
| `tests/test-docs-current.js` | **24 passed, 0 failed.** No ratchet moved: citation mismatches 54 → 54, untraceable census 34 → 34, and `data/docs-currency-baseline.json` was left untouched |
| `tests/test-docs-quarantine.js` | **all checks passed** |
| `tests/test-roadmap-register.js` | **3 passed, 0 failed** (511 items) |
| `tests/test-artifact-rerunnable.js` | **ALL GREEN — 5 checks**, 1 known unre-runnable, was 1 |
| `node build/build_pdfs.js --check` | clean |
| `tests/test-web-quarantine-loaders.js`, `tests/test-web-status.js` | **PRE-EXISTING RED**, exit 1 both. Stale `web/` build products; WEB is paused. Reported, not chased — the gate itself prints the drift, `web/quarantine-data.js` built 2026-08-25 against a clause list that has since changed |

**HOW THE FIGURES WERE KEPT TRACEABLE.** Every superseded count published in this pass — 34, 100,
927, 717, 669, 10429, 10539 — is written into the 5.264.0 CHANGELOG entry as well as into the prose.
`engine/docs_scan.js` treats a figure recorded in `CHANGELOG.md` as traceable by design, because an
artifact holds only its current numbers while a change record is what holds the ones it used to
hold. That is why the untraceable-figure census did not move.

## 6. WHAT THIS PASS DID NOT DO

- **No refit, and none was started.** `REFIT OWED` stands, and it is a REFIT rather than a restamp:
  the damage table these weights were fitted against has been regenerated, so the feature FUNCTION's
  input changed. A restamp would answer the fixture gate, silence the table gate, and write over the
  evidence.
- **No quarantined figure was released.** Leaf calibration — this division's one number — is still
  WITHHELD, not annotated. `data/winrate-backtest.json` is downstream of MEDICHAM by its generator;
  it becomes quotable when the gate opens AND `node engine/backtest_winrate.js` is re-run.
- **No artifact was re-run and no engine file was touched.** This was a publication over a settled
  tree. Ages were checked against the clock before reading.
- **No commit.** Will pushes.

## 7. WHAT IS STILL ON THE BOARD

Five of the 27 part a board while the protocol never diverges at all, and **three of those five are
one family**: `p*.active[*].stall` reading `medi 0` against `sd 3`. The obvious staging does not
reproduce it, so the `duration: 2` grace is modelled and the condition is something else. That is
ENGINE's next target and it is named with evidence in `docs/ENGINE.md` and in
`docs/_reports/2026-09-06-dice-address-pass.md`.
