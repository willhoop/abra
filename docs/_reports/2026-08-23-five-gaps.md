# The five register gaps, filed — plus one decision row and two extensions

Source: `docs/_reports/2026-08-23-remaining-56.md`, committed as `ea26208`. That triage read
`git show HEAD:data/game-differential.json` at commit `192d917` — release `c66976713feb`, showdown
`20ad99ffc9a5`, census pin `80e648f34d56`, team store pinned to `data/team-pool-frozen` (8,778 teams,
1,968 picked), **961 games / 95 diverged / 0 threw**, generated 2026-08-23T02:40:37Z.

**Nothing was run.** No game was played, no release was cut, `game_differential.js` was not invoked,
`status.js` / `roster.js` / `quarantine.js` / the test suite were not run, and nothing under `engine/`
or `tests/` was edited. Two files under `engine/` and one under `tests/` were READ to learn the
register's own rules (`quarantine.js`'s closed/defect detector, `open_work.js`, `test-roadmap-register.js`)
so that the rows filed here trip the right gate and no other. The only file changed is
`docs/ROADMAP.md`. **Not committed, not pushed.**

---

## 1. WHAT WAS FILED — SIX NEW ROWS, #371 to #376

| row | mechanism | n of 47 | verdict carried |
|---|---|---|---|
| **#371** | **Showdown refuses a move and we execute it**, three sub-causes on one signature | **8** | engine DEFECT for (a) and (b) — 4 of 47; **no verdict** for (c) — 4 of 47 |
| **#372** | the target's berry against the attacker's own Life Orb / recoil chip | 2 | **none** — divergence observed, cause unprobed |
| **#373** | mega-evolution resolution order (`detailschange`) | 2 | **none** — divergence observed, cause unprobed |
| **#374** | the two members of group K that #360 does not cover (Cursed Body, redundant Sitrus `-activate`) | 2 | **none** — announcement shape, zero board impact |
| **#375** | **the dump cap, as an instrument row** | (blocks 25 of 47) | instrument DEFECT — measured, not inferred |
| **#376** | three Protect/Detect orderings that are exact speed ties | 3 | **NOT A DEFECT — decision needed from Will** |

**Row counts.** 6 rows filed. **4 of the 6 carry no verdict by design** (#372, #373, #374, #376) —
plus sub-cause (c) inside #371, which is a no-verdict half inside a row whose other half is a
confirmed defect. **0 reopens.** **2 extensions** rather than fresh rows (§3). **1 decision row** —
#376.

### #371 — the refusal family, filed as ONE row with the sub-causes named

Signature: the authority writes `|-fail|SLOT` and we write the move's effect; both sampled cases carry
`||[still]` on the authority's `|move|` line, which is `sim/battle-actions.ts:594-596` (a `Try` /
`PrepareHit` that returned an explicit `false`) or `:645-647` (an all-false `TryHit`).

- **(a) Endure never reaches the stall gate — 2 of 8, confirmed and source-cited.** `endure.onPrepareHit`
  (`data/moves.ts:4816-4818`) is byte-identical to `protect`'s; Champions overrides `protect` and does
  not override `endure`. Our gate is keyed on the chooser's ACTION KIND (`it.a.kind==='protect'`,
  `engine/medicham2-browser.js:14901`) and `PROTECTMOVES` (`:3891`) omits `endure`. **Both engine lines
  were verified by the coordinator, not re-read here** — that file is under active ENGINE edit tonight,
  so the row tells the next reader to grep `PROTECTMOVES` and `_shieldPending` and never those line
  numbers.
- **The row says the fix is already in the tree.** An uncommitted, unmeasured edit for (a) is sitting in
  the working copy from a stopped agent. The row states that rather than describing the defect as
  untouched, so nobody re-fixes it and nobody assumes it was measured.
- **(b) a trapping move re-applied to an already-trapped body — 2 of 8.** Mechanism derived from the
  format (`meanlook`/`block` → `addVolatile('trapped')`, refused on a repeat); the instance is a
  hypothesis and the row says so.
- **(c) four refusals recorded as UNATTRIBUTABLE BY CONSTRUCTION, not unknown-cause.** `mentions: []`,
  `max_uses: undefined`, absent from the 60 sampled `first_divergences`. The row names #375 as the
  reason. This distinction is the point: "unknown cause" invites somebody to go looking, and there is
  nothing in the file to look at.

### #375 — the dump cap

`first_divergences` samples **60 of 95 games** and carries **22 of the 47 causes**; **25 of the 47** hold
nothing but a normalised cause string. It kills #371(c) and five of #363's six `-boost` rows outright.
Filed as an instrument DEFECT because the shortfall is measured, not suspected. It carries Will's own
words — *"the cost of a card is a few hundred bytes; the cost of a missed root cause is an evening"* —
the three asks (per-divergence context; the stripped `[from]`/`[of]` and SPECIES published beside each
cause; `MEDFAILS.entryOrderTie` / `replaceOrderTie` in the artifact), the note that **a stopped agent was
part-way through raising it**, and the separation from **#349**, which is the RANKING problem and not the
sampling cap.

### #376 — the decision, not a defect

`order_probe` in the artifact holds all three pairs with `getActionSpeed()` read off the AUTHORITY:
122 v 122, 134 v 134, 228 v 228, `speed_gap: 0`, `same_priority: true`, priority 4 both sides. **A tie has
no correct answer.** The status cell says `NOT A DEFECT` in the exact spelling `engine/quarantine.js`
treats as an explicit ruling (`roadmapRowSaysBroken` checks it BEFORE the `DEFECT` token), so this row
cannot inflate the open-defect count while the scoring rule is unchosen.

**It is a new row on #290's own instruction, not a reopen.** #290 is closed and its final sentence reads
*"The remaining 29 have a different cause and want their own row rather than this one being widened."*
The row also records that #290 found **9 of 25 pairs NOT tied** while all three shield pairs here ARE
tied — opposite readings of two different samples, both true, and the older one must not be restated as
covering this.

**Three options are written into the row so it can be answered in one line:** (i) align the two tie
devices and keep scoring; (ii) declare a tied group unscoreable, publish the count, and remove it from
the divergence total; (iii) keep counting and accept an agreement floor no engine change can lift. One
caveat is carried: the second pair's authority line is `|move|p1b: Garchomp|Protect||[still]`, so that
Protect FAILED on the authority and may be #371(a) wearing an ordering class — at most 2 of the 3 are
tie-only.

---

## 2. THE OVERLAP CHECK, RUN BEFORE FILING

Every candidate was checked against the register first. Results:

- **#371** — no row. #232 (a family of six sharing one counter) and #178 (Endure out of `shieldsUser`)
  are cited as the road the gate is keyed on, and neither covers the keying defect.
- **#372** — no row, and the row says why it is none of THREE neighbours: **not #338** (Life Orb recoil
  not firing at all — a presence question), **not #167** (the Sitrus threshold — the HP question), and
  **not #332** (the berry against the `upkeep` marker and faint processing at turn end). The third
  exclusion is mine, not the triage's: #332 is the nearest row and would have swallowed this.
- **#373** — no row. #322 moved the pre-turn CHARGE announcement to order 107 on the same argument;
  the mega bracket (order 104) has none. **#115 is cited as the narrowing fact**: it measured that our
  mega step ALREADY sorts, so this is which key, or a tie, and not a missing sort.
- **#374** — #360 covers Telepathy only, and is a different shape (line-type substitution against an
  extra/misplaced announcement).
- **#375** — #349 owns the ranking's honesty; this is the sampling cap. Different door, different fix.
- **#376** — #290, closed, instructs that the remainder gets its own row.
- **the six `-boost` rows** — already #363. Filed as an EXTENSION, not a row.
- **the `??:farigiraf` Roar** — already #224, reopened. Nothing filed.

## 3. THE TWO EXTENSIONS

Both are register rows whose divergence COUNT was measured on an older artifact and is stale against
the settled one. Neither headline is rewritten — the extension is appended and the status cell says
what changed, so the earlier claim stays legible.

- **#363** (`-boost field 3`): 9 of 96 on `59bb68aa89a9` → **6 of 47** on `c66976713feb`, in five causes
  (one `n=2`). The `-unboost` half this row recorded as ABSENT is **back as exactly one cause**, which is
  #351's original observation. Attribution recoverable from the file is **1 of 6** (`moody` appears once
  in a whole-file scan, `acupressure` zero times). The candidate split is now bounded from the PINNED
  POOL: Acupressure can explain at most 1 of the 6. The other five are blocked by **#375**.
- **#353** (`|switch|` order): 3 → **5**, the two new ones both post-faint replacements, and one of them
  refutes "replacements go in speed order of the incoming body" from inside the file. Its unmet
  instrument ask is now also carried by **#375**.

## 4. WHAT IS STILL UNFILED — REPORTED, NOT FILED

Two remainders from the triage's sixteen mechanisms were outside this brief and are left for routing
rather than filed on my own initiative:

- **Group P, 1 of 47** — `|-supereffective|p1a|1 <> |move|p1a|gravity` after both engines agree on a
  `[from] lockedmove` Phantom Force. Unattributed, possibly the locked-move charge turn. **No register
  row exists for it.** It is the only one of the 47 with no row at all after tonight.
- **Group J's announce-order half, up to 3 of 47** — #339 covers the spread-drain HP ARITHMETIC only,
  not the per-target step and announcement walk (the `-immune` ordering and the drain-line interleave).
  #339 is open; this would be an extension to it or a sibling row, and it needs somebody to decide which.

After the six rows above, **15 of the triage's 16 mechanisms carry a register row** and 46 of the 47
divergences are named somewhere.

## 5. WHAT IS OWED AND WAS NOT DONE HERE

- `node engine/status.js --write` — **not run**, by instruction (light mode). The generated blocks in the
  division ledgers do not yet reflect these rows.
- CHANGELOG entry and version bump — **not written**. `.githooks/pre-commit` runs the docs-currency and
  roadmap-register gates on any commit touching `docs/`, so whoever commits this should expect to satisfy
  them in the same pass.
- Nothing was committed or pushed.
