# ENGINE pass 10: the two Reg M-B tag holds, three probe reds, the first narration fixes

**Date.** 2026-09-23. **Division.** ENGINE (agent) and coordinator. **Line.** abra/regmc 0.80.0–0.86.1.
**Status.** A findings record. It is historical and never cited as current state.

**Written by the coordinator.** Network drops (ENOTFOUND, ECONNRESET) killed the agent three times. It died
for good after merging main into its branch and before writing this report. Each commit body on branch
`engine-pass10` is the primary account. This file carries the verdict and the OWED block.

## VERDICT IN ONE PARAGRAPH

Court Change is implemented. It swaps the side conditions under Reg M-C. What goes through a Substitute is
now a derived tag, so Overdrive reaches the body. Both needed new Reg M-B tag rows, approved on the Ice
Spinner terms: `data/tags.json` gained rows and removed none. `protocol-events.json` and `move-effects.js`
are byte-identical. Pass 9's three probe reds were probe defects, and one of them was only half fixed. The
coordinator finished it in 0.86.1: `probe_volley_collapse_clamp` hardcoded the Reg M-B checkout. Five
narration fixes landed:

- Double Shock and Burn Up fail naming themselves.
- A refused self-aimed volatile move fails out loud.
- Steel Roller ends the terrain in its own onHit.
- A revived body's second faint is announced.
- Speed-tied bodies at one residual order run handler-major.

**No lattice, roster or gate was re-measured on these engine bytes.** Every such figure is owed.

The coordinator ran all ten new probes in both regulations in main after the merge. All exit 0 except
`probe_court_change` under Reg M-B, which exits 2. That probe finds no legal Court Change learner with a
quiet ability in Reg M-B, so the fixture is unbuilt. Treat it as COULD-NOT-STAGE: it counts against the
gate until a fixture is constructed.

## OWED, NOT RUN

Run these one at a time, from main, through the wrapper, on ONE fresh release per regulation, cut after
this merge. Releases `89ac57f1f81b` and `485d0a6840ad` are now stale against the engine.

```
node engine/engine_release.js cut "gate re-read after ENGINE pass 10 (abra/regmc 0.86.1)"
ABRA_REGULATION=regmc node engine/engine_release.js cut "Reg M-C gate re-read after ENGINE pass 10"
# Reg M-C narration still open after pass 10: Emergency Exit / Berserk timing, White Herb, Curse message
# order, Lightning Rod, Magic Bounce, a burn/sleep order, the faint-line instrument question. Re-bucket
# from a FULL dump first:
node engine/game_differential.js --regulation regmc --steering empirical --arm middle --end-state --census <M-C gate census pin> --team-store data/team-pool-frozen-regmc --release <M-C id> --games 1200 --write --out data/verification/gd-regmc-pass10.json --dump-games 2000 --dump-out data/verification/gd-regmc-pass10-dump.json
# Court Change under Reg M-B: construct the fixture (a legal learner, any ability, with a board where its
# ability cannot act), then:
node tests/probe_court_change.js --regulation regmb
# Heal Bell's control never exercises the cure (its board has no status). Give it a status to cure.
# Then the roster, six stages, on the NEW release ids:
SHOWDOWN_PATH=C:/Users/willj/Projects/Pokemon/pokemon-showdown tools\lownode.cmd tests/roster.js --stage items --reds --write --release <M-B id>
SHOWDOWN_PATH=C:/Users/willj/Projects/Pokemon/pokemon-showdown tools\lownode.cmd tests/roster.js --stage abilities --reds --write --release <M-B id>
SHOWDOWN_PATH=C:/Users/willj/Projects/Pokemon/pokemon-showdown tools\lownode.cmd tests/roster.js --stage moves --reds --write --release <M-B id>
ABRA_REGULATION=regmc SHOWDOWN_PATH=C:/Users/willj/Projects/Pokemon/pokemon-showdown-mc tools\lownode.cmd tests/roster.js --stage items --reds --write --release <M-C id>
ABRA_REGULATION=regmc SHOWDOWN_PATH=C:/Users/willj/Projects/Pokemon/pokemon-showdown-mc tools\lownode.cmd tests/roster.js --stage abilities --reds --write --release <M-C id>
ABRA_REGULATION=regmc SHOWDOWN_PATH=C:/Users/willj/Projects/Pokemon/pokemon-showdown-mc tools\lownode.cmd tests/roster.js --stage moves --reds --write --release <M-C id>
# Every published artifact the gates read (lattices 1200/1350/1950 both regs, damage differentials, both
# censuses, all_mechanics_fire, the Reg M-B 12000 held-out), then:
node engine/quarantine.js --regulation regmc --stamp-whole-game
node engine/register_reality.js
node engine/register_reality.js --regulation regmc
node engine/quarantine.js
node engine/quarantine.js --regulation regmc
node engine/status.js --write
```
