# Living docs brought to 5.243.0 — the gate reversal, written as a reversal

2026-09-03. MEASURE. Documentation only: no game was played, no engine was run, no artifact was
regenerated. The only executable run in this pass was `node tests/test-docs-current.js`, which is
this work's own gate.

## Why the pass existed

`CHANGELOG.md` topped at **5.243.0** with six version-headed documents still reading 5.242.0.
`tests/test-docs-current.js` clause 2 would have blocked the commit. A header bump alone was not the
fix: the 5.243.0 block is a **reversal of a published position**, and CLAUDE.md's changelog rule is
that a prior conclusion is never silently rewritten — what changed and why is stated.

## The substance, as stated in every document

Verified by the coordinator and not re-derived here. Every figure below appears identically in all
eight documents; nothing was authored in this pass.

| claim | figure |
|---|---|
| the gate before | 8 of 8 PASS |
| the gate now | **GATE: CLOSED — 1 of 8 clauses fail.** Nothing was tuned |
| coverage driver `census-coverage-seeking/v1` | **17 of 961 reached a result (1.8%)**, 944 stopped at the 12-turn cap, **0** boards parted |
| empirical driver `empirical-click/v1` | **474 of 961 reached a result (49.3%)**, **77** boards parted |
| pins, identical for both | release `8ad06030e129`, cap 12, pool `0d103fb9fa87`, census pin `9446a684709d`, 961 games of a 1200 PAIR budget. **The driver is the only difference** |
| board-material | **77 of 961 (8.0%)** |
| protocol first-divergence | **168** |
| what the clause prints | **167** — it gates on protocol first-divergence, not board-material. NOT changed: the owner's 2026-08-22 call is board-material, and moving it in the pass that turned the gate red is indistinguishable from tuning |
| the refusal | `steering.policy` not `empirical-click/v1` → `MEASURED ON THE WRONG POPULATION`, `clauseExit` 2. Selftest 159 passed / 0 failed |
| downstream | `data/policy-weights.json` untouched, MAG paused, `status.js --write` NOT run |

**Two quantities, never pooled.** Board-material 77 and protocol first-divergence 168 answer
different questions. Every one of the eight documents names which one it is quoting, every time it
quotes one.

## What was written, and to whose audience

| file | what landed |
|---|---|
| `docs/ABRA-whitepaper.md` | technical block: the defect, the exact control (one free variable), the corrected two quantities, the three instrument changes, the declared 167-vs-77 mismatch, and the statement that the earlier suggestion the quarantine was near lifting rested on the coverage arm |
| `docs/ABRA-deck-plain-english.md` | plain English, no jargon: "you cannot disagree about how a game ended if the games do not end"; states plainly that this reverses something published and that the simulator is further from finished than we said |
| `docs/ABRA-technical-docs.md` | ASD-STE100, Diátaxis headings — RULE / DEFECT / CONTROL / CORRECTION / RESULT / HOW TO REPRODUCE / WHAT IS STILL OPEN / WHAT WAS NOT RUN. One idea per sentence. Explicitly supersedes the 5.209.0 block's instruction "the gate must read OPEN, 8 of 8" |
| `docs/DAMAGE-STAGES.md` | damage-path reference: no stage moved and no damage number moved, and it says why that matters here — a damage error that only shows after a faint, a forced switch, or in the last third of a game is invisible where 944 of 961 games stop at the cap. The per-band direct comparison stands on its own evidence; the whole-game reading no longer corroborates it |
| `docs/MODELS.md` | per-model ledger: no model changed, no fitted vector owed, and the quarantine governing every model here is **further from lifting than this ledger had been saying**. Re-run list ROADMAP #57 is not shorter today |
| `docs/SUMMARY.md` | whole-project block, plus three in-place corrections (below) |
| `docs/MEASURE.md` | division ledger: this is a ruler defect and it is this division's. Pin table + driver table, the retraction, why it was invisible (`status.js` already printed `driver policies the gate quotes — 1 of 2`), the three door-closing changes, red-before-trusted, and an OWED block |
| `docs/ENGINE.md` | division ledger: the engine did not get worse, the measurement of it got honest. No engine byte, no census figure, no mechanic moved. Names the shape of the missing evidence and carries the downstream note (ROADMAP #440's closeted perish row, Supreme Overlord 5 → 1) |

## Three in-place corrections in `docs/SUMMARY.md`

Two were forced by clause 3b(b) — the artifacts were republished under the document, so rows that
cite them by name stated figures the files no longer hold. The third is a current-state table cell.
All three are **marked superseded, not deleted**, following the convention this document already used
at 5.209.0.

1. the 5.209.0 block's `| whole-game differential | data/game-differential.json |` row — appended
   *"PRIOR reading, superseded at 5.243.0: it was taken on the census-coverage arm, where the games
   mostly do not end, and the artifact has since been republished from the empirical arm"*.
2. the 5.208.0 block's `| the same game on both engines |` row — same marker; and its
   `| staged mechanics, each one compared |` row citing `data/all-mechanics-fire.json` — marked
   superseded with *"read the current count out of the file rather than from this row"*. **No new
   count was typed for it**: the artifact was regenerated and printing state is not this pass's job.
3. `## The components at a glance`, the MEDICHAM **Status** cell. Was: *"the MEDICHAM gate is still
   shut against it, with five of its eight clauses WITHHELD rather than failing"*. Now: *"The
   MEDICHAM gate reads CLOSED — 1 of 8 clauses fail (2026-09-03). The whole-game clause is genuinely
   red on the empirical arm; the earlier 8-of-8 PASS was measured on a coverage-driver population
   where the games do not end, and is retracted."* That table is a CURRENT-STATE table, so a stale
   cell in it reads as a claim about tonight.

## What was deliberately NOT done

- **No `<!-- GENERATED -->` block was hand-edited.** `docs/MEASURE.md` and `docs/ENGINE.md` both carry
  one; both new sections were inserted **below** the closing `<!-- /GENERATED -->` marker, and the
  splicer asserted that ordering before writing. Both blocks are stamped `2026-09-01 17:21` and are
  one pass behind. `node engine/status.js --write` is in OWED.
- **`data/docs-currency-baseline.json` was not edited.** The citation-mismatch ratchet had risen
  66 → 68 under the republished artifacts; it is back at exactly 66 by fixing documents, not by
  moving the floor.
- **No dated block was rewritten.** The 5.207.0 through 5.209.0 blocks in the whitepaper, the
  technical docs and SUMMARY stand as written, including their `8 of 8 PASS, OPEN` tables. They are
  superseded from above and, where a row cites a republished artifact, marked in place.
- **No number was authored.** Every figure came from the coordinator's verified list, which is the
  `## [5.243.0]` CHANGELOG block. Where a document needed a figure that was not on that list — the
  current `data/all-mechanics-fire.json` game count — the row says to read the file instead.
- **No quarantined figure is quoted anywhere in the new text.**

## Line endings

A bare-LF insert had to be repaired once already today, so this was measured before and after
rather than assumed. The splicer detected each file's native ending and matched it.

| file | CRLF before → after | bare LF before → after |
|---|---|---|
| `docs/ABRA-whitepaper.md` | 0 → 0 | 2124 → 2125 (file is natively LF) |
| `docs/ABRA-deck-plain-english.md` | 0 → 0 | 1444 → 1445 (natively LF) |
| `docs/ABRA-technical-docs.md` | 0 → 0 | 1764 → 1765 (natively LF) |
| `docs/DAMAGE-STAGES.md` | 0 → 0 | 972 → 973 (natively LF) |
| `docs/MODELS.md` | 0 → 0 | 2200 → 2201 (natively LF) |
| `docs/SUMMARY.md` | 0 → 0 | 1090 → 1091 (natively LF) |
| `docs/MEASURE.md` | 5649 → 5736 | **0 → 0** |
| `docs/ENGINE.md` | 38633 → 38665 | **115 → 115** |

**The six primary documents are natively pure LF on disk, not CRLF** — measured, byte by byte, before
anything was written. `git config core.autocrlf` is `true`, which is why the checked-out bytes are
LF and the committed bytes are not; matching the file's own on-disk ending is the correct action
either way, and every one of the six is still 100% LF with zero mixing.

`docs/MEASURE.md` is pure CRLF and stayed pure CRLF: **0 bare LF, before and after.**

`docs/ENGINE.md` is **mixed and already was**: 115 bare-LF line endings at lines 151–265, which is
the 5.241.0 "TWO DAMAGE-PATH ROWS" section written by an earlier pass. This pass inserted CRLF only
and added **zero** new bare LF; the count is unchanged. Per the reporting rule, that pre-existing
region is **reported and left alone** — it is somebody else's section and normalising it would be an
unrequested rewrite of their bytes.

## The gate

```
node tests/test-docs-current.js
DOC CURRENCY TESTS: 23 passed, 0 failed
```

Before this pass: 21 passed, **2 failed** —

- clause 2 (`every version-headed document is at 5.243.0`) named all six documents at 5.242.0;
- clause 3b(b) (`a figure attributed to an artifact is IN that artifact`) had risen from its baseline
  of 66 to 68, because `data/game-differential.json` and `data/all-mechanics-fire.json` were
  republished under rows in SUMMARY that cite them by name.

After: clause 2 reads `25 versioned, 19 pinned`, all current. Clause 3b(b) reads `baseline 66, now
66`. Clause 3b(c) (the untraceable census) is unmoved at `35 across 6 documents` — the new text adds
no untraceable figure, because every figure in it is recorded in `CHANGELOG.md`, which
`docs_scan.js` treats as a trace by design.

## Red elsewhere, pre-existing, stated rather than filed

Not caused by this pass and not touched by it: `tests/test-web-status.js` (12) and
`tests/test-web-quarantine-loaders.js` (2). Their payload was built 2026-08-25 declaring
`open:false` against an OPEN gate; they were red before this change, and WEB is under a declared
pause. Commands are in OWED.

## OWED

```
node engine/status.js --write
```
The `<!-- GENERATED -->` blocks in all five division ledgers are stamped `2026-09-01 17:21` and are
one pass behind. `docs/MEASURE.md`'s block still prints the pre-change gate; `docs/ENGINE.md`'s does
too. Until it runs, neither generated block may be read as current — the prose sections above it are.

**A decision, not a command — the owner's.** Move the whole-game clause from protocol
first-divergence (prints 167) to board-material (77 of 961). His 2026-08-22 call already makes
board-material the bar. It was not done here because changing the counted quantity in the same pass
that turned the gate red is indistinguishable from tuning.

```
node engine/feature_fixture.js --stamp data/policy-weights.json
```
**WILL'S CALL. NOT TO BE RUN.** Unchanged from 5.242.0 — it fuses two independent staleness causes,
and MAG is paused.

```
node tests/test-web-status.js
node tests/test-web-quarantine-loaders.js
```
Red and pre-existing (12 and 2). WEB is paused; these are recorded so they are not re-discovered as
new.

Not owed by anyone: no re-run of the differential, the roster, the census or the mechanics harness
is implied by this pass. Nothing was measured here.
