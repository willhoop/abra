# 7.0.0 fold-in — docs/MODELS.md (MEASURE)

Scope: `docs/MODELS.md` and `docs/MODELS.pdf` only. No other document, no engine file, no git, no games
played, `tests/test-docs-current.js` not run (shared baseline).

## What landed

- Header raised to `**Version 7.0.0 · Last updated 2026-09-11.**`
- A 7.0.0 head block, newest-first above the 2026-09-11 CORRECTION block, carrying the engine state read
  out of the artifacts and nothing else. Every figure sits in its own paragraph with its artifact and
  field named.
- A 7.0.0 MECHANICS STATE block at the head of the MEDICHAM section folding in the 27 notes rows since
  6.0.0: the engine fixes (CHANGELOG 6.5.0, 6.6.0, 6.7.0, 6.11.0, 6.12.0, 6.14.1, 6.18.0, 6.22.0,
  6.24.0, 6.26.0), the instrument changes (6.1.0, 6.2.0, 6.8.0, 6.9.1, 6.10.0, 6.13.0, 6.14.0, 6.22.0,
  6.24.0), the protocol-event count, the rollout-speed measurements (6.9.2, 6.12.2) and the interleave
  independence result (6.10.2, 6.14.1).
- A dated 7.0.0 status paragraph in each downstream model section: DODUO, MACHAMP, WOBBUFFET, DITTO,
  KADABRA, SLOWKING, MEW, MAG, MILTANK, GARY, PORYGON2. Each says the same thing — measured under an
  engine that has since changed, re-runnable when the gate reads OPEN, **not re-run** (Will,
  2026-08-11, *"Dont re run the artifacts weve bene over this"*; re-run list ROADMAP #57) — with MAG
  and MILTANK additionally marked as being reworked, and the MAG item stated as a REFIT, not a restamp.
- No model figure restored. No withdrawn figure restored. No strikethrough. Dated blocks untouched;
  every addition is a new dated block above them.

## The `<<GATE>>` placeholders

Two, both for the coordinator to fill from a verified run:

- `docs/MODELS.md:5` — head block: *"THE GATE READS `<<GATE>>` …"*
- `docs/MODELS.md:1323` — MEDICHAM: *"… WITH THE GATE VERDICT AT `<<GATE>>`"*

Everything depending on the verdict is phrased to survive either way ("becomes re-runnable when the
gate reads OPEN", "the verdict is at the head of this ledger"), so neither outcome makes a sentence
false.

Corroboration, not a substitute for the verified run: `node tests/test-docs-quarantine.js` (read-only,
run 2026-09-11T21:41Z) computed the withheld set live and printed *"THE GATE IS OPEN — nothing is
quarantined today"*, and passed all of its checks.

## Figures published, and what each is bound to

All read this session; none typed from memory.

| figure | binding |
|---|---|
| board-material 0 of 961; narration 0; void 0 of 961; cap 0 at `turns_cap` 50; boundaries 10,705 = 10,705; `first_board_divergences` empty | `data/game-differential.json`, release `534442d71183`, generated 2026-09-11T20:36:57Z |
| end state SAME-END-STATE 960 / DIFFERENT 0 / ENDED-APART 0 / THREW 1; `state.not_compared` 8 | same artifact, `end_state[0].summary.verdicts` |
| flags: `games` 961, arm `middle`, `pins.digest` de38d17e15a2, steering `empirical-click/v1`, `mode` string | same artifact |
| compared 6,000 / agreed 6,000 / disagreed 0; both multi-hit skips 0; `volley.move_rows` 130, `volley.bond_rows` 12, `unstageable` 1, `no_arrival` 4 | `data/engine-diff.json`, release `534442d71183` |
| roster 142 of 148, 139 of 200 (out-of-scope 116 = 114/1/1), 486 of 497; DIFFER 0, DID-NOT-FIRE 0; COULD-NOT-STAGE 6/159/11; CONTROL-NOT-QUIET 13; DEFERRED 0/5/3 | `data/roster.{items,abilities,moves}.json`, release `534442d71183` |
| census live 883 of probed 883, missing/unarmed/hollow/threw 0 | `data/mechanics-census.json`, generated 2026-09-11T21:07:09Z |
| harness 4,702 games, 0 threw; diverged moves 4 / abilities 1 / items 0 | `data/all-mechanics-fire.json`, release `534442d71183` |
| corner arms 961−960 and 961−959, protocol 12 and 15 | `data/verification/game-differential-{top,bottom}-tie-first.json`, release `b42b81899631`, and CHANGELOG 6.22.0 |
| 45 emitted protocol events (91 / 45 / 49 / 10) | `data/protocol-events.json` |
| copy cost ≈ a third of a turn, ≈ 6% of a rollout | CHANGELOG 6.9.2 (wording taken from the entry) |
| `battleTurn` 16.77% of samples, `norm` 7.58%, 300 games, release `13257c8bc397` | `data/verification/opt-plan-2026-09-11/profile-summary.json`, `phases.A1.self_top` |
| double wipe 43 of 43 → 0 of 43 → 43 of 43 under the knob | CHANGELOG 6.14.1; `data/verification/interleave-2026-09-11/interleave-aefcb93baf14.json` |
| staged mechanics 740 → 783 of 845; planner 841 of 847; refused pairs 420 → 43 → 0 | CHANGELOG 6.14.0, 6.9.1, 6.22.0, 6.24.0 |
| pool's last protocol divergence 1 of 961 → 0 of 961 | CHANGELOG 6.26.0 |

Every release id cited (`534442d71183`, `b42b81899631`, `aefcb93baf14`, `13257c8bc397`) is tracked —
29 files each under `data/releases/<id>`, checked with a read-only `git ls-files`, so provenance RULE 5
is satisfied for each published figure.

## Basis — CHANGED, and why

Declared in the head block. A reader can no longer be told that the lab's denominators mean what they
meant at 6.0.0 (scope is now decided by one module: abilities 200, moves 497, staged denominator 845,
against 201 / 498 / a different staging in the 6.0.0 block), that a mechanic is staged on a hand-picked
body (the harness and the roster now play derived legal fixtures), or that the gate's clause set is the
one 6.0.0 passed (twenty instruments registered afterwards, ten live defects found). Old and new
coverage series do not link — that is what makes this a MAJOR rather than a better number.

## Not done, and deliberately

- **No figure could not be bound.** Two I chose not to restate rather than bind loosely: the 6.12.2 row's
  18.1% / 16.9% / 13.0% aggregate shares, which are not in `profile-summary.json` in that form (the
  artifact carries per-phase `self_top` percentages, so I published `phases.A1` directly and named the
  phase); and the 6.6.0 conformance ratchet 21 → 5, whose CHANGELOG entry I did not read, so I omitted
  the figure rather than cite an entry I had not verified carries it.
- **Reported, not touched (`report it, leave it`):** the MAG section still prints figures that derive
  from `data/policy-weights.json` and from played games — the three-feature weight table (`speedSwing`,
  `screenValue`, `healValue`) with its CIs at roughly line 2036, the choice-lock 6.52% and 222 → 239
  switch events, the opponent-model 52.9% / 84% / 78%, the covariate-shift SE figures, and MEW's 51.0%
  mirror symmetry. `tests/test-docs-quarantine.js` does not accuse any of them today because the gate
  computes nothing withheld, and removing them is a judgement outside this brief. If the coordinator
  wants them out, it is a separate pass.
- ROADMAP #577's four hard-arm figures are already absent from this file (`million-run`,
  `mutation-coverage` and `search-decision-profile` appear only in dated prose about instrument
  defects, with no figure resting on an untracked release), and 6.4.0's owed `2.92%` is already gone.
  The 6.18.0 emitted-event correction (44 → 45) is already correct in this file.

## Build

`cmd //c tools\lownode.cmd build\md_to_pdf.js docs\MODELS.md` → `wrote docs\MODELS.pdf (2310 KB) via
chrome.exe`, exit 0. The first attempt with `cmd /c` printed only a shell banner and did not run — Git
Bash damaged the flag; `//c` is the working form.
