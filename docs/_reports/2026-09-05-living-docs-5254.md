# Living-docs pass for CHANGELOG 5.254.0 — what landed, what was checked, what is owed

Documentation only. No game was played, no engine was run, nothing under `engine/` or `tests/` was
touched, `docs/ROADMAP.md` was not touched, `data/docs-currency-baseline.json` was not touched, and
nothing was committed.

## FILES BROUGHT CURRENT

| file | header | what was added |
|---|---|---|
| `docs/ABRA-deck-plain-english.md` | 5.253.0 → **5.254.0**, 2026-09-05 | 7 plain-English paragraphs |
| `docs/ABRA-technical-docs.md` | 5.253.0 → **5.254.0**, 2026-09-05 | 8 labelled blocks (DEFECT / EFFECT / ACTION / STATE / SCOPE) |
| `docs/ABRA-whitepaper.md` | 5.253.0 → **5.254.0**, 2026-09-05 | 11 paragraphs with the authority cited at each site |
| `docs/DAMAGE-STAGES.md` | 5.253.0 → **5.254.0**, 2026-09-05 | 3 paragraphs: no stage moved, the instrument was re-run, and why the sun fix belongs on a damage page |
| `docs/MODELS.md` | 5.253.0 → **5.254.0**, 2026-09-05 | 4 paragraphs: distance to the gate, the frozen-input exposure, the refit |
| `docs/SUMMARY.md` | 5.253.0 → **5.254.0**, 2026-09-05 | artifact table + 4 paragraphs |
| `docs/MEASURE.md` | unversioned ledger | new dated section, inserted BELOW the `<!-- /GENERATED -->` close and above the 5.253.0 section |
| `docs/ENGINE.md` | unversioned ledger | new dated section for the batch-7 / sun-freeze work, inserted between the `abra-tags` section and the batch-6 section, which is where it belongs in release order |

Nothing above a `<!-- /GENERATED -->` close was edited. `engine/status.js --write` was not run by
this pass; both generated blocks still stamp `2026-09-04 21:41`, from the engine pass that produced
this version.

## THE GAP THIS PASS FOUND IN THE LEDGERS

`docs/ENGINE.md` carried the batch-5 section, the batch-6 section and the `abra-tags` section, and
carried **no section for batch 7** — the sun/freeze fix that took board-material 40 → 37 and protocol
124 → 122. That work existed only in `docs/_reports/2026-09-04-fix-batch-7.md` and in the CHANGELOG.
It is now in the ledger, in release order, sourced from the report and from
`data/verification/fix-batch-7.json`.

## WHICH ARTIFACT HOLDS WHICH WHOLE-GAME FIGURE

Read out of the artifacts by this pass, not copied from prose:

| figure | artifact | value |
|---|---|---|
| board-material, PUBLISHED (what the gate prints) | `data/game-differential.json` | `961 - 915` = **46 of 961**, protocol 141 |
| board-material, batch 6 | `data/verification/fix-batch-6.json` | `961 - 921` = **40 of 961**, protocol 124 |
| board-material, batch 7 (tonight's engine) | `data/verification/fix-batch-7.json` | `961 - 924` = **37 of 961**, protocol 122 |
| stage-by-stage damage | `data/engine-diff.json` | release `ae608567e8a8`, 6000 requested, 6000 compared, 6000 agreed, **0 disagreed** |

Every document that states 37 also names `data/verification/fix-batch-7.json`, and every document
that states 46 also says `data/game-differential.json` was not republished. The whitepaper, the
technical docs, DAMAGE-STAGES, MEASURE and ENGINE each carry that pairing explicitly.

## DO THE GENERATED BLOCKS AGREE WITH THE PROSE ABOVE THEM

Checked, not assumed. Both blocks stamp `2026-09-04 21:41`.

- `docs/MEASURE.md` prints leaf calibration QUARANTINED, `MEDICHAM is not correct — 2 of 9 gate
  clauses fail`, both of them whole-game clauses; and `REFIT OWED` naming `data/abra-tags.js`
  (2026-09-04 21:38) among the inputs that moved after the fit. The new prose says the same three
  things, and says the block agrees.
- `docs/ENGINE.md` prints `829/829 probed mechanics live`, `0/6000 differential comparisons
  disagree`, and the two WITHHELD clauses. The new prose says the same. The block's `21:27` damage
  timestamp is `data/engine-diff.json`'s `2026-09-05T01:27:22Z` in local time, on release
  `ae608567e8a8` — the same release the prose names.

**One thing the blocks do NOT say, and the prose now does:** the generated blocks read the
UNREPUBLISHED `data/game-differential.json`, so they describe the 46 and never the 37. That is
correct behaviour, and it is why the artifact is named beside every figure.

## LINE ENDINGS — MEASURED, NOT ASSUMED

All eight documents are **100% CRLF** on disk. Measured before editing (`grep -c $'\r$'` equal to
`wc -l` in every file) and again after, so the inserted text is CRLF throughout and no file gained a
mixed ending. The claim that these files are LF-only is false.

## GATE

`node tests/test-docs-current.js` — **24 passed, 0 failed, exit 0.** It was 23/1 before this pass,
the one failure being clause 2 with all six version-headed documents at 5.253.0.

Neither ratchet moved and neither was allowed to move:

- cited-artifact mismatches: **66 before, 66 after** (no new entry)
- untraceable-figure census: **35 across 6 documents before, 35 after**
- `data/docs-currency-baseline.json` untouched; the run reports *no ratchet movement*

That was engineered rather than lucky. Every figure written this pass is either present in the
artifact the block cites, or is recorded in `CHANGELOG.md` under this version, and blocks that quote
the superseded published figure beside tonight's measurement say so in words the scanner recognises.

## NOTED, LEFT ALONE

The session scratchpad already contained draft blocks from earlier sessions (`alakazam.txt`,
`ops.txt`, `technical.txt`, `measure-section.txt`, `ops-section.txt`). They were not written by this
session, were not executed, and were not deleted.

## OWED

- **`data/game-differential.json` is not republished** and still holds board-material 46 of 961. Every
  document says so, but the gate will keep printing 46 until a settled-tree pass rewrites it. That is
  a MEASURE job and it is not done.
- **`engine/tag_dex.js` does not rebuild `data/abra-tags.js` in the same act that writes its source.**
  Deliberately deferred by the engine pass: verifying it needs a `tag_dex` run, which would strand
  release `ae608567e8a8` and void the five re-runs completed on it.
- **The MAG refit stays OWED and is a REFIT, not a restamp** — and this version adds a reason rather
  than removing one, since `data/abra-tags.js` is now named among the inputs that moved after the fit
  and is frozen into every release a measurement is taken against.
- **The PDFs were not rebuilt.** `docs/ABRA-whitepaper.pdf`, `docs/ABRA-deck-plain-english.pdf`,
  `docs/ABRA-technical-docs.pdf` and `docs/SUMMARY.pdf` are one version behind their `.md`.
- **Nothing is committed and nothing is pushed.** Local disk only, by instruction.
- **`node engine/status.js --write` was not run by this pass**, so the generated blocks are current to
  the engine pass rather than to this one. They carry no figure this pass authored.
