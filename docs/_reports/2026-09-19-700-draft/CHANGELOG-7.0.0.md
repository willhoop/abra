# DRAFT — the `## [7.0.0]` entry publishing would add to `CHANGELOG.md`

Not in the real file. Paste it above the top entry (`## [6.69.0]` today) only when Will says publish. Every
figure was read on 2026-09-19 from the artifacts on release `6180c4712761` (HEAD `165b338c`), or printed by
`node engine/quarantine.js`, `node engine/coverage.js` and `node engine/open_work.js`, all run through
`tools\lownode.cmd`.

---

## [7.0.0] — 2026-09-19

### Changed
- **THE MEDICHAM GATE READS OPEN ON THREE TEAM LATTICES, WITH THE BLIND-SPOT CLAUSES IN FORCE —
  `GATE: OPEN — MEDICHAM passes both conditions; nothing is withheld`, all ten clauses PASS on release
  `6180c4712761` — AND THIS IS A MAJOR BECAUSE THE BASIS MOVED.** Every gating clause passes, including
  the blind-spot clauses (6.64.0, 6.66.1, 6.67.1); that is not a claim that MEDICHAM is correct, and the
  limits are under Notes. 6.0.0 read one team lattice; 7.0.0 requires zero on three chosen to share few
  teams (6.45.0, 6.46.0). BOARD-MATERIAL (`state.games` less `state.games_board_never_diverged`) reads
  0 of 961, 0 of 1069 and 0 of 1497 at `--games` 1200, 1350 and 1950 (`data/game-differential.json`,
  `data/game-differential.g1350.json`, `data/game-differential.g1950.json`); `threw` 0 / 0 / 0; undeclared
  NARRATION 0 / 0 / 0 (raw 0 / 1 / 2, all the declared Supreme Overlord `fallenundefined` row); turn
  boundaries 10,716, 11,856 and 16,725, all identical; none void, none cut off at `turns_cap` 50. Pins:
  census `57deebd09273`, driver `faf70ecbca71`, pools `0d103fb9fa87` / `7e7a37ded7fc` / `a5ce76242f8d`,
  arm `middle`, `empirical-click/v1`. `data/engine-diff.json` 0 of 6000 at the midpoint and all 16 other
  indices. Roster 148 of 148, 194 of 200, 494 of 497, 0 DIFFER, 0 DID-NOT-FIRE, 0 COULD-NOT-STAGE,
  122 red demonstrations CAUGHT (22 / 63 / 37). `data/mechanics-census.json` 969 live of 969.
  `data/all-mechanics-fire.json` 4638 games, 0 threw; abilities FIRED 198 of 200 in scope, moves 497 of
  497 resolved on both engines, items 148 of 148; 42 live-control credits, all earned; 2 of 841 control
  arms parted, none on a board.
- **The living-document set is folded in from the notes rows 6.0.1 to 6.69.0** (92 owed before the pass,
  `node engine/docs_scan.js --owed`). The white paper, the deck, the technical docs, `docs/SUMMARY.md` and
  `docs/MODELS.md` carry a 7.0.0 block and a 7.0.0 header.
- **NOTHING DOWNSTREAM WAS RE-RUN, AND NOTHING DOWNSTREAM IS RESTORED.** The open gate makes the 69 of 261
  artifacts `node engine/quarantine.js` lists as downstream RE-RUNNABLE, not true. By Will's instruction
  (2026-08-11: *"Dont re run the artifacts weve bene over this"*) MAG, MILTANK, R1–R4, leaf calibration and
  every rollout-reading model figure stay absent from the documents with their generator named.

### Notes
- **What the gate does not cover, stated in all five documents:** Illusion is closeted and unmodelled;
  **Frisk** (DID-NOT-FIRE; the fixture's foes hold no item) and **Axe Kick** (the staged board parts on the
  confusion counter, 2 against 1) are OPEN and being fixed, excused only by the owner's deferral and the
  reach shelf; the sample is three lattices of 961 / 1,069 / 1,497 games from one frozen open-sheet pool,
  capped at 50 turns, one driver, with a rule-of-three bound of about 0.20% per game on the largest;
  closed-sheet and bo1 play is not sampled; 0 of 17,536 bodies on a real spread; 76 effects print nothing
  when they fire; 23 of 24 uncomparable leaves are covered only by other instruments and one by none;
  860 of 901 entities exercised in a real game; 0 of 8 ranged mechanics fully staged by an arm.
- **Known and open, named by no register row:** Coaching's `-fail` after `[notarget]` (6.57.0 notes row).
  It parts no lattice game.
- **The corner arms were not re-measured**; their last reading is on `b42b81899631` (6.22.0).
