# DRAFT — the `## [7.0.0]` entry publishing would add to `CHANGELOG.md`

Not in the real file. Paste it above `## [6.61.0]` only when Will says publish. Every figure was read on
2026-09-19 from the artifacts on release `4c9b0cc4a4da` at HEAD `e85e48c8`, or printed by
`node engine/quarantine.js`, `node engine/coverage.js`, `node engine/open_work.js` and
`node engine/docs_scan.js --owed`.

---

## [7.0.0] — 2026-09-19

### Changed
- **THE MEDICHAM GATE READS OPEN ON THREE TEAM LATTICES — `GATE: OPEN — MEDICHAM passes both conditions;
  nothing is withheld`, all nine clauses PASS on release `4c9b0cc4a4da` — AND THIS IS A MAJOR BECAUSE THE
  BASIS MOVED.** 6.0.0 read one team lattice; 7.0.0 requires zero on three chosen to share few teams
  (6.45.0, 6.46.0). BOARD-MATERIAL (`state.games` less `state.games_board_never_diverged`) reads 0 of 961,
  0 of 1069 and 0 of 1497 at `--games` 1200, 1350 and 1950 (`data/game-differential.json`,
  `data/game-differential.g1350.json`, `data/game-differential.g1950.json`); undeclared NARRATION reads
  0 / 0 / 0 (raw 0 / 1 / 2, all the declared Supreme Overlord `fallenundefined` row); turn boundaries
  10,705, 11,842 and 16,713, all identical; none void, none cut off at `turns_cap` 50. Pins: census
  `1298f25115e3`, pools `0d103fb9fa87` / `7e7a37ded7fc` / `a5ce76242f8d`, arm `middle`, `empirical-click/v1`.
  `data/engine-diff.json` 0 of 6000 at the midpoint and all 16 other indices. Roster 148 of 148, 194 of 200,
  494 of 497, 0 DIFFER, 0 DID-NOT-FIRE, 0 in-scope COULD-NOT-STAGE, 122 red demonstrations CAUGHT
  (22 / 63 / 37). `data/mechanics-census.json` 955 live of 955. `data/all-mechanics-fire.json` 4700 games,
  0 threw.
- **The living-document set is folded in from 81 notes rows** (6.0.1 to 6.61.0; `node engine/docs_scan.js
  --owed` before the pass). The white paper, the deck, the technical docs, `docs/SUMMARY.md` and
  `docs/MODELS.md` carry a 7.0.0 block and a 7.0.0 header.
- **NOTHING DOWNSTREAM WAS RE-RUN, AND NOTHING DOWNSTREAM IS RESTORED.** The open gate makes every
  artifact `node engine/quarantine.js` lists as downstream RE-RUNNABLE, not true. By Will's instruction
  (2026-08-11: *"Dont re run the artifacts weve bene over this"*) MAG, MILTANK, R1–R4, leaf calibration and
  every rollout-reading model figure stay absent from the documents with their generator named.

### Notes
- **Not covered by the verdict, printed by `node engine/coverage.js`:** no differential body plays a real
  spread; 852 of 887 entities exercised in a real game; 0 of 8 ranged mechanics fully staged by an arm;
  one driver.
- **Four lattice games THREW** (1 / 1 / 2): the authority refused a disabled move the empirical driver
  drew. Their boards agreed up to the stop, so they count as never diverged. A driver defect, not an
  engine one; not fixed.
- **Known and open, named by no register row:** Coaching's `-fail` after `[notarget]` (6.57.0 notes row);
  Tidy Up's order and missing `-activate`, and `-unboost|0` at −6 (6.59.0 notes row). None parts a
  lattice game. Axe Kick's staged fixture parts a board on the confusion counter; the move is deferred
  by the owner below the usage shelf.
- **The corner arms were not re-measured**; their last reading is on `b42b81899631` (6.22.0).
