# Living docs brought to 5.256.0 — the charged move that struck the wrong slot

Dated findings record. Not maintained, not current state, superseded by whatever the gate prints.
Documentation-only pass: no game was played, nothing under `engine/` or `tests/` was touched,
`docs/ROADMAP.md` was not touched, `data/docs-currency-baseline.json` was not touched, nothing was
committed.

## What moved

| file | change |
|---|---|
| `docs/ABRA-whitepaper.md` | version 5.255.0 → 5.256.0; nine new blocks above the 5.255.0 material |
| `docs/ABRA-technical-docs.md` | version bumped; eight new ASD-STE100 blocks |
| `docs/ABRA-deck-plain-english.md` | version bumped; nine new plain-English blocks |
| `docs/DAMAGE-STAGES.md` | version bumped; three blocks — nothing on the damage chain moved |
| `docs/MODELS.md` | version bumped; six blocks — no model changed, quarantine unchanged |
| `docs/SUMMARY.md` | version bumped; a new question/artifact/answer table plus seven blocks |
| `docs/MEASURE.md` | new section, no version header on this file |
| `docs/ENGINE.md` | new section, superseding the `vol.charging` paragraph below it from above |

Every insertion is above the previous version's material. No dated block was rewritten. No
`<!-- GENERATED -->` block was hand-edited — asserted mechanically, not by eye: every diff hunk was
compared against every `GENERATED`/`/GENERATED` line range in the eight files and all eight are clear.

## The headline, as it was written

`vol.charging`'s release turn aimed at `live(foes)[0]` where the authority replays the `targetLoc`
stored on the sub-volatile, so a Phantom Force charged at slot b struck slot a. It was **invisible by
construction**: the driver in use until this version resolved every foe-aimed click with the lowest
live index for both slots, so re-aiming was a no-op and a correct implementation and the defect
produce identical games. The driver's focus-fire bug was hiding a real targeting defect. The claim
written on the card — the charge wrapper surviving a BeforeMove refusal, 2 times in 961 games — was
the minor half.

Every document names the arm on every figure and says that `arms_comparable` refuses to pair them:
joint board-material 110 → 53, protocol 191 → 138, VOID 38 → 4; empirical 35 → 34, protocol 120 → 121.
`data/game-differential.json` still holds 46 and is what the gate prints, stated as stale in every
document that quotes it. The controls are in the text of all eight: the knob-cleared runs reproduce
110 and 35 exactly, `drift` says only `medicham2-browser.js` moved, so the whole 57-game movement is
these two fixes and nothing else. The three method findings (the one-hour-old fixture, the second
driver change in one night to expose rather than cause a defect, the pin-arms assertion that had only
ever passed by luck) and the honest column (3 of 8, both empirical misses by one, all three joint
misses in the same direction) are carried in each document in that document's voice.

## No number was authored

Every figure came out of the `## [5.256.0]` block. Four of them were additionally verified against
the artifacts on disk before being written, and they hold exactly:

| artifact | `games` | `state.games_board_never_diverged` | board-material | `diverged` | `mid_void.void_games` |
|---|---|---|---|---|---|
| `data/verification/charge-fixture-joint.json` | 961 | 908 | **53** | 138 | 4 |
| `data/verification/charge-fixture-joint-knobs.json` | 961 | 851 | **110** | 191 | 38 |
| `data/verification/charge-fixture-empirical.json` | 961 | 927 | **34** | 121 | 4 |
| `data/verification/charge-fixture-empirical-knobs.json` | 961 | 926 | **35** | 120 | 4 |

110 − 53 = 57, which is the movement the controls attribute. The knob-cleared figures are the
pre-fix figures exactly, in both arms.

## How the drafts were checked before insertion

A scratch pre-checker called `engine/docs_scan.js`'s own shipping functions — `paragraphs`,
`citationsIn`, `artifactNumbers`, `artifactHas`, `figuresInText`, `isUniversal`, `QUALIFIED`,
`untraceableCensus` with its injectable `read` — on each draft before it went near a document, plus a
per-line backtick-parity count. It was shown able to go red first, on a planted draft: it caught a
bogus `87654`, an unpaired backtick and reported the qualified citation block, and returned clean on
the real drafts. Every added line has even backtick parity. Line endings were measured per file
rather than assumed: all eight are uniformly CRLF and remained so, which `git diff --stat` confirms —
each file shows only the lines actually added.

The citation rule was satisfied rather than dodged: the blocks that name `data/game-differential.json`
say the published figure is stale, which is the `QUALIFIED` escape, and their figures (46, 961, and in
one case 53/138/34/121) are present in that artifact anyway. `data/mechanics-census.json` was checked
to contain 829 before the SUMMARY row citing it was written.

## The gate

`node tests/test-docs-current.js` — **24 passed, 0 failed, exit 0.** It was red before this pass on
exactly one clause, naming the six stale documents; that clause now reads *"every version-headed
document is at 5.256.0 or is a declared pin (25 versioned, 19 pinned)"*. No ratchet moved: undeclared
unversioned 56, archive 25, retraction violations 8, citation mismatches 66, untraceable census 35
across 6 documents — all identical to the pre-pass run. The baseline file was left untouched and says
so in the run's own footer.

## OWED — not discharged by this pass

- **`node engine/status.js --write` was not run**, so every `<!-- GENERATED -->` block in
  `docs/MEASURE.md`, `docs/ENGINE.md` and `docs/MODELS.md` is still stamped `2026-09-04 23:52`. The
  brief forbade it. It is owed and it is owed again for the same reason it was owed last pass.
- **Leaf calibration stays WITHHELD.** `data/winrate-backtest.json` is downstream of MEDICHAM, the
  gate is shut, and no reliability curve may be published through an engine whose boards still part.
  The standing brief is not discharged by a documentation pass and this document does not pretend it is.
- **The MAG refit stays OWED, and it is a refit rather than a restamp** — the simulator moved again.
- **The noise floor is still unmeasured, now across two arms.** Nothing here states the spread between
  two halves of one arm, which is why the empirical arm's one-game move is not reported as an effect.
- **Engine items carried into the ledgers and not closed by this pass:** one new control-arm board row
  attributed and not diagnosed; 13 of the joint arm's 53 unnamed under the artifact's 40-row cap; the
  semi-invulnerable half of the abort fix wired and not staged; `tests/test-wiring.js` a pre-existing
  red that plays self-play games and was not re-run.
- **Nothing was committed**, per the brief. All eight files are modified on disk only.
