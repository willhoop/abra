# 6.68.0 re-measured on `6180c4712761` — 2026-09-19, ENGINE (measure only)

This is a findings record, not a living document. The register rows it feeds supersede it, and it is not cited
as current state. `node engine/status.js` and `node engine/quarantine.js` hold the current state.

---

## 0. VERDICT

- **GATE: OPEN.** `node engine/quarantine.js` prints `GATE: OPEN — MEDICHAM passes both conditions; nothing is
  withheld`, and all 10 gating clauses PASS. It ran once, after every artifact below had been written, with no
  other writer live.
- **Whole game: board-material 0 / 0 / 0, undeclared narration 0 / 0 / 0, threw 0 / 0 / 0** at `--games`
  1200 / 1350 / 1950. The only protocol divergences are the three declared Supreme Overlord `fallenundefined`
  games. They are the same games, at the same indices, as on `d92bdfb50d88`.
- **Staged-game battery:** every in-scope row is proven or owner-excused. Unearned FIRED is **0**. Control-arm
  board partings are **0**.
- **No code changed.** I declared no version beyond the brief's 6.69.0 (MINOR, **Basis.** unchanged). Under
  `CLAUDE.md`, the gate opening is the archetypal MAJOR, and Will holds that decision (the 7.0.0 draft is held).

## 1. PINS

| pin | value |
|---|---|
| release | `6180c4712761`, first cut 2026-09-19T19:26:51Z, HEAD `666d0ddb` (6.68.0). `engine_release.js list`: `0 of 27 files have moved since`. `cuts.jsonl` still holds 1 event after every run (no phantom cut) |
| census | For the lattices and the battery: `data/mechanics-census.json` = HEAD blob `a5c57cd2`, `sha12Content` **`57deebd09273`**, **969 live / 969 probed / 0 missing**, `run_ok: true`. All three lattices stamp `steering.input_digest` `57deebd09273`. Step 3 regenerated the census **after** those runs (969 / 969 / 0). The new file digests to `05f1485c064f`: only `generated` and the stochastic detail text of the Iron Head flinch row changed |
| team store | `--team-store data/team-pool-frozen`, pools `0d103fb9fa87` / `7e7a37ded7fc` / `a5ce76242f8d` (picked 1968 / 2206 / 3069 of 8778), the same as `d92bdfb50d88` |
| driver | `steering.driver_code.digest` **`faf70ecbca71`** over 12 files, and each run printed that it held still. The last lattice run used `d0ef2b6bf52e`. **The digest moved (6.65.1), so no earlier lattice artifact is comparable** |
| flags | `--release 6180c4712761 --team-store data/team-pool-frozen --steering empirical --arm middle --end-state --games {1200,1350,1950} --write --out <canonical path> --dump-games 400 --dump-out data/_6180_dump_g<N>.json` (relative). `turns_cap` 50 in every artifact |
| mode | `A/middle/pins:de38d17e15a2/credit:observed-effect/v1/nature:real` |
| launcher | A node script I wrote in this session's scratchpad (`e6180/run.js`) calls `spawn(ComSpec, ['/c', 'tools\\lownode.cmd', ...argv])`, the argv route that `tools/lownode.cmd`'s header documents. There is no PowerShell tool in this session. The first launch failed at spawn: the Bash heredoc stripped the backslashes. Nothing ran and no artifact was touched. The retry was written with the Write tool |

## 2. LATTICES

| `--games` | games | board-material | narration undeclared (raw) | protocol-diverged | threw | boundaries identical | generated | wall |
|---|---|---|---|---|---|---|---|---|
| 1200 | 961 | **0** | **0** (0) | 0 | **0** | 10716 / 10716 | 19:45:43Z | 497 s |
| 1350 | 1069 | **0** | **0** (1) | 1 | **0** | 11856 / 11856 | 19:47:24Z | 599 s |
| 1950 | 1497 | **0** | **0** (2) | 2 | **0** | 16725 / 16725 | 19:48:51Z | 686 s |

- Board-material is `state.games − state.games_board_never_diverged`.
- Narration is quarantine's NARRATION clause.
- Every `generated` stamp moved from 16:28–16:30Z (`d92bdfb50d88`) to the times above.

**Every protocol divergence, from the full `--dump-games` output.** The dumps held 1 of 1 and 2 of 2 diverging
games. The 1200 lattice wrote no dump because nothing diverged. I moved both dumps to the scratchpad.

| lattice | config | seed (first) | agreed lines | authority | ours | mechanism |
|---|---|---|---|---|---|---|
| 1350 | pair-speedctrl | `…2661010853` | 70 | `\|-end\|p1b: Kingambit\|fallenundefined\|[silent]` | `\|-damage\|p2b: Garchomp\|35/183\|[from] item: Life Orb` | declared Supreme Overlord `fallenundefined` |
| 1950 | omit-weather | `…2663628673` | 34 | `\|-end\|p1a: Kingambit\|fallenundefined\|[silent]` | `\|switch\|p1a: Delphox\|…` | the same |
| 1950 | pair-redirect-priority | `…2656799052` | 23 | `\|-end\|p2a: Kingambit\|fallenundefined\|[silent]` | `\|switch\|p2a: Sableye\|…` | the same |

All three games end the battle on both engines, and the board holds. **No game parted a board, so there was
nothing to bucket.**

## 3. THE STAGED-GAME BATTERY (`data/all-mechanics-fire.json`)

Command: `--release 6180c4712761 --kind all --team-store data/team-pool-frozen --write`. The artifact was
generated at 19:38:03Z (it was 16:31:26Z on `d92bdfb50d88`). **4,638 games, 0 threw, 0 sheets unassembled,
`red_ok: true`.**

| kind | in scope | verdicts |
|---|---|---|
| abilities | 200 | **FIRED 198**, DID-NOT-FIRE **1** (Frisk), UNPROVEN-UNCONTROLLED **1** (Illusion), SHOWDOWN-ONLY 0, MEDICHAM-ONLY 0 |
| moves | 497 | resolved 497 of 497 on the authority and **497 of 497 on MEDICHAM** (the 8 unresolved on `d92bdfb50d88` all read `medicham_resolved: true`); diverged 0 unshelved; 2 shelved and diverging (Bitter Malice, Night Daze: Illusion carriers); board STATE 1 (Axe Kick, below the reach shelf) |
| items | 148 | **FIRED 148**, all boards NO-DIVERGENCE |

**Control watch** (`summary.abilities.control_watch`):
- `fired_on_live_control_unearned`: **[] (0)**.
- 42 FIRED rows rest on a live control, and all 42 are earned by the subject's own authority receipt (40 state, 2 log).
- `fired_on_loud_control`: Gale Wings, Magic Guard, Mirror Armor, Slush Rush, all earned.
- `control_not_quiet`: 152 (was 153).
- The legacy chooser: 13 rows, and the first alternative was quiet in all 13.

**Control-arm partings** (`summary.control_arm_partings`):
- 841 rows have a control arm. 2 parted and **`board_material` is 0**.
- Both partings are ANNOUNCEMENT-ONLY: `moves:bittermalice` and `moves:nightdaze`. Both rows are shelved by the
  owner, because their only carriers hold Illusion.

**In-scope rows that are not proven, are unearned, or part a board in either arm:**
- **Frisk: DID-NOT-FIRE**, refined CANNOT-FIRE-IN-THIS-FIXTURE. The gate excuses it as DEFERRED-BY-OWNER
  (`tests/roster.js DEFERRED`). **The regression is in the instrument's old credit, not in the engine.**
  - On `d92bdfb50d88` the control was Cursed Body. Frisk's FIRED there was Cursed Body's `vol.disable`, which
    moved on both boards.
  - The quiet-control preference now picks Insomnia, and nothing moves on either engine.
  - The authority announces Frisk only for a foe that holds an item (`data/abilities.ts:1538`
    `if (target.item)`; the Champions mod does not override it). The fixture's foes (Feraligatr, Aegislash)
    carry no item.
- **Illusion: UNPROVEN-UNCONTROLLED.** Closeted by Will (ROADMAP #160), as expected.
- **Axe Kick: board STATE on the subject arm.** `p2a feraligatr vol.confusion` reads 2 on the authority and 1
  on ours, with no line difference. This is unchanged. It sits below the reach shelf (2 clicks in 64,846
  stored games), and the gate judges it there.
- **No other row.**

**The `d92bdfb50d88` owed items, on this battery:**
- Natural Cure reads FIRED on the planner against Cloud Nine, with both boards NO-DIVERGENCE.
- The eight moves resolve on both engines.
- Hyper Cutter's control is now Iron Fist, not Anger Point, so **this battery no longer stages the Anger Point
  case** (Anger Point with a secondary drop in the same hit). I ran `tests/probe_hit_event_buff_order.js` on
  this release instead. It reads PASS: 221 pairs staged, 0 failing, 37 red proven, 71 controls held.
- Magma Armor's control is now Solid Rock, and its row reads NO-DIVERGENCE in both arms.

## 4. ROSTER, DAMAGE, CENSUS, COVERAGE

| artifact | before | after | reading |
|---|---|---|---|
| `data/roster.items.json` | 16:32:33Z `d92bdfb50d88` | 19:49:32Z `6180c4712761` | 148 MATCH, 0 DIFFER / DID-NOT-FIRE / COULD-NOT-STAGE / CONTROL-NOT-QUIET. Anchors 22 of 22. **Reds 22 of 22 CAUGHT.** Exit 0 |
| `data/roster.abilities.json` | 16:33:14Z | 19:51:36Z | 194 MATCH, 6 DEFERRED-BY-OWNER, 0 otherwise. Anchors 63 of 63. **Reds 63 of 63 CAUGHT.** Exit 0 |
| `data/roster.moves.json` | 16:35:28Z | 19:53:14Z | 494 MATCH, 3 DEFERRED-BY-OWNER, 0 otherwise. Anchors 37 of 37. **Reds 37 of 37 CAUGHT.** Exit 0 |
| `data/engine-diff.json` | 16:39:48Z | 19:53:14Z `6180c4712761` | `--n 6000`, seed 20260804: requested 6000, compared 6000, **0 disagreed at the midpoint, top, bottom and idx01–idx14. Published** (written to the canonical path; `data/verification/` untouched). 1 row dropped by exception: the same Dragon Darts authority throw |
| `data/mechanics-census.json` | 19:26:33Z | 19:54:42Z | **969 live / 969 probed / 0 missing**, `run_ok: true`. No change from HEAD in any row's verdict |

Each roster stage's fixture check printed some sets as illegal and not baselined: items 17 of 479, abilities 94 of
703, moves 57 of 791. The battery's check printed 0 of 102. I did not check whether the roster figures are new.
They are listed under OWED.

**`node engine/coverage.js`:**

| row | value (was, on `d92bdfb50d88`) |
|---|---|
| board leaves compared | 56 (56); 0 uncompared leaves can stand at a boundary |
| uncomparable leaves with a firing writer | 23 of 24 (23 of 24); the missing one is `volatile:powershift` |
| move leaves whose EFFECT was exercised | 7 of 11 (7 of 11) |
| staged mechanics that fired | 843 of 845 (843 of 845): moves 497/497, abilities 198/200, items 148/148 |
| fired mechanics with a board compared | 843 of 845 (843 of 845) |
| tags with an engine consumer | 299 of 301 (299 of 301) |
| turn boundaries compared | 10716 (10716) |
| entities exercised in a real game | **860 of 901** (859 of 899) |
| differential bodies on a REAL spread | 0 of 17536 |

## 5. THE GATE, CLAUSE BY CLAUSE

`node engine/quarantine.js` (HEAD bytes), exit 0:

| clause | reading |
|---|---|
| game differential (damage) | PASS: midpoint 0 of 6000, top 0/6000, bottom 0/6000, idx01–idx14 0/6000 |
| deliberate roster / items | PASS: 148 of 148 tested |
| deliberate roster / abilities | PASS: 194 of 200 tested |
| deliberate roster / moves | PASS: 494 of 497 tested |
| coverage / every used mechanic is measured | PASS: all 412 moves above 25 clicks |
| board leaves / nothing uncompared at a boundary | PASS: 0 standing, 56 compared, 1 `BS.snapshot` call site |
| whole-game BOARD-MATERIAL | PASS: `ZERO ON EVERY LATTICE`, 0/961, 0/1069, 0/1497 |
| whole-game NARRATION | PASS: `ZERO ON EVERY LATTICE` (raw 0 / 1 / 2, all declared) |
| mechanics / each staged and compared | PASS: 0 diverge (+1 board-only, Axe Kick, below the reach shelf). PROOF: every in-scope row proven. Owner-excused 2 (`ability:frisk` DEFERRED-BY-OWNER, `ability:illusion` closet). CONTROL WATCH: 42 live, 42 earned, 0 not earned. CONTROL ARMS: 2 of 841 parted, none on a board |
| no open, known engine defect | PASS: 194 verdicts read, 11 NOT-A-DEFECT rows excused |

The printout then lists **69 of 261** downstream artifacts as RE-RUNNABLE, not current (ROADMAP #57).

`node engine/status.js --write` restamped the five ledgers. Its first lines print `FEATURE SEMANTICS CHECK FAILED`
on `data/policy-weights.json`, because the fixture changed. That is MAG, which is paused and waived, not
MEDICHAM. I report it and leave it alone.

## 6. FILES CHANGED

- **Code:** none.
- **Data, written by the runs:** `data/game-differential{,.g1350,.g1950}.json`, `data/all-mechanics-fire.json`,
  `data/roster.{items,abilities,moves}{,.prev}.json`, `data/roster.json`, `data/engine-diff.json`,
  `data/published-samples.json`, `data/mechanics-census.json` and `data/provenance-stamp.json`.
- **Docs:**
  - `CHANGELOG.md`: 6.69.0, Notes only.
  - `docs/RUNNING-NOTES.md`: one row, MINOR, **Basis.** unchanged.
  - `docs/ENGINE.md`: one section and its hand list.
  - The five ledgers, restamped by `status.js --write`.
  - This report.
- **White paper: not edited.** No current-tense figure in it is superseded. Every gate or whole-game figure sits
  in a dated block (6.0.0, and the 2026-09-11 and 2026-09-12 updates). The gate opening is a MAJOR's rewrite,
  not a figure swap.
- **Held docs (technical docs, MODELS, SUMMARY): not touched.** I scanned the INDEX text (`git show :<f>`), which
  equals HEAD for all three. Every gate figure there is inside a dated block (for example "At HEAD `bf2d594f` …
  2 of 9", "5.252.0 … THE GATE IS STILL SHUT"). Nothing needed replacing, so neither the index nor the working
  copy was edited. `docs/_reports/2026-09-19-700-draft/` was not touched.
- **Scratchpad:** before `e6180/`, I wrote `run.js`, `lat{1200,1350,1950}.{log,pid}` and `battery.{log,pid}` in
  the scratchpad root. **I overwrote `run.js` without first checking whether a file of that name existed.** The
  scratchpad holds other sessions' files (`run-head.js`, `run_lownode.js` and others), so an earlier `run.js`
  may have been replaced. Nothing was deleted.
- **Processes:** every process I started exited on its own. None was killed.
- **git:** no git command that writes was run.

## OWED, NOT RUN

1. **Earn Frisk's row.** Stage a foe that holds an item, so that `data/abilities.ts:1538` announces, against the
   quiet Insomnia control. It is owner-deferred, so it does not gate, but its old FIRED is gone.
2. **The gate is OPEN. Declaring the MAJOR is Will's call.** Under `CLAUDE.md` this is the archetypal basis
   change, and the notes row names it. I declared neither the major nor a basis change. The held 7.0.0 draft
   needs re-reading against this reading.
3. **`CHANGELOG.md` has lost its `## [6.65.0]` heading.** Commit `d7b784c7` (6.65.1) replaced the line
   `## [6.65.0] — 2026-09-19` with `## [6.65.1] — 2026-09-19`. 6.65.0's Notes now sit under 6.65.1. I did not
   repair it.
4. **`docs/ENGINE.md` carries `CHANGELOG <<VER>>` placeholders** in many section headings from worktree passes
   (for example 6.68.0's, "ALL ELEVEN UNEARNED ABILITY ROWS…"). They were not filled at merge. I did not touch
   them.
5. **Roster fixture-legality printouts.** Sets printed as illegal and not baselined: items 17, abilities 94,
   moves 57. Find out whether these are new since `d92bdfb50d88` and whether any fixture uses an out-of-format
   entity.
6. **Not claimed:** a census-pinned or driver-pinned before/after. The driver moved (`d0ef2b6bf52e` →
   `faf70ecbca71`) and the census moved (`9a62d9593f12` → `57deebd09273`). The census was then regenerated
   (`05f1485c064f`, identical rows) after the lattices ran.
8. **`tests/test-docs-current.js` reads 35 passed, 2 failed on the working tree.** The failing clauses are
   3b(b), a figure its cited artifact does not contain (baseline 18, now 29), and 3b(d), a figure bound to no
   trace. **All 23 NEW entries are in Will's held working-copy drafts:** technical docs 12, MODELS 5, deck 3,
   SUMMARY 3. They include `883` against a census that reads 969, `4,702` against a battery that reads 4638, and
   `487` against a roster that reads 494. The HEAD artifacts did not hold those figures either, so the entries
   predate this run. **None of the entries is in a file I edited** (CHANGELOG, RUNNING-NOTES, ENGINE.md).
   I did not run `--staged`, because I staged nothing. The hook reads the index, so it judges HEAD's text for
   the held docs.
7. **Anger Point with a secondary drop in the same hit** is now covered only by
   `tests/probe_hit_event_buff_order.js` (PASS on this release). The battery no longer stages it, because
   Hyper Cutter's control moved to Iron Fist.
