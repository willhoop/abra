# TWO ROWS FILED, ONE FENCE, AND #463 IS NOT A PREMATURE CLOSE

Register edits and this report only. **No engine byte moved, no artifact was hand-edited, nothing was
committed, and nothing that plays a game was run.** Every claim below was checked at the code, at the
Showdown source, at a git-historical artifact or at a derivation on the format before it was written
down. Both input reports were treated as evidence and both were found wrong on a fact of substance.

Files changed: `docs/ROADMAP.md` (three new rows #541/#542/#543, one dated addendum on #463), this
report. Side effect declared: `node engine/open_work.js` rewrote `data/open-work.json`, which is what
that tool does on every run.

**Reads were made safe.** `engine/medicham2-browser.js` is dirty in the working tree (another agent
owns it), so every engine read below is from the FROZEN COPY the measurement was taken on —
`data/releases/8ad06030e129/engine/medicham2-browser.js` — never from the live file.
`data/game-differential.json` has not moved (mtime Sep 3 22:16, run started 04:52). The two working
files the diagnosis pass left behind, `data/_diag77-sample.json` and `data/_diag77-cards.json`
(both 04:18), were read and **left in place**.

---

## 1. #463 — RIGHT ABOUT A NARROWER CLAIM. NOT A PREMATURE CLOSE.

**The brief's premise — "6 of 961 now, on the same pins" — is not true of the pin that decides it.**
The pool, the census, the arm and the 961 matchups are the same. **The DRIVER is not**, and the
driver is what selects the lines.

| artifact | date | `steering.policy` | release | boards never diverged | `stall` board-material |
|---|---|---|---|---|---|
| `ba7d92ba` (the close) | 2026-08-26 | **`census-coverage-seeking/v1`** | `667278050dcf` | 959 / 961 | **2** |
| `a969ba10` | 2026-08-29 | **`census-coverage-seeking/v1`** | `e129bca605e3` | **961 / 961** | **0** |
| `a347d6d0` (current) | 2026-09-04 | **`empirical-click/v1`** | `8ad06030e129` | 884 / 961 | **10** |

Checked rather than assumed: every `config|seed` pair in the `first_divergences` table of both older
artifacts (15 and 6 rows) is present in the current 961 — **the matchup population is identical**, and
`team_pool_digest 0d103fb9fa87` / `input_digest 9446a684709d` are identical in all three. Only the
action policy differs, and `empirical-click/v1` was introduced on 2026-08-29, **three days after this
row closed**.

**So no measurement available on 2026-08-26 read anything but 2, and the same clause read ZERO three
days later on the driver it was measured on.** A close that a later, different instrument reopens is
not a premature close; it is a claim whose scope was never written down.

**And the 6 are not one thing, nor 6.** Ten of the 77 carry an `active[].stall` leaf, not six — the
diagnosis report grouped them as M7 (4) + M9 (2) and left four riding inside larger cascades.
Directions: `medi 0 / sd 9` ×5, `medi 0 / sd 3` ×4, `medi 9 / sd 0` ×1.

- **Four are the ROLL, not the spend** → filed as **#543**. Each has a successful Protect on the
  victim's previous turn (both engines at counter 3), an `|move|<x>|encore` + `|-start|<victim>|encore`
  on the parting turn, and parts on `|-singleturn|<victim>|protect` against `|-fail|<victim>`. Three go
  the authority's way and one goes ours. Derived from `data/conditions.ts:439` (no Champions override):
  `onStallMove` runs `randomChance(1, counter)` and DELETES the volatile on a loss, `onRestart` triples
  it — **so a board reading 9 is the record of a shield that held and a 0 the record of one that
  failed.** The counter and the narration part together, at the roll. #463 decides WHEN a held counter
  is spent and cannot produce a disagreement about a roll.
- **Two are this row's own DECLARED, NOT FIXED residue.** `medi 0 / sd 3`, no protocol divergence,
  `end_reason: both engines ended the battle` — which is `turnEndedInResidual`, written into
  `_stallExpire`'s header by the fix itself. True without a reopen.
- **Four ride inside 6-, 17- and 23-leaf cascades** whose first leaf is a sleep, a placement or a forme
  divergence. Attributing those to `stall` would read a passenger as the driver.

**Action taken:** a dated `AUDITED 2026-09-04 BY MEASURE` addendum in #463's first cell. **The status
cell is untouched and the row stays CLOSED.** `roadmapRowIsClosed` = true and `roadmapRowSaysBroken` =
false, both unchanged. It carries no executable marker, so the `register_reality.js` trap that #403 hit
does not apply here — and the addendum deliberately avoids the literal marker string.

---

## 2. #541 — M8'S WRONG-BODY HALF, AND THE DIAGNOSIS NAMED THE WRONG CARRIERS

**Derived from the seven cards, not from the summary: Wandering Spirit 5, Mummy 2, the MOVE Skill Swap
ZERO.** `docs/_reports/2026-09-04-the-77-by-mechanism.md` reads *"Carriers: Skill Swap x2, Wandering
Spirit x2, Mummy x1"*. `Skill Swap` is the effect NAME `Battle#skillSwap` prints for an ABILITY-driven
swap. Anyone probing the move stages a mechanic that is not in this set.

**The authority.** `wanderingspirit.onDamagingHit` → `this.skillSwap(source, target)`
(`data/abilities.ts`; `/data/mods/champions/abilities.ts` does not mention it). `skillSwap`
(`sim/battle.ts:1311`) announces on the **SOURCE — the ATTACKER** — refuses when EITHER body is
`fainted`, honours `failskillswap`, and ends with **TWO** `singleEvent('Start', …)`. Mummy is
`source.setAbility('mummy', target)`, whose `mummy` switch case announces on the HOLDER with the victim
and `[ability] <old>` in fields 4 and 5, and ends with one `Start`.

**This engine, one site — WIRE 80, `rewritesAbilityOnContact`, `_rw.mode==='swap'`:**

```js
else if(_rw.mode==='swap'){const _t=m.ability;m.ability=tg.ability;tg.ability=_t;
  if(TR){TR.act(tg,'ability: '+_t);TR.ab(m,m.ability);TR.ab(tg,tg.ability);}}
```

**The two WRITES pick the right bodies. `TR.act(tg, …)` announces on the HOLDER where the authority
announces on the ATTACKER, and no `Start` is raised for either acquired ability at all.** 5 of 5, one
direction, under the differential's own class `-activate: a different body`. The `onStart` half is
board-material on its own card: the authority hands Runerigus an Intimidate and immediately drops both
p2 bodies' Attack; we announce and apply nothing.

**This INVERTS the brief's framing and the correction matters.** The brief expected *a bad SELECTOR
hands a CORRECT predicate the wrong body*. Here the selector for the WRITE is correct and the
ANNOUNCEMENT carries the wrong body — so a fix aimed at the write would move nothing.

**The board leaf is measured and NOT attributed, deliberately.** `p1.party.metagross.ability
medi=wanderingspirit sd=clearbody` runs OPPOSITE to the derivation: the authority printed
`|-activate|p1b: Metagross|Skill Swap|Wandering Spirit|Clear Body|[of] p2a: Lopunny`, which by
`skillSwap`'s own lines gives Metagross Wandering Spirit. Three live candidates, none tested — the
`POST_FAINT` hold in `board_state.js` (it skips `ability` only when BOTH engines call a body dead, while
`faintMessages` restores `ability = baseAbility` on the authority's corpse); the missing
`source.fainted || target.fainted` refusal, which IS reachable because `Pokemon#faint()` sets
`faintQueued` and **not** `fainted`; and the two absent `Start` events.

### `engine/side_selection_census.js` DOES NOT COVER THESE SITES

Asked explicitly, answered at the file. That census matches a **ternary** whose branches are a matched
`<name>A`/`<name>B` pair, or a `'p1'`/`'p2'` label pair — 102 sites, 81 UNCLASSIFIED, artifact dated
2026-08-29. The WIRE 80 site contains no ternary and **picks no side at all**: `m` and `tg` are
already-resolved bodies and the confusion is of **ROLE** — attacker against holder — inside a resolved
pair. That shape is outside the census's own stated limit, which names a far-side helper, index
arithmetic and a written-out filter, and does not include it.

---

## 3. #542 — M2 AS A FENCE, AND THE PROPOSED SPLIT DOES NOT SURVIVE THE ARITHMETIC

The register-corrections agent's judgement — the row is worth more as a fence — is endorsed and
executed. Its proposed split (index half = games 39/67, multiplier half = 17/18/32) is **wrong on both
halves**, and that is the fence's main content.

**The band, derived.** `Battle#randomizer` (`sim/battle.ts:2388`) is
`tr(tr(baseDamage * (100 - this.random(16))) / 100)` — sixteen indices, 85–100%, so the widest ratio one
base can produce is **100/85 = 1.1765**. Champions overrides the damage step
(`data/mods/champions/scripts.ts:226`) and calls the same `randomizer`.

**FOUR OF THE SEVEN ARE ONE MECHANISM AND IT IS FAIRY AURA.** `fairyaura.onAnyBasePower` is
`chainModify([… 5448, 4096])` = **×1.33008**, no Champions override, boosting every Fairy move on the
field from either side. Measured against HP-before read off the wire:

| hit | SD dmg | our dmg | ratio | the aura holder, that turn |
|---|---|---|---|---|
| Moonblast → Floette-Mega (149/149) | 75 | 57 | 1.316 | **switched IN** |
| Moonblast → Floette-Mega (140/149) | 58 | 43 | 1.349 | **switched IN** |
| Moonblast → Archaludon | 88 | 117 | 1.330 | **switched OUT** |
| Moonblast → Gengar | 34 | 45 | 1.324 | **FAINTED** |

Three are exact to the unit after truncation (57 × 1.33008 = 75.8 → 75; 88 × 1.33008 = 117.0 → 117;
34 × 1.33008 = 45.2 → 45); the fourth is one index out. **The sign flips with the door**, which is what
makes it one mechanism and not four: entry, exit, faint — the aura's field presence is untracked across
all three.

**The forme check the report demanded, done and the answer is no.** Derived on the format: `floettemega`
is legal and its **single ability IS Fairy Aura**; `floetteeternal` is legal; plain `floette` is `Past`
and ILLEGAL here. **Both legal formes have base HP 74, so a forme mix-up is SILENT on the HP leaf** —
and their Sp. Def is 128 against 148, worth 1.156×, which is not 1.33. Live second-order term, not the
gap.

**And the "index half" is one game, not two.** Beat Up into a Milotic that switched in at 170/170:
9 damage against 6 — **ratio 1.5, outside the band**, so a count or a per-hit base (#333's road), not a
roll. Gigaton Hammer → Lucario is 1.346 on a **Steel** move that no aura can reach: **UNATTRIBUTED**.
Only Vacuum Wave → Gardevoir, at a gap of 1, is index-compatible.

**What #333/#334 do NOT cover, stated on the row:** #333 is the multi-hit damage path never converted to
the sixteen-index roll; #334 is the confusion self-hit index. Both are about WHICH INDEX. Neither names
a whole-move multiplier and no register row does.

---

## 4. #543 — THE `stall` ROLL, WHICH `docs/ENGINE.md` CALLS FILED AND WHICH WAS NOT

`docs/ENGINE.md` (the 2026-08-29 `_armShieldGate` account) says of the four games left after the re-arm
fix: *"An opposite-sign pair on the same denominator is a DIE defect, not a gate defect … the counters
now agree, the draw does not. **Filed, not fixed.**"* **No register row named it.** `open_work.js` prints
0 UNREGISTERED because that detector sees only defects a live instrument counts, and nothing counts this.

**Two candidate causes and one of them is the instrument — the row leads with that, on #222's precedent.**
(i) the `!!this.queue.willAct()` short-circuit disagrees so one engine draws no die at all — **#232 is
CLOSED over this half** and settled which actions the queue scan counts, not a turn where both engines
pass the scan and disagree below it; (ii) the shared address misaligns — this engine draws `_R.stall()`
on its own stream (#222's split, correct), and the differential maps it into the authority's catch-all
bucket, `MID_ADDR_CAT = { stall: 'any' }`, because Showdown's `randomChance(1, counter)` executes inside
`stall.onStallMove` and matches none of acc/crit/sec/dmg. The `any` nth index is shared with every other
unclassified authority draw, and Encore is a site that takes one (`sim/battle-actions.ts:233`, #478).

**Nothing currently measures it:** the artifact's `unshared_address_shapes` and
`low_identity_by_category` carry acc/crit/sec/dmg and **no `stall` row at all**, and all ten stall games
sit in `shared-addresses-agree`, judged at an `overlap_floor` of 0.9.

---

## 5. GATE ARITHMETIC — CHECKED, NOT ASSUMED

- `node tests/test-roadmap-register.js` → **3 passed, 0 failed** (509 items, 317 ledger citations,
  5 declared exceptions).
- `openDefectClause()` called directly → **`ok = true`**, unchanged.
- `roadmapRowIsClosed` / `roadmapRowSaysBroken` on #463 → **true / false**, unchanged by the addendum.
- `open_work.js`: 503 → **506 register rows**, 72 → **74 assert breakage**, **0 UNREGISTERED**. All
  three new rows print.
- All three new rows are well-formed three-cell rows (exactly **4 unescaped pipes** each) and carry
  `INSTRUMENT OWED:` rather than any invented `VERIFIED BY`. None contains `NOT A DEFECT`.

---

## 6. WHAT IS OWED

- **`node engine/register_reality.js` WAS NOT RUN, and it is not safe under this brief.** It
  `execFileSync`s every marker it finds, and the register carries **two rows pointing at
  `node engine/game_differential.js --arm middle --team-store data/team-pool-frozen`** — a 1,200-game
  differential — plus `tests/roster.js --stage moves`, `all_mechanics_fire.js --kind abilities`,
  `probe_corpse_in_slot.js --games 1200`, `engine/wire_ladder.js`, `quarantine.js --whole-game` and 29
  rows on `tests/test-mechanics.js`. That is heavy, it plays games, and it would run every one of them
  against an `engine/medicham2-browser.js` that another agent is mid-edit on. **#403 therefore still
  sits in `staleRows` off the old artifact and will move to `debt` on the next safe run.** It belongs
  to whoever owns the next gate pass, on a clean tree.
- **Three `INSTRUMENT OWED` debts declared and none exists:** (a) a staged contact move into a live
  Wandering Spirit holder and a live Mummy holder asserting the announcement body, the field count and
  that an acquired Intimidate applies its drop, with a fainting-holder arm as the control; (b) a staged
  Moonblast with a Fairy Aura holder entering, leaving and fainting on the turn of the hit, against a
  knob-cleared non-aura control; (c) an arm that stages the Encore'd second Protect and records whether
  a die was drawn at all and **at which address** — the count is the symptom, the address is the claim.
- **#463's `stall` residue has no row of its own and does not need one** — it is written into
  `_stallExpire`'s header and now into #463's addendum. If anyone wants it gated, that is a new row.
- **REPORTED, NOT ACTED ON: #463's row is a malformed table row.** It carries **14 unescaped pipes**
  where a three-cell row needs 4 — its quoted protocol lines (`|upkeep|`, `|win|B`) were never escaped —
  so it renders as roughly eleven extra columns. No gate reads it, `test-roadmap-register.js` is green,
  and I did not repair it: the text is dated evidence and rewriting its bytes is not this pass's call.
  My addendum's own pipes are all escaped, so the row is no worse.
- **`node engine/status.js --write` was NOT run**, for the same reason the previous pass gave: it
  restamps the `<!-- GENERATED -->` blocks in every division ledger and `docs/ENGINE.md` is owned by
  another agent right now.
- **CONCURRENCY, OBSERVED.** `docs/ROADMAP.md` grew by six rows (#535–#540) between a grep at 04:20 and
  an insert at 04:58 — my first insert landed after §5.8's two-column table because the file had moved
  underneath it, and was moved to §5.6 in a second surgical pass. Every edit is a unique-string
  replacement over a file read seconds earlier and no other row moved, but a full-file rewrite by
  anyone else would silently drop one side's work.
