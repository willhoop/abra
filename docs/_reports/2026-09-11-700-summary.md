# 2026-09-11 — MEASURE: the 7.0.0 fold-in of `docs/SUMMARY.md`

Historical findings record (see CLAUDE.md — `docs/_reports/` is never maintained, never cited as current
state, and is superseded by the register rows and artifacts it feeds).

## Scope

One document only: `docs/SUMMARY.md` and its PDF. The white paper, the deck, the technical docs and
MODELS were held by other agents in this pass and were not touched. No test was run that writes the
shared docs baseline (`tests/test-docs-current.js`), no git command was run, and no game was played.

## What landed

- Header bumped to `**Version 7.0.0 · 2026-09-11 · Will Hooper**` (`docs_scan.versionHeader` parses it
  as `7.0.0` at line 3).
- A new dated `7.0.0` block at the head of the document: a fourteen-row table of the current gate
  instruments, each row citing the artifact it is read from, followed by the major-revision
  justification, the twenty-seven-row fold-in statement, and the one-line list of engine fixes since
  6.0.0.
- A dated `UPDATE` under the existing 2026-09-11 correction recording that #511 (6.22.0), #318 (6.24.0),
  #601 and #440 (6.26.0) closed. The 2-of-9 and 1-of-9 readings above it are kept as dated evidence.
- `The components at a glance`: MEDICHAM's status cell gains a dated 7.0.0 lead; MAG, WOBBUFFET and
  SLOWKING each gain an explicit **not re-run**; a new row groups MILTANK, PORYGON2, DODUO and GARY as
  downstream and carries no figure.
- `The plan, four phases`: a dated line recording that phase 1 was reopened and worked, with the current
  state deferred to the gate rather than the box.
- `Measurement validity`: ten new rows for the instrument work of the 6.x series — staging planner, one
  scope, coverage counters, the register, exit codes, release tracking (RULE 5), figure traceability,
  the docs gate, conformance, and what a search may assume.
- A dated store paragraph (sharding, the 100 MB limit, quality filter 1.4.0, the corrupt-winner
  declaration) and a dated divisions paragraph naming what each of the five divisions owes.

## Figures and how each is bound

Every figure is bound in its own row or paragraph, either to an artifact that holds the value or to the
CHANGELOG entry that records it. Read from artifacts on release `534442d71183`:
`data/game-differential.json` (961 games, 961 never board-diverged, 10,705 turn boundaries compared and
identical, end-state verdicts 960 / THREW 1), `data/engine-diff.json` (6,000 compared, 0 disagreed,
volley move rows 130, bond rows 12, seed 20260804), `data/roster.items.json` (142 of 148),
`data/roster.abilities.json` (139 of 200 in scope, 116 of 316 out of scope, 13 CONTROL-NOT-QUIET),
`data/roster.moves.json` (486 of 497, 3 of 500 with no legal carrier), `data/mechanics-census.json` (883
live of 883 probed), `data/all-mechanics-fire.json` (4,702 games, planner games 3,836). The corner arms
are read from the two `data/verification/game-differential-{top,bottom}-tie-first.json` artifacts on
release `b42b81899631` and are labelled as NOT re-measured on the current release. Coverage (740 to 783
of 845) is bound to CHANGELOG 6.14.0 and marked as printed by `node engine/coverage.js` rather than
typed. Store and instrument figures are bound to CHANGELOG 6.3.0, 6.6.1, 6.6.2, 6.4.1, 6.6.0, 6.9.1,
6.9.2, 6.10.0, 6.12.1, 6.12.2, 6.12.3, 6.13.0, 6.14.1 and 6.2.0.

Artifact values were read twice, eight minutes apart, with mtimes unchanged between the reads, because
the gate artifacts had been rewritten four minutes before the first read.

All 44 Pokémon entity names used in the new text (24 moves, 16 abilities, 4 items) were checked against
`Dex.forFormat('gen9championsvgc2026regmb')` with the legality filter. None is non-standard in Reg M-B.

## `<<GATE>>` placeholders

Four, for the coordinator to fill from a verified run: lines **5**, **9**, **1314** and **1419**.
Everything that depends on the gate is phrased to survive either reading — the withheld-set row and the
leaf-calibration row both say that no downstream figure is quoted here either way, because an open gate
makes those artifacts re-runnable rather than true, and none was re-run (ROADMAP #57).

## Checks run (read-only)

`docs_scan.untraceableCensus` and `docs_scan.citationMismatches` over `docs/SUMMARY.md`, before and
after. Before: 0 unbound, 0 mismatches. After: **0 mismatches**, and **1 unbound, which is not from this
pass** (below). An intermediate run showed five unbound figures in the corner-arms row: naming
`state.games` / `state.games_board_never_diverged` beside figures made them named-leaf claims, which
resolve against one cited artifact only and are never rescued by the rest of the file, so with two
artifacts cited the pair could not bind. The row was rewritten to state board-material and protocol
counts without the field names, and to cite CHANGELOG 6.22.0 as well; it binds.

## One figure left unbound, and it is not this pass's

`docs/SUMMARY.md:1224` — *"64 games of 1,995 agree completely, against 6 games at the baseline"*, in the
3.68.0 RELEASE LADDER paragraph, which this pass did not touch. `data/wire-ladder.json` (the artifact
that paragraph tells the reader to read every figure from) holds neither 64 nor 1,995; it was last
written 2026-08-07. The grandfather list holds that sentence's hash for the value `1,995` but not for
`64`, so `64` was previously excused by its dated-block/blame position rather than grandfathered, and
inserting text above it shifted the line. It is left standing: the paragraph is dated evidence, the
figure is not mine, and editing a dated block to agree with today is what this repository forbids.
Owed to whoever next re-runs or retires the wire ladder.

## Not done here

The gate verdict itself (placeholder), the CHANGELOG 7.0.0 entry, the RUNNING-NOTES row and ROADMAP —
all MEASURE-owned elsewhere in this pass — and the other four living documents.
