# 2026-09-03 — the living-docs pass for 5.241.0

LIGHT MODE. No game was played, no heavy job was run. The only command executed against the repo was
`node tests/test-docs-current.js`.

**Result: the gate is GREEN — 23 passed, 0 failed, exit 0.** Before this pass it was 22 passed,
1 failed, with rule 2 naming the six documents below at 5.240.0 against a CHANGELOG top of 5.241.0.

Nothing was added to `data/docs-currency-baseline.json`. No ratchet was moved. No
`<!-- GENERATED -->` block was touched (none of the six files contains one).

---

## 1. What was brought current

Six documents moved 5.240.0 -> 5.241.0. Each gained a change record written in its own register, and
two gained a body edit because a body claim had gone stale or incomplete.

| document | header | new version record | body edit |
|---|---|---|---|
| `docs/ABRA-whitepaper.md` | 5.241.0 · 2026-09-03 | technical, both fixes, both measurement notes | — |
| `docs/ABRA-technical-docs.md` | 5.241.0 · 2026-09-03 | RULE / DEFECT / PROOF / ENGINE / COUNTERS / KNOBS / INSTRUMENTS / MEASURED, short sentences | — |
| `docs/ABRA-deck-plain-english.md` | 5.241.0 · 2026-09-03 | plain English, no identifiers, no line numbers | — |
| `docs/SUMMARY.md` | 5.241.0 · 2026-09-03 | whole-project summary | — |
| `docs/DAMAGE-STAGES.md` | 5.241.0 · 2026-09-03 | no stage moved; one road GAINED a stage | new §3 subsection, *"The crit, third road"* |
| `docs/MODELS.md` | 5.241.0 · 2026-09-03 | no model changed; the damage-table verdict | MAG section: `**Damage table:**` restated |

### 1a. The DAMAGE-STAGES body edit

`## 3. THE TWO NON-MODIFIERS` covered two crit roads, both about POSITION. It now carries a third
subsection about ABSENCE, because a stage that is never entered produces no wrong number to compare —
it produces the same number every time, which is why this document could sit correct for months while
the road was dead. The subsection states where the fix lands in this document's own terms: the payout
is re-priced through the certain-crit path that §3 already measures as correct at both endpoints,
rather than multiplied afterwards like the battle loop's rolled crit. It explicitly says the rolled-crit
defect is UNCHANGED, so the table above it is not read as superseded.

### 1b. The MODELS body edit

`## MAGNEMITE (MAG)` carried `**Damage table:** 318 species.` as a live line. That is stale. It is now
preceded by a dated restatement — the table holds 322 rows, the 2026-08-02 paragraph is left exactly
as written rather than rewritten in place, and the reader is pointed at the 5.241.0 record for the
argument and the bound.

---

## 2. The figures used, and where each came from

Every figure below was supplied verified by the coordinator. **No figure was authored in this pass**,
and no figure was derived, rounded, or restated in a second form.

| figure | used in |
|---|---|
| Census 827 -> 829 live / 829 probed / 0 missing / 0 hollow / 0 threw / 0 unarmed | whitepaper, technical docs, deck, SUMMARY, MODELS |
| delayed hit: authority 72 with `-crit` / 48 without; this engine 69 both times; `delayedHitCritDrawn` 0 | all six |
| authority anchors `data/conditions.ts:415`, `sim/battle-actions.ts:1156`, `:1636-1642`, `critMult = [0,24,8,2,1]` at `:1633`, x1.5 at `data/mods/champions/scripts.ts:222`, `\|-crit\|` at `:285`; no Champions `futuremove` key | whitepaper, technical docs, DAMAGE-STAGES |
| substitute: recoil -25 against -12, drain +62 against +21; `data/moves.ts:18341-18357`, no Champions override | whitepaper, technical docs, deck, SUMMARY, DAMAGE-STAGES, MODELS |
| `engine/feature_fixture.js:741` hashes `m.ty`, present on 0 of 322 rows; `wt` not hashed; NOT fixed | whitepaper, technical docs, deck, SUMMARY, MODELS |
| damage table: RESTAMP not refit; 91 touched rows all megas/in-battle formes; `st`,`bs`,`t`,`item`,`ab` moved on ZERO rows; 76 mega moveset rewrites; two `buildMon` call sites at `engine/board.js:1466` and `:3956`; mega `mv` reaches 0 of 16,830; upper bound 12 of 16,830 (0.07%) | whitepaper, technical docs, deck, SUMMARY, MODELS |

**A figure I wanted and did not use.** The 5.240.0 records quote the pinned pool as 961 games. A first
draft of the SUMMARY entry wrote *"the 961-game comparison was not run"*. That number was not on the
verified list, it describes a run that did not happen, and quoting it would have implied a sample size
for a measurement with no sample. It reads *"the whole-game comparison against the pinned pool of real
ladder games was NOT run"* instead.

**The pool is WITHHELD, not captioned.** All five documents that mention a scoreboard say the pinned
pool was not run and that no movement is claimed in either direction, and say why (light mode). None
of them prints a pool number with a warning attached, which is the failure mode CLAUDE.md names.

**No quarantined figure is quoted.** The MODELS entry states explicitly that the RESTAMP verdict is
not a clean bill of health, and that the weight vector remains withheld for reasons the damage table
has nothing to do with.

---

## 3. Constraints observed

- **Never hand-edit a `<!-- GENERATED -->` block** — verified by grep: none of the six documents
  contains one. The generated blocks that DO need restamping are in the division ledgers, and the
  command is in OWED.
- **`docs/_reports/` is not current state** — the two 2026-09-03 reports were read for narrative
  substance (which mechanism, which control, which order the fix lands in). Every NUMBER came from the
  verified list, not from those files.
- **One fact, one number** — the six entries were cross-checked figure by figure after writing. The
  technical-docs entry was corrected in place: it stated `m.ty` names a field no row has, without the
  `0 of 322 rows` the other five carry. It now carries it.
- **Nothing was deleted.** The stale `**Damage table:** 318 species` paragraph in MODELS is preserved
  as the dated record it is.

---

## 4. Things noticed and deliberately NOT changed

Reported, not touched — each belongs to another pass or another division.

1. **`docs/ROADMAP.md:1303` (#26) quotes a superseded damage-table digest.** It reads *"318->317
   species, `405c836793d1`->`1b66b563c229`"*. The 2026-09-03 verdict report reproduces the digest
   history and puts HEAD at 322 rows / `1bda9df11d73`, so the row on the register is describing the
   middle of three states. `docs/ROADMAP.md` is already staged-modified by the ENGINE pass and is not
   one of the six documents in this brief; I did not edit it. **This is a real currency defect in the
   register and it needs a pass of its own.**
2. **`docs/ENGINE.md:26685` and `:14230`** carry the same 318->322 framing. `docs/ENGINE.md` is
   ENGINE's ledger, is staged-modified in this same tree, and carries a generated block — out of scope
   here, and it must be restamped rather than hand-edited.
3. **The untraceable-figures census reports 35 figures across 6 documents** (14 MODELS, 10 whitepaper,
   1 technical docs, plus three documents outside this brief). It did not move in this pass. It is
   report-only by design, and clearing it is not this pass's job.

---

## 5. Gate output, verbatim on the load-bearing clauses

Before (staged tree, six documents at 5.240.0):

```
  FAIL every version-headed document is at 5.241.0 or is a declared pin (25 versioned, 19 pinned) — undeclared and stale:
         docs/ABRA-deck-plain-english.md @ 5.240.0
         docs/ABRA-technical-docs.md @ 5.240.0
         docs/ABRA-whitepaper.md @ 5.240.0
         docs/DAMAGE-STAGES.md @ 5.240.0
         docs/MODELS.md @ 5.240.0
         docs/SUMMARY.md @ 5.240.0
DOC CURRENCY TESTS: 22 passed, 1 failed
```

After:

```
  ok   every version-headed document is at 5.241.0 or is a declared pin (25 versioned, 19 pinned)
  ok   retracted figures restated as fact: no new entries (baseline 8, now 8)
  ok   figures a cited artifact does not contain: no new entries (baseline 66, now 66)
  ok   no living document gained untraceable figures (35 across 6 documents)
(no ratchet movement — data\docs-currency-baseline.json left untouched.)
DOC CURRENCY TESTS: 23 passed, 0 failed
```

Nothing was committed. The publisher commits.

---

# OWED — exact commands light mode stopped me running

Run from `C:\Users\willj\Projects\Pokemon\ABRA` with
`SHOWDOWN_PATH=C:/Users/willj/Projects/Pokemon/pokemon-showdown` set. Everything here either pins the
cores, plays a game, or writes a generated block.

**1. The generated blocks in the division ledgers are NOT restamped by this pass and are one pass
stale.** This is the only OWED item that belongs to this documentation pass specifically:

```
tools\lownode.cmd engine\status.js
tools\lownode.cmd engine\status.js --write
```

**2. The rest of the battery, which this pass is downstream of rather than responsible for.** These
are the ENGINE pass's own OWED list, repeated here so one file holds the whole debt for 5.241.0.
The full statement of each is in `docs/_reports/2026-09-03-crit-draw-and-substitute-clamp.md` §5:

```
node engine/engine_release.js cut "ROADMAP #419 delayed-hit crit draw + #416 substitute dealt clamp"
tools\lownode.cmd engine\game_differential.js --whole-game --release <NEW_ID> --team-store data/team-pool-frozen
tools\lownode.cmd tests\roster.js --stage items --reds
tools\lownode.cmd tests\roster.js --stage abilities --reds
tools\lownode.cmd tests\roster.js --stage moves --reds
tools\lownode.cmd engine\quarantine.js
tools\lownode.cmd tests\run-all.js
tools\lownode.cmd tests\test-engine-diff.js
```

`tests\test-engine-diff.js` is load-bearing for `docs/DAMAGE-STAGES.md` specifically. That document's
5.241.0 entry states the corner damage differential was NOT re-run and that the previous release's
figure must not be read as covering this change — a road that gained a crit stage can move a damage
number. **Until that command runs, the DAMAGE-STAGES entry is correct only because it withholds.**

**3. The restamp and its ruler fix, which the MODELS entry describes and which nobody has run.**
The order matters and is not negotiable — the ruler fix moves the digest, so it lands WITH the
restamp, never before it. Both need Will's acceptance first, because the target is MAG's:

```
node engine/feature_fixture.js --stamp data/policy-weights.json
node engine/feature_fixture.js --stamp data/joint-weights.json
node engine/feature_fixture.js --check data/policy-weights.json
```

**4. A register correction this pass could not make.** `docs/ROADMAP.md:1303` quotes a superseded
digest pair (§4 item 1). It needs an owner and a pass; there is no command for it.
