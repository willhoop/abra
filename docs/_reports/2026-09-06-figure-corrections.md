# Two published figures corrected — MEASURE, 2026-09-06

Dated findings record. Not a living document, not current state, never cited as either.

Version **5.259.0**. Applied to `docs/ABRA-whitepaper.md`, `docs/SUMMARY.md`, `docs/WEB.md`,
`CHANGELOG.md`, four masthead stamps, seven PDFs, and one ratchet line the test wrote itself.
Nothing was committed. **No engine job, differential, census, roster or release cut was run**, and
`node engine/status.js --write` was NOT run — an ENGINE agent was editing `engine/medicham2-browser.js`,
`engine/tag_dex.js`, `data/tags.json` and `data/abra-tags.js` throughout, and all of those are
modified in the tree right now. Neither correction lives inside a `<!-- GENERATED -->` block.

Artifacts were read at `HEAD` or checked against the clock first:

- `data/quality-filter.json` — mtime **2026-08-27 01:37**, ten days cold. Read at `HEAD` for the
  funnel and via `git show <commit>:` for the history.
- `data/winrate-backtest.json` — mtime **2026-08-04 03:09**. Stable, read live.
- `data/rollout-r4.json` — read live for the MILTANK ruling only.

This pass VERIFIED the prior report `docs/_reports/2026-09-06-stale-citations-and-parity-check.md`
independently rather than relaying it. Everything below was re-measured here.

---

## 1. What was verified before anything was applied

**The white paper's citation.** `data/quality-filter.json` has six commits under `git log --follow`.
A grep for `19589|8713` across all six returns **0 matches in every one**. A grep for turn-bearing
keys across all six returns exactly `min_turns` and `after_min_turns` in each — **no version has ever
carried a mean-turn field**, so `1.71x`, `7.4` and `4.3` were never citable to it either. The prior
report's central claim holds: this is a citation that never held, not a citation that went stale.

**The replacement figures are literal values in the artifact.** At `HEAD`, version 1.3.0,
`provenance.measured_on` 2026-08-27, `provenance.store_size` **67,384**, funnel
`after_min_turns` **26,142** to `after_full_bring` **18,908**. 7,234 is the subtraction of two named
fields and is written as such rather than quoted as a field.

**The WEB figure.** `data/winrate-backtest.json` contains `51.0` and `1314` — the citation was
faithful, which is exactly why every gate passed it. Its `measured_against` block records
`engine/medicham2-browser.js` at **134,648** bytes, sha `0710a325219e`, `measured_at`
2026-08-04T07:09:31Z. Its own summary string is the source of the 51.0% sentence.

**The MILTANK figure is quarantined AND was uncited — both, not either.** It is
`data/rollout-r4.json` (`arm1_share_pct: 55.5`, verdict string carrying *"engine moved since;
transfer assumed, not measured"*), regenerated by `engine/rollout_r4.js`. `docs/SEARCH.md` already
rules on it: *"The 55.5% is not retracted. It is unquotable."* `docs/WEB.md` cited nothing at all,
so the citation gate had nothing to compare and could not have fired. Verdict: **quarantined and
uncited**, treated as both.

---

## 2. Correction 1 — the full-bring sentence

Applied identically at `docs/ABRA-whitepaper.md` and `docs/SUMMARY.md`, in the same pass, so the
retraction registry cannot go red on a copy left behind. One paragraph became three:

1. the claim, now resting on `rules.require_full_bring.known_limitation` and the funnel step;
2. **CORRECTION**, with the prior claim quoted in full FIRST, then why the citation never held, then
   where the figures really came from;
3. the RATIO **withdrawn**, inside a strikethrough, with the condition that makes it quotable again.

**The counts are corrected, not retracted, and that distinction is load-bearing.** They were a real
one-off measurement over the UNION of the three raw, unfiltered stores at commit `fd59a4c7`
(2026-07-31) — a population the quality filter neither computes nor describes, which is why they run
about four times the funnel's. Calling them false would be the second wrong statement about them.

**They are deliberately NOT entered in the derived retraction registry, and this was checked rather
than assumed.** `engine/docs_scan.js` `retractionRegistry()` registers anything inside a strikethrough
on a line carrying a retraction verb, and `isDistinctive` admits any bare count at or above 1000. Had
those two counts been struck through on a verb line, they would have registered `strong` and then
accused `docs/THESIS-DEFENCE-REVIEW-2026-07-31.md:162` and `:163`, which state them as that review's
own direct measurement of that population on that date — a true dated statement, in a file this pass
was not authorised to edit. Two new red rows on a ratchet, for punishing the correct behaviour. So the
counts are stated as a misattribution and the RATIO carries the strikethrough; `1.71`, `7.4` and
`4.3` sit below the distinctiveness floor (not percents, all under 1000) and register nothing.

`citationMismatches` skips a paragraph whose text matches `QUALIFIED`, so the correction paragraph —
which contains "prior", "corrected" and "withdrawn" — is not read as a fresh mis-citation. That is
the mechanism the gate provides for exactly this, not a workaround.

---

## 3. Correction 2 — `docs/WEB.md` withholds, and there were THREE copies, not one

`docs/WEB.md` is LIVING, confirmed three ways: CLAUDE.md's living-docs rule names it as WEB's ledger;
`engine/docs_scan.js liveDocs()` enumerates every `.md` in `docs/` with one exemption, `CHANGELOG.md`;
and `data/docs-currency-baseline.json` lists it under `unversioned_exempt`, whose `note_unversioned`
reads *"EXEMPT FROM RULE 1 ONLY - they are still scanned by the retracted-number rules."* It is not in
`archive_grandfathered`, not in `docs/archive/`, and carries no `SUPERSEDED` header. WEB being a
paused division cuts the other way: a pause stops new work and does not make a published figure true.

**The brief named one instance. There were three.** A grep for the leaf figure in the file returned
lines 118, 335 and 347 — the Stadium caveat and two places in the model-map entry, one of which said
*"WEB still authors no number here"* while carrying the number. All three are now withheld; leaving
either of the later two would have been the identical defect in a quieter place.

Both withheld blocks are written in `engine/status.js`'s own shape, from `sayHeld()`:
*"<label>: QUARANTINED — the figure is withheld, not annotated"*, then the artifact and why it is
downstream, then the condition and the command. The commands are `node engine/backtest_winrate.js`
and `node engine/rollout_r4.js`, each confirmed as the sole writer of its artifact by grep over
`engine/*.js`. The only figure left in either block is **134,648**, which is
`measured_against["engine/medicham2-browser.js"].bytes` in the cited artifact and says nothing about
calibration. The size ratio is written in words — "more than twenty times" — because a derived ratio
against a file ENGINE is editing this minute would be wrong within the hour.

**Rule 1 of `tests/test-docs-current.js` already had a hand-typed entry for this figure**, and it was
PASSING, because its `allowIfNear` admits a nearby `CI` or `interval` and the sentence carried its
interval. That is a caption clearing a quarantine check. Removing the figure makes the entry moot
rather than merely satisfied.

---

## 4. Why nothing caught either, filed and not fixed here

- **`engine/docs_scan.js` has no quarantine clause.** Grep for `quarantin` returns nothing. Its
  citation rule asks *"is this figure in the artifact it cites?"* and a quarantined artifact answers
  yes. `engine/status.js` withholds the figure; `docs_scan.js` clears the document republishing it.
- **`docs/WEB.md` receives no generated stamp, ever.** `engine/status.js` iterates
  `SECTIONS = { ENGINE, MEASURE, SEARCH, OPS }`; WEB is not in the set and the file holds no
  `<!-- GENERATED -->` block. Every number in the WEB ledger is hand-typed and cannot self-correct —
  the fourteen-stale-handoffs mechanism, inside the ledger system that replaced them.
- **An uncited figure is invisible to the citation gate by construction.** MILTANK's share sat in a
  living document for weeks with nothing to check it against.

Both instrument gaps are MEASURE's to file. Neither is fixed in this pass.

---

## 5. Version and gates

**5.258.0 to 5.259.0** (minor). Six documents carry unpinned version headers and rule 2 requires all
six to track the CHANGELOG top, so all six moved: `ABRA-whitepaper`, `SUMMARY`,
`ABRA-deck-plain-english`, `ABRA-technical-docs`, `MODELS`, `DAMAGE-STAGES`. The last four received a
masthead bump and a 5.259.0 headline in each document's own register; only the white paper and the
summary carry the corrected text. `docs/WEB.md` has no version header and needs none.

```
node tests/test-docs-current.js                24 passed, 0 failed
node tests/test-roadmap-register.js             3 passed, 0 failed
node tests/test-register-cell-parse.js          PASSED
node tests/test-register-reality-readonly.js   10 passed, 0 failed
node build/build_pdfs.js --check               every document has a current PDF
```

**One ratchet moved, TIGHTER, and the test wrote it itself.** Rule 3b(b), figures a cited artifact
does not contain, went **67 to 59**: `data/docs-currency-baseline.json` retired exactly the eight
entries this pass removed — four `docs/ABRA-whitepaper.md` rows and the four matching
`docs/SUMMARY.md` rows, each keyed to `data/quality-filter.json`. `changelog_top_at_baseline` was
restamped 5.187.0 to 5.259.0. **That file was written by `tests/test-docs-current.js`, not by hand** —
the ratchet drops a retired entry automatically because removing one can only make the check
stricter, and the tool says so in its own output: *"the RATCHET moved, so this is a finding and
belongs in the same commit."* No written reason in that file was found wrong and none was edited.

Untraceable-figures census unchanged at 35 across 6 documents. Retraction violations unchanged at the
baseline of 8. Seven PDFs rebuilt, 0 failed.

**Backtick parity was checked per inserted line** — an `awk` count per line, all even — on every block
before it was spliced, including the CHANGELOG entry.

---

## 6. What this pass did not do

- Did not commit, did not push, did not delete anything.
- Did not run `engine/status.js --write`, any engine job, differential, census, roster or release cut.
- Did not touch `engine/`, `tests/`, `web/`, `docs/ENGINE.md` or `docs/MEASURE.md`. The `engine/`,
  `tests/roster.js` and most `data/` modifications in `git status` are the ENGINE agent's, not this
  pass's.
- **Reported and left alone:** eight untracked `data/verification/*longtail-C-*.json` files and four
  untracked `tests/probe_*.js` files, which appeared during this pass and belong to ENGINE.
- Did not re-derive the length ratio. That needs a pass through `engine/quality.js` over a PINNED
  store — the store is appended hourly by OPS — and it is a filter pass, not an engine job. It is the
  one thing owed before the withdrawn figure can be replaced by a measured one.
