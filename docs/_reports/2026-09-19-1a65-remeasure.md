# 6.57.0 re-measured on `1a6550ea5ec6` — 2026-09-19, ENGINE (measure only)

A findings record, not a living document. Superseded by the register rows it feeds; not cited as current
state. `node engine/status.js` and `node engine/quarantine.js` hold the current state.

---

## PREDICTION — written before any lattice was launched

Source: `docs/_reports/2026-09-19-8a41-remeasure.md` §3–§4 (the whole population on `8a4140de3eaa`) and the
four fix reports (`sucker-sash`, `narration-a`, `narration-b`, `tie-order`).

**Board-material 0 / 0 / 0** at `--games` 1200 / 1350 / 1950. The Sucker Punch game (`…2636045527`, g1350)
and the Focus Sash / Future Sight game (`…2657391947`, g1950) each replayed clean in the sucker-sash worktree.

**Narration (undeclared) 0 / 2 / 5**, from 0 / 10 / 23, if every fixed game plays clean to the end:

| lattice | bucket on `8a4140de3eaa` | fixed by | predicted |
|---|---|---|---|
| 1350 | spread no-foe 2, Synchronize 2, Roost 1 | narration A | LEAVE (5) |
| 1350 | sand residual `…2660691219` (weather corpse sort), Sitrus pair `…2635337217` (cached Speed) | tie order | LEAVE (2) |
| 1350 | mega after a +6 Helping Hand `…2660691219` (pair-redirect-priority) | narration B | LEAVE (1) |
| 1350 | Forewarn `-activate` (owner-deferred) | — | **STAY (2)** |
| 1950 | spread 1, Synchronize 1, Coaching 2, Roost 3 | narration A | LEAVE (7) |
| 1950 | Life Dew 2, end-of-turn order 2, reactions in one hit 3, Dragon Darts 1 | narration B (+ Electro Shot, tie order) | LEAVE (8) |
| 1950 | Perish slot order 2, Salt Cure / Infestation 1 | tie order (`…2634548064` then meets Coaching, fixed by A) | LEAVE (3) |
| 1950 | Chilly Reception 1, Sleep Powder under Misty 1, Forewarn 1, Mortal Spin 1, Magician 1 | — | **STAY (5)** |

Declared `fallenundefined` (Supreme Overlord) rows are expected to stay (raw = undeclared + 1 / + 2).

**Risks.** A fixed game can part again on its next divergence (narration B saw one of nine do so), so the
narration prediction is a floor. **Joins** are possible: 6.57.0 moved die paths (Sucker Punch into Future
Sight / Pollen Puff users, Sash / Sturdy on a payout or self-hit, Round promotion) and the in-move Update
now sorts on cached Speed (577 firings in 67 games in the tie-order worktree). The pivot White Herb fix is
board-changing in the Intimidate shape. Predicted joins: none, held weakly.

---

## 0. VERDICT

- **Board-material is 0 of 961, 0 of 1069 and 0 of 1497** at `--games` 1200 / 1350 / 1950. It was 0 / 1 / 1
  before. **This is the first time it has read zero on all three lattices.** The prediction was 0 / 0 / 0, and
  the runs read exactly that.
  - Sucker Punch into an Encored Swords Dance (`…2636045527`, g1350, turn 15) LEFT.
  - Future Sight into a Focus Sash Kleavor (`…2657391947`, g1950, turn 6) LEFT.
  - **No game joined**, on the board or in the protocol, on any lattice.
  - Every compared turn boundary is identical: 10705 of 10705, 11842 of 11842 and 16713 of 16713.
- **Undeclared narration is 0 / 2 / 6**, down from 0 / 10 / 23. The prediction was 0 / 2 / 5. Raw narration is
  0 / 3 / 8, less the declared Supreme Overlord `fallenundefined` row.
  - 25 games left.
  - Every game that stayed is at the same index as before, with one exception. `…2662455751` (g1950) was a
    Roost game at index 93. It now plays through the Roost line and parts at index 145 on a mechanism that was
    not in the list before: Parting Shot into a Hyper Cutter body. This one game is the whole of the prediction
    miss. It is a game that moved to its next divergence. It is not a join.
- **The gate reads `CLOSED — 1 of 9 GATING clauses fail`.** The one failing clause is the whole-game NARRATION
  clause, which is **now armed**. The last run read 1 of 8 and printed NARRATION as RPRT. It now counts as a
  gating clause and reads `NON-ZERO ON 2 OF 3 LATTICES`. The other eight clauses PASS, and whole-game
  BOARD-MATERIAL is one of them (`ZERO ON EVERY LATTICE`). **I do not declare the gate open, and it is not
  open.**
- **The roster, the damage diff and AMF read the same as on `8a4140de3eaa`.**
  - Roster items 148 of 148, abilities 194 of 200 and moves 494 of 497, with 0 DIFFER, 0 DID-NOT-FIRE and
    0 COULD-NOT-STAGE.
  - Anchors are live at 22/22, 63/63 and 37/37. Every red is CAUGHT (22 / 63 / 37).
  - The damage diff reads 0 of 6000 at the midpoint, the top, the bottom and idx01–idx14.
  - AMF played 4702 games and threw 0 times.

## 1. PINS

| pin | value |
|---|---|
| release | `1a6550ea5ec6`, cut 2026-09-19T11:11:47Z on HEAD `f72847ec` (6.57.0). `engine_release.js list` reads `0 of 27 files have moved since` |
| census | `data/mechanics-census.json` at HEAD, generated 11:11:34Z, 947 live. `steering.input_digest` is **`ef0eefdb9f2d`** in all three artifacts. The last run used `9f251986d34e` (925 live), so this is not a strict census-pinned before/after. Under `--steering empirical` the census is credited only; it does not select |
| team store | `--team-store data/team-pool-frozen`, pools `0d103fb9fa87` / `7e7a37ded7fc` / `a5ce76242f8d`. These are the same as the last run |
| flags | `--release 1a6550ea5ec6 --team-store data/team-pool-frozen --steering empirical --arm middle --end-state --games {1200,1350,1950} --write --out <canonical path>`, cap 50 (`TURNS_DEFAULT`). This matches the 8a41 run |
| mode | `A/middle/pins:de38d17e15a2/credit:observed-effect/v1/nature:real`, `driver_code_stable` true, driver code `3f21624ad50d`, 0 void |
| launcher | Each run went through `tools\lownode.cmd` as an argv vector (`spawn('cmd.exe', ['/c','tools\\lownode.cmd', ...])`), from a launcher I wrote in the session scratchpad |

**The dump is not from the `--write` run, and the reason is recorded here.** The `--write` runs for 1350 and
1950 wrote their artifacts, then exited 1 when they tried to write the dump. I had passed an absolute
`--dump-out`, and `D()` joins it onto the repo root (`ENOENT …\ABRA\C:\Users\…`). The artifacts were already on
disk by then: the log prints `-> …\data\game-differential.g1350.json` before the error. So I re-ran both
lattices **without `--write`**, on the same flags, with `--dump-out data/_1a65_dump_g<N>.json` (a relative
path). They played the same games: 3 of 3 and 8 of 8 diverging, with the same seeds and indices as the written
artifacts. The dumps were moved to the session scratchpad afterwards.

## 2. RESULTS

| `--games` | games | board-material (was) | narration undeclared (raw) (was) | protocol-diverged (was) | boundaries identical | generated | wall |
|---|---|---|---|---|---|---|---|
| 1200 | 961 | **0** (0) | **0** (0) (0) | 0 (0) | 10705/10705 | 11:30:48Z | 436 s |
| 1350 | 1069 | **0** (1) | **2** (3) (10) | 3 (12) | 11842/11842 | 11:32:16Z | 524 s |
| 1950 | 1497 | **0** (1) | **6** (8) (23) | 8 (26) | 16713/16713 | 11:33:43Z | 607 s |

Board-material is `state.games − state.games_board_never_diverged`. Narration is quarantine's NARRATION clause.
Left, stayed and joined come from `state.first_board_divergences` (0 / 0 / 0 entries) and `first_divergences`
(0 / 3 / 8 entries, cap 60). Both lists are the whole population. They are compared against `git show HEAD:` of
the three artifacts, which hold the `8a4140de3eaa` run.

| predicted | actual |
|---|---|
| 1350 Sucker Punch `…2636045527` LEAVE | **LEFT** (board and protocol) |
| 1950 Future Sight / Sash `…2657391947` LEAVE | **LEFT** (board and protocol) |
| narration 1350: 8 LEAVE, Forewarn 2 STAY | **exact.** 8 left, both Forewarn cards stayed at idx 6 and 38 |
| narration 1950: 18 LEAVE, 5 STAY | **17 left, 6 stayed.** The five named stays held at the same index. `…2662455751` (Roost) moved 93 → 145 onto Hyper Cutter |
| joins: none | **none**, on any lattice, board or protocol |

The games that left, by lattice. The fix that took each one is from the 8a41 bucket table.

- g1350 (9 games):
  - Sucker Punch `…2636045527`, board and protocol;
  - spread with no foe: `…2656658836` and `…2657573463`;
  - Synchronize: `…2654088012` and `…2654041758`;
  - Roost: `…2658575001`;
  - the sand corpse sort, `…2660691219` baseline;
  - the Sitrus pair: `…2635337217`;
  - mega after Helping Hand: `…2660691219` pair-redirect-priority.
- g1950 (18 games):
  - Sash / Future Sight `…2657391947`, board and protocol;
  - Perish order: `…2634548064` and `…2635249755`;
  - spread: `…2634671092`;
  - Roost: `…2656606681` and `…2634369113`;
  - the Life Dew pair: `…2634231341` and `…2635082691`;
  - end-of-turn order: `…2659688578`;
  - reactions in one hit: `…2656439218` and `…2654135529`;
  - Synchronize: `…2634370112`;
  - Dragon Darts: `…2662428145`;
  - the pivot and replacement order: `…2634665687` and `…2663429418`;
  - Salt Cure: `…2654068794`;
  - Coaching: `…2636014030` and `…2635841176`.

## 3. ROSTER, DAMAGE, AMF, GATE, COVERAGE

| artifact | before | after | reading |
|---|---|---|---|
| `data/roster.items.json` | 08:39:01Z | 11:26:21Z | 148 MATCH, 0 DIFFER, 0 DID-NOT-FIRE, 0 COULD-NOT-STAGE; anchors 22/22, 22 CAUGHT. Exit 0 |
| `data/roster.abilities.json` | 08:42:38Z | 11:27:45Z | 194 MATCH, 6 DEFERRED-BY-OWNER; anchors 63/63, 63 CAUGHT. Exit 0 |
| `data/roster.moves.json` | 08:46:09Z | 11:30:00Z | 494 MATCH, 3 DEFERRED-BY-OWNER; anchors 37/37, 37 CAUGHT. Exit 0 |
| `data/engine-diff.json` | 08:46:59Z | 11:44:19Z | 0 of 6000 at the midpoint, top, bottom and idx01–idx14 (seed 20260804, `--n 6000`). Dropped by exception: 1, the same Dragon Darts authority throw as before |
| `data/all-mechanics-fire.json` | 08:49:39Z | 11:35:10Z | 4702 games, 0 threw. Summary counts are identical to before: moves diverged 1 (Axe Kick STATE), abilities 1 (Supreme Overlord, declared) |

I first ran the damage diff without `--n`. It measured 150 rows and **refused to republish** over the
6000-row artifact (exit 3, ROADMAP #257). It rewrote the tracked `data/verification/engine-diff.n150.json`,
and I restored that file with `git restore`. It was my own run's write. The re-run at `--n 6000` published.

**`node engine/quarantine.js`** (through `tools\lownode.cmd`, exit 0): **`GATE: CLOSED — 1 of 9 GATING clauses
fail`**.

| clause | reading |
|---|---|
| game differential (damage) | PASS. Midpoint 0 of 6000, top 0/6000, bottom 0/6000, idx01–idx14 0/6000 |
| deliberate roster / items | PASS. 148 of 148 tested |
| deliberate roster / abilities | PASS. 194 of 200 tested |
| deliberate roster / moves | PASS. 494 of 497 tested |
| coverage / every used mechanic is measured | PASS. All 412 moves above 25 clicks are measured |
| whole-game BOARD-MATERIAL | **PASS. `ZERO ON EVERY LATTICE`**: 0 of 961 / 0 of 1069 / 0 of 1497 |
| whole-game NARRATION | **FAIL. `NON-ZERO ON 2 OF 3 LATTICES`**: 0 of 961 / 2 of 1069 / 6 of 1497. It is now a gating clause; the last run printed it as RPRT |
| mechanics / each one staged and compared | PASS. 2 diverge, 1 declared, 1 below the reach shelf, 0 left |
| no open, known engine defect | PASS. 194 verdicts read |

**`node engine/coverage.js`.** I ran it a second time because the first read landed within a minute of the
damage-diff write and printed its torn-read warning. The second run printed no such warning:

| row | value |
|---|---|
| board leaves compared | 55 (the ceiling; 0 uncompared leaves can stand at a boundary) |
| uncomparable leaves with a firing writer | 23 of 25 (`volatile:powershift` and `volatile:unburden` have no writer that fired) |
| move leaves whose EFFECT was exercised | 7 of 11 |
| staged mechanics that fired | 785 of 845 (moves 497/497, abilities 140/200, items 148/148) |
| fired mechanics with a board compared | 785 of 845 |
| tags with an engine consumer | 297 of 299 (`needsUntrackedState`, `readsOwnItem`) |
| tags with a census probe | 299 |
| moves the damage diff can compare | 500 |
| ranged mechanics fully staged | 0 of 8 |
| turn boundaries compared | 10705 |
| entities exercised in a real game | 844 of 879 |
| differential bodies on a REAL spread | 0 of 17536 |
| driver policies the gate quotes | 1 |

## 4. NARRATION BY MECHANISM: EVERY REMAINING GAME

I bucketed all 11 dump cards by hand (3 at g1350 and 8 at g1950, the whole population). The turn is the last
`|turn|` line in the card's `before` window. `0` means the split comes before `|turn|1` (lead switch-in), and
`—` means the window holds no `|turn|` line.

| family | mechanism | lattice | config | seed (first) | first div. index | turn | authority line / ours |
|---|---|---|---|---|---|---|---|
| **announcement** | Forewarn `-activate` missing (**owner-deferred**) | 1350 | pair-redirect-priority | `…2659123487` | 6 | 0 | `-activate\|p2b: Musharna\|ability: Forewarn\|Flare Blitz` / `\|turn\|1` |
| | | 1350 | pair-speedctrl | `…2657893729` | 38 | 2 | `-activate\|p1a: Musharna\|ability: Forewarn\|Muddy Water` / `\|turn\|3` |
| | | 1950 | omit-weather | `…2634615536` | 4 | 0 | `-activate\|p2a: Musharna\|ability: Forewarn\|Overheat` / `\|turn\|1` |
| | Mortal Spin's `[from]` / `[of]` on the Toxic Spikes removal | 1950 | omit-intimidate | `…2659871951` | 79 | — | `-sideend\|p2: B\|Toxic Spikes\|[from] move: Mortal Spin\|[of] p2b: Glimmora` / `-sideend\|p2: \|move: toxicspikes` |
| | Magician on a Focus Sash: an extra `-enditem [silent]` before the `-item` | 1950 | omit-intimidate | `…2662767282` | 119 | 8 | `-item\|p2b: Klefki\|Focus Sash\|[from] ability: Magician` / `-enditem\|p1b: Weavile\|focussash\|[silent][from] ability: magician` |
| **refusal line** | Chilly Reception `-fail` missing (after `-prepare`) | 1950 | omit-intimidate | `…2662362231` | 105 | 7 | `-fail\|p1b: Slowking` / `-weather\|snowscape\|[upkeep]`. The next line also differs: our Yawn `-end` lacks `[silent]` |
| | Sleep Powder into a sleeping body under Misty Terrain | 1950 | pair-protect-bust | `…2661874022` | 52 | 5 | `-fail\|p1a: Meowstic\|slp` / `-activate\|p1a: Meowstic\|move: Misty Terrain` |
| **line order (NEW)** | Parting Shot into Hyper Cutter: the authority refuses the Attack drop, then lowers Special Attack; ours does it in the other order | 1950 | pair-speedctrl | `…2662455751` | 145 (was 93, Roost) | 9 | `-fail\|p2a: Mawile\|unboost\|Attack\|[from] ability: Hyper Cutter` / `-unboost\|p2a: Mawile\|spa\|1`. Both lines follow in swapped order, then the two engines agree again |
| **declared** (not counted) | Supreme Overlord `fallenundefined` | 1350 | pair-speedctrl | `…2661010853` | 70 | 4 | declared: the authority is wrong |
| | | 1950 | omit-weather | `…2663628673` | 34 | 3 | declared |
| | | 1950 | pair-redirect-priority | `…2656799052` | 23 | 2 | declared |

**Undeclared: 1350 = 2 (Forewarn 2), 1950 = 6** (Forewarn 1, Mortal Spin 1, Magician 1, Chilly Reception 1,
Sleep Powder / Misty 1, Hyper Cutter order 1). By family: 5 announcements (3 of them owner-deferred), 2 refusal
lines, 1 line order.

**For the next fix wave.** Forewarn is Will's deferral, so it is not ENGINE's to fix. It holds **3 of the 8**
remaining games by itself (2 at g1350, 1 at g1950), and while it stays deferred the narration clause cannot
reach zero at g1350. The other five are single-game mechanisms, and each has a one-line authority diff printed
above. A Forewarn ruling (declare it, or un-defer it) decides whether g1350 can close at all.

## 5. FILES CHANGED

- **Code:** none.
- **Data, written by the runs:**
  - `data/game-differential{,.g1350,.g1950}.json`;
  - `data/roster.{items,abilities,moves}{,.prev}.json` and `data/roster.json`;
  - `data/engine-diff.json` and `data/all-mechanics-fire.json`;
  - `data/provenance-stamp.json` and `data/published-samples.json`.
- **Docs:**
  - `CHANGELOG.md` (6.58.0);
  - `docs/RUNNING-NOTES.md` (one row);
  - `docs/ENGINE.md` (one section);
  - `docs/ABRA-whitepaper.md`: the roster sentence's citation `8a4140de3eaa` / 6.55.0 is replaced by
    `1a6550ea5ec6` / 6.58.0. Its figures did not change;
  - the five ledgers, restamped by `status.js --write`;
  - this report.
- **Not touched:**
  - the held 7.0.0 documents and their PDFs, `docs/_reports/2026-09-19-700-draft/` and
    `docs/_reports/2026-09-11-*`;
  - git: I ran no git command except one `git restore` of `data/verification/engine-diff.n150.json`, which my
    own refused run had rewritten.
- **Processes:** every process I started ended on its own. None was killed.
- **Scratch files:** my dumps, logs and launcher are in the session scratchpad. The scratchpad also holds files
  from earlier sessions (`_8a41_dump_*`, `*.patch`, `amf.*` and others). I did not run them or delete them.

## OWED, NOT RUN

1. **The version decision is the coordinator's.** Board-material is zero on all three lattices, and the gate
   stays CLOSED on the NARRATION clause (0 / 2 / 6). I wrote 6.58.0 with **Basis.** unchanged, as briefed. The
   gate has not opened.
2. **`data/releases/1a6550ea5ec6/` is untracked.** The white paper now cites it, so provenance will report
   "published figure on an untracked release" until the commit runs `git add -f` on it. 6.56.0 did the same for
   earlier releases.
3. **`tests/test-docs-current.js` reads 35 passed, 2 failed.** Every NEW entry in both failing clauses is in a
   held 7.0.0 document:
   - the deck: `487` and `883`;
   - the technical docs: `487`, plus 12 untraced figures;
   - MODELS: `487` and `883`;
   - SUMMARY: `487` and `883`.

   None of them is in a file this pass wrote. The commit will need those documents set aside again, as it did
   for 6.55.0.
4. **Narration fix wave, from §4:**
   - Parting Shot into Hyper Cutter line order (new);
   - Chilly Reception `-fail`;
   - Sleep Powder under Misty Terrain;
   - Mortal Spin `[from]`;
   - Magician's extra `-enditem`;
   - Forewarn, which needs Will's ruling first.

   Write a probe first for each one.
5. **Not claimed:** a census-pinned before/after. The census moved from `9f251986d34e` to `ef0eefdb9f2d`
   between the two runs.
6. **Seen, not touched:** `status.js` prints `FEATURE SEMANTICS CHECK FAILED` for `data/policy-weights.json`
   (fixture 10 → 12 scenarios; damage table 318 → 322 species). That is MAG/MEASURE, it was already there, and
   it is not mine.
