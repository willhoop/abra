# Living docs brought to 5.257.0 — the driver that was handed one option, and two corrections

Dated findings record. Not maintained, not current state, superseded by whatever the gate prints.
Documentation-only pass: no game was played, nothing under `engine/` or `tests/` was touched,
`docs/ROADMAP.md` was not touched, `data/docs-currency-baseline.json` was not touched, nothing was
committed.

## What the killed pass had already done: NOTHING

Checked before writing a word, in both directions — the version headers, and the content markers of
the 5.257.0 material (`22.2%`, `prefer_narrowed`, `20.79`, "sixteen remaining", the string `5.257`).

| file | header before this pass | 5.257.0 content before this pass |
|---|---|---|
| `docs/ABRA-deck-plain-english.md` | 5.256.0 | none |
| `docs/ABRA-technical-docs.md` | 5.256.0 | none |
| `docs/ABRA-whitepaper.md` | 5.256.0 | none |
| `docs/DAMAGE-STAGES.md` | 5.256.0 | none |
| `docs/MODELS.md` | 5.256.0 | none |
| `docs/SUMMARY.md` | 5.256.0 | none |
| `docs/MEASURE.md` | unversioned ledger | none |
| `docs/ENGINE.md` | unversioned ledger | none |

The killed pass's last words were "Now DAMAGE-STAGES, MODELS and SUMMARY drafts", which reads as
though the first three were finished. They were not: the only uncommitted movement in `docs/` was
four `<!-- GENERATED -->` restamps in `ENGINE`, `MEASURE`, `OPS` and `SEARCH` left by an earlier
`status.js --write` (provenance counts, census timestamp, store counts, `_stamped 2026-09-05 02:59`).
**Nothing was double-inserted, because nothing had been inserted.**

## What moved in this pass

| file | change |
|---|---|
| `docs/ABRA-whitepaper.md` | 5.256.0 → 5.257.0; eight blocks above the 5.256.0 material |
| `docs/ABRA-deck-plain-english.md` | 5.256.0 → 5.257.0; eight plain-English blocks |
| `docs/ABRA-technical-docs.md` | 5.256.0 → 5.257.0; eight STE blocks (DEFECT / MEASUREMENT / CONTROL / REPAIR) |
| `docs/DAMAGE-STAGES.md` | 5.256.0 → 5.257.0; four blocks — no stage moved, and the re-run is owed twice over |
| `docs/MODELS.md` | 5.256.0 → 5.257.0; six blocks; quarantine unchanged, no fitted vector written |
| `docs/SUMMARY.md` | 5.256.0 → 5.257.0; headline, artifact/arm table, three blocks |
| `docs/MEASURE.md` | new dated section under the GENERATED block (unversioned ledger) |
| `docs/ENGINE.md` | new dated section under the GENERATED block (unversioned ledger) |

Every insertion is an ADDITION above the previous dated material. No dated block was rewritten; the
5.256.0 blocks stand where they were and are superseded from above, in the wording the corrections
require ("this version's changelog note records … that is withdrawn").

## The two corrections, written as corrections in all eight

1. **The joint-arm non-determinism is WITHDRAWN.** Named as published — the 5.257.0 changelog note's
   "167, 167 and 138 on identical pins … under investigation … every joint figure published tonight —
   110, 53, 167, 138 — is one draw from an uncharacterised distribution". Replaced with the six-run
   split at the protect fix (02:27): `121/34, 121/34, 138/53` with `prefer_narrowed` 20,507 / 20,507 /
   20,353 on the driver as it was, `167/69, 167/69, 147/55` with `prefer_narrowed` 0 on the repaired
   driver, bit-identical within each side down to `credit_events` and `shuffle_calls`. **The joint 53
   stands and repeats.** The larger defect — the pins freeze every INPUT and never froze the
   instrument, `arms_comparable` printing COMPARABLE about two runs playing different driver code,
   and its own limits block having named the hole in prose — is carried in every document, with
   "a named limit is not a guard" as the transferable line.
2. **The empirical arm on the repaired driver reads 55 board-material, not 47.** Both readings named:
   47 is the same run through the pre-widening comparator at 40 leaves and stays correct as the second
   leg of the paired protect measurement; 55 is the widened comparator at 54 leaves, protocol 147.

Also carried everywhere: the residual protect rate at **1.53x** its own input as a *different,
measured* defect (a marginal `P(move|species)` renormalised onto the four moves a body carries, row
mass 0.917 over 8 becoming 0.521 over about 3.1, Protect always surviving the subsetting) — named,
not fixed. And the arm/artifact rule on every whole-game figure, with `data/game-differential.json`
still holding **46 of 961** and still being what the gate prints.

## How the drafts were checked before insertion

Each draft went through `engine/docs_scan.js`'s own shipping functions before it touched a document —
`untraceableCensus` with `read` injected, `citationsIn` + `artifactHas` per cited artifact,
`retractionRegistry`, plus a per-line backtick-parity count. Every draft returned **0 untraceable
figures**, no new citation mismatch and no new retraction-registry entry. A dry run over every figure
in the brief found exactly one that no artifact and no changelog line carries — **17,387**, the
"would have narrowed" count — so it appears in no document.

One fragile construction was removed rather than relied on: a paragraph naming `data/move-priors.json`
would have been judged by the cited-artifact clause, and passed only because the word "priors" happens
to match that clause's QUALIFIED escape. The artifact is named without the `data/` prefix instead, so
the paragraph is judged by the census, which it passes on its own figures.

Line endings were measured per file rather than assumed: all eight are **100% CRLF**, and the inserter
refuses a mixed-ending file and writes in the ending it measured. Post-edit: whitepaper 2304/0,
deck 1592/0, technical 1925/0, DAMAGE-STAGES 1050/0, MODELS 2330/0, SUMMARY 1281/0, MEASURE 6391/0,
ENGINE 39864/0 (CRLF/LF).

No `<!-- GENERATED -->` block was hand-edited. The only deletions in `ENGINE.md` and `MEASURE.md`
belong to the earlier `status.js --write` and were already in the tree; this pass's diff in both files
is a pure insertion beneath `<!-- /GENERATED -->`.

## The gate

`node tests/test-docs-current.js` — **24 passed, 0 failed.** It was red before this pass on exactly
one clause (six version-headed documents stale at 5.256.0) and that clause is now green. The
untraceable census is unchanged at **35 across 6 documents**, the citation-mismatch ratchet at 66 and
the retraction ratchet at 8, all with **no ratchet movement**, so `data/docs-currency-baseline.json`
was left untouched by the run as well as by me.

## OWED

- **`node engine/status.js --write` has not been run since 02:59.** Every `<!-- GENERATED -->` block
  in the ledgers is stamped to that pass, and both new sections say so in their own text. This pass
  did not run it.
- **`CHANGELOG.md`'s own 5.257.0 note still carries the withdrawn claim** ("Under investigation;
  until it is settled, every joint figure published tonight … is not quoted as a result"). The eight
  documents now supersede it and name it as withdrawn; the changelog block itself was not touched,
  because it is a dated record and it was outside the eight files this pass was given. A correction
  appended inside the 5.257.0 entry — never rewriting what it published — is owed.
- **The stage-by-stage instrument is owed a re-run**, now for a second reason: `medicham2-browser.js`
  moved under it (recorded at 5.256.0), and the artifact predates the instrument stamp so it cannot
  say which driver bytes produced it.
- **`tests/test-game-differential.js` is red on 4 clauses** and needs a clean-tree re-run before it is
  diagnosed. Untouched here — this pass ran nothing under `tests/` except the docs gate.
- **Leaf calibration stays WITHHELD**, the MAG refit stays OWED as a refit, and the whole-game noise
  floor is still unmeasured across both arms.
- **Nothing was committed.** Eight documents are modified on disk beside the pre-existing engine and
  data changes.
