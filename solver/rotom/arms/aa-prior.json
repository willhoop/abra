{
 "name": "aa-prior",
 "registered": "2026-09-25",
 "question": "A/A placebo on the first ladder bot: two identical DODUO-greedy (prior) arms. The residual difference must read zero; it checks login, rating capture and bookkeeping on the real server.",
 "arms": {
  "A": {
   "policy": "prior",
   "max_ms": 0,
   "preview_max_ms": 20000
  },
  "B": {
   "policy": "prior",
   "max_ms": 0,
   "preview_max_ms": 20000
  }
 },
 "assignment": "per series, sha256(seed|k|arm) mod 2 over the sorted arm ids (solver/rotom/ladder.js assign); committed before the first game",
 "metric": "per-series residual r = S - E from the server's pre-series ratings; ties S = 0.5",
 "burn_in": "series below a 1300 pre-series rating are burn-in",
 "sprt": {
  "statistic": "mean(r | A) - mean(r | B), normal approximation, sigma_r = 0.5",
  "h0": 0.0,
  "h1": 0.07,
  "alpha": 0.05,
  "beta": 0.05,
  "read": "once, at a bound",
  "expected": "accept H0"
 },
 "headline": "mean rating over the last N >= 100 series +- SD. Never the peak."
}